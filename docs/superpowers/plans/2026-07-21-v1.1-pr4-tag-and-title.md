# PR #4 · tag-and-title · Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 关闭 known-limitations.md §2 #1(自由标签)+ #7(历史会话标题 LLM 生成排查)。

**Architecture:**
- 后端新表 `tags` / `doc_tags`(db 作用域,多对多)
- 后端新文件 `backend/api/tags.py`(7 个路由:tag CRUD 4 + doc-tag 关联 2 + 按 tag 过滤 1)
- 后端改 `sqlite.py`:加 CRUD 函数 + ALTER TABLE 在 boot 阶段执行
- 后端改 `sessions.py`:排查流式标题触发,若未触发则修复
- 前端新组件 `TagChip.tsx` / `TagPicker.tsx`
- 前端改 `KnowledgePage.tsx`:sidebar 标签区 + 文档打标 + 过滤条
- 前端改 `global.css`:tag chip 样式
- 前端改 `backend/api/boot.py`:加 `schema_migration` 阶段

**Tech Stack:** FastAPI / SQLite / Pydantic / React 18 + TypeScript / Vitest

## Global Constraints

- **版本**:v1.1.0;本 PR 不动 version(PR #1 已设)
- **不修改**:`.env` / `package.bat` / `start.bat` / `stop.bat` / 容器镜像 tag
- **依赖**:不引入新第三方包
- **样式**:XAIAgent 令牌
- **单测**:`tests/unit/test_tags_api.py`(10 测)+ `tests/unit/test_session_title.py`(3 测)
- **作用域决策**:tag 跨 db 同名允许(UNIQUE(database_id, name));`database_id='default'` 为默认值

## File Structure

| 文件 | 责任 |
|---|---|
| `backend/core/sqlite.py` | 新表 + CRUD 函数 + ALTER TABLE |
| `backend/api/tags.py`(新) | 7 个路由 |
| `backend/main.py` | 注册 tags 路由 |
| `backend/api/boot.py` | 加 schema_migration 阶段 |
| `backend/api/sessions.py` | 排查 + 修复流式标题 |
| `frontend/src/components/TagChip.tsx`(新) | 标签 chip 组件(3 mode) |
| `frontend/src/components/TagPicker.tsx`(新) | 标签多选弹窗 |
| `frontend/src/pages/KnowledgePage.tsx` | sidebar 标签区 + 文档打标 + 过滤 |
| `frontend/src/lib/api.ts` | 加 tag CRUD API |
| `frontend/src/styles/global.css` | tag 样式 |
| `tests/unit/test_tags_api.py`(新) | 10 测 |
| `tests/unit/test_session_title.py`(新) | 3 测 |

---

## Task 4.1: 后端 `tags` + `doc_tags` 表 + CRUD 函数

**Files:**
- Modify: `backend/core/sqlite.py:init_db`
- Modify: `backend/core/sqlite.py`(加 CRUD 函数)
- Test: `tests/unit/test_tags_api.py`

- [ ] **Step 1: 写失败测试 — tags 表创建**

```python
# tests/unit/test_tags_api.py
from backend.core.sqlite import init_db, list_tags


def test_init_db_creates_tags_table(tmp_path):
    """init_db 应建 tags 表。"""
    db_path = tmp_path / "test.db"
    init_db(db_path)
    conn_path = db_path
    import sqlite3
    conn = sqlite3.connect(str(conn_path))
    cur = conn.execute(
        "SELECT name FROM sqlite_master WHERE type='table' AND name='tags'"
    )
    assert cur.fetchone() is not None
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /e && backend/.venv/Scripts/python -m pytest tests/unit/test_tags_api.py::test_init_db_creates_tags_table -v`
Expected: AssertionError

- [ ] **Step 3: 改 `backend/core/sqlite.py:init_db` 加表创建**

定位 init_db 函数,在现有 `CREATE TABLE` 之后追加:

```python
        # v1.1.0 PR#4: 自由标签 — 独立于 Database 的多对多标签
        conn.executescript(
            """
            CREATE TABLE IF NOT EXISTS tags (
                id          INTEGER PRIMARY KEY AUTOINCREMENT,
                database_id TEXT    NOT NULL,
                name        TEXT    NOT NULL,
                color       TEXT    NOT NULL DEFAULT '#FF540E',
                created_at  TEXT    NOT NULL,
                UNIQUE(database_id, name)
            );

            CREATE TABLE IF NOT EXISTS doc_tags (
                source      TEXT    NOT NULL,
                tag_id      INTEGER NOT NULL,
                created_at  TEXT    NOT NULL,
                PRIMARY KEY (source, tag_id),
                FOREIGN KEY (tag_id) REFERENCES tags(id) ON DELETE CASCADE
            );

            CREATE INDEX IF NOT EXISTS idx_doc_tags_tag ON doc_tags(tag_id);
            """
        )
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /e && backend/.venv/Scripts/python -m pytest tests/unit/test_tags_api.py::test_init_db_creates_tags_table -v`
Expected: PASS

- [ ] **Step 5: 在 `sqlite.py` 加 CRUD 函数**

定位 sqlite.py 末尾,在最后一个函数后追加:

```python
# ---------------------------------------------------------------------------
# Tags (v1.1.0 PR#4)
# ---------------------------------------------------------------------------


def create_tag(
    database_id: str,
    name: str,
    color: str = "#FF540E",
    db_path: Optional[Path] = None,
) -> int:
    """Create a tag. Returns tag id. Raises ValueError on duplicate name within db."""
    if not database_id or not name:
        raise ValueError("database_id and name required")
    conn = get_connection(db_path)
    try:
        cur = conn.execute(
            """
            INSERT INTO tags (database_id, name, color, created_at)
            VALUES (?, ?, ?, ?)
            """,
            (database_id, name, color, _now_iso()),
        )
        conn.commit()
        return cur.lastrowid
    except sqlite3.IntegrityError as exc:
        raise ValueError(f"tag '{name}' already exists in database '{database_id}'") from exc
    finally:
        conn.close()


def list_tags(database_id: str, db_path: Optional[Path] = None) -> List[dict[str, Any]]:
    conn = get_connection(db_path)
    try:
        cur = conn.execute(
            "SELECT * FROM tags WHERE database_id = ? ORDER BY name",
            (database_id,),
        )
        return cur.fetchall()
    finally:
        conn.close()


def get_tag(tag_id: int, db_path: Optional[Path] = None) -> Optional[dict[str, Any]]:
    conn = get_connection(db_path)
    try:
        cur = conn.execute("SELECT * FROM tags WHERE id = ?", (tag_id,))
        return cur.fetchone()
    finally:
        conn.close()


def update_tag(
    tag_id: int,
    name: Optional[str] = None,
    color: Optional[str] = None,
    db_path: Optional[Path] = None,
) -> None:
    conn = get_connection(db_path)
    try:
        fields = []
        params: List[Any] = []
        if name is not None:
            fields.append("name = ?")
            params.append(name)
        if color is not None:
            fields.append("color = ?")
            params.append(color)
        if fields:
            params.append(tag_id)
            conn.execute(
                f"UPDATE tags SET {', '.join(fields)} WHERE id = ?",
                params,
            )
            conn.commit()
    finally:
        conn.close()


def delete_tag(tag_id: int, db_path: Optional[Path] = None) -> None:
    """Delete tag (CASCADE removes doc_tags rows)."""
    conn = get_connection(db_path)
    try:
        conn.execute("DELETE FROM tags WHERE id = ?", (tag_id,))
        conn.commit()
    finally:
        conn.close()


def assign_tags_to_doc(
    source: str, tag_ids: List[int], db_path: Optional[Path] = None
) -> int:
    """批量打标。Returns number of rows inserted (skips existing)."""
    if not tag_ids:
        return 0
    conn = get_connection(db_path)
    try:
        rows = [
            (source, tid, _now_iso())
            for tid in tag_ids
        ]
        cur = conn.executemany(
            """
            INSERT OR IGNORE INTO doc_tags (source, tag_id, created_at)
            VALUES (?, ?, ?)
            """,
            rows,
        )
        conn.commit()
        return cur.rowcount
    finally:
        conn.close()


def unassign_tag_from_doc(
    source: str, tag_id: int, db_path: Optional[Path] = None
) -> int:
    conn = get_connection(db_path)
    try:
        cur = conn.execute(
            "DELETE FROM doc_tags WHERE source = ? AND tag_id = ?",
            (source, tag_id),
        )
        conn.commit()
        return cur.rowcount
    finally:
        conn.close()


def list_tags_for_doc(source: str, db_path: Optional[Path] = None) -> List[dict[str, Any]]:
    conn = get_connection(db_path)
    try:
        cur = conn.execute(
            """
            SELECT t.* FROM tags t
            JOIN doc_tags dt ON dt.tag_id = t.id
            WHERE dt.source = ?
            ORDER BY t.name
            """,
            (source,),
        )
        return cur.fetchall()
    finally:
        conn.close()


def list_documents_by_tags(
    database_id: str,
    tag_ids: List[int],
    db_path: Optional[Path] = None,
) -> List[dict[str, Any]]:
    """列出指定 db 下同时拥有所有 tag_ids 的文档 sources。

    Returns:
        [{"source": str, "tag_count": int}, ...]
    """
    if not tag_ids:
        return []
    conn = get_connection(db_path)
    try:
        # 通过 source 前缀推断 db
        placeholder = ",".join("?" * len(tag_ids))
        cur = conn.execute(
            f"""
            SELECT dt.source, COUNT(DISTINCT dt.tag_id) AS tag_count
            FROM doc_tags dt
            WHERE dt.tag_id IN ({placeholder})
              AND dt.source LIKE ?
            GROUP BY dt.source
            HAVING tag_count = ?
            ORDER BY dt.source
            """,
            (*tag_ids, f"{database_id}::%", len(tag_ids)),
        )
        return cur.fetchall()
    finally:
        conn.close()
```

- [ ] **Step 6: 写失败测试 — CRUD 完整路径**

```python
# tests/unit/test_tags_api.py 加:
def test_create_and_list_tags(tmp_path):
    """create_tag → list_tags 应返回该 tag。"""
    from backend.core.sqlite import init_db, create_tag, list_tags, DEFAULT_DATABASE_ID
    db_path = tmp_path / "test.db"
    init_db(db_path)
    tid = create_tag(DEFAULT_DATABASE_ID, "财务", db_path=db_path)
    tags = list_tags(DEFAULT_DATABASE_ID, db_path=db_path)
    assert len(tags) == 1
    assert tags[0]["name"] == "财务"
    assert tags[0]["color"] == "#FF540E"


def test_create_duplicate_tag_raises(tmp_path):
    """同 db 同名 → ValueError。"""
    from backend.core.sqlite import init_db, create_tag, DEFAULT_DATABASE_ID
    db_path = tmp_path / "test.db"
    init_db(db_path)
    create_tag(DEFAULT_DATABASE_ID, "财务", db_path=db_path)
    import pytest
    with pytest.raises(ValueError):
        create_tag(DEFAULT_DATABASE_ID, "财务", db_path=db_path)


def test_assign_and_list_doc_tags(tmp_path):
    """assign_tags_to_doc → list_tags_for_doc 应返回关联 tag。"""
    from backend.core.sqlite import (
        init_db, create_tag, assign_tags_to_doc, list_tags_for_doc,
        DEFAULT_DATABASE_ID,
    )
    db_path = tmp_path / "test.db"
    init_db(db_path)
    t1 = create_tag(DEFAULT_DATABASE_ID, "财务", db_path=db_path)
    t2 = create_tag(DEFAULT_DATABASE_ID, "Q3", db_path=db_path)
    assign_tags_to_doc("default::budget.pdf", [t1, t2], db_path=db_path)
    tags = list_tags_for_doc("default::budget.pdf", db_path=db_path)
    assert {t["name"] for t in tags} == {"财务", "Q3"}


def test_list_documents_by_tags_intersect(tmp_path):
    """list_documents_by_tags 应只返回同时拥有所有 tag 的文档。"""
    from backend.core.sqlite import (
        init_db, create_tag, assign_tags_to_doc, list_documents_by_tags,
        DEFAULT_DATABASE_ID,
    )
    db_path = tmp_path / "test.db"
    init_db(db_path)
    t1 = create_tag(DEFAULT_DATABASE_ID, "财务", db_path=db_path)
    t2 = create_tag(DEFAULT_DATABASE_ID, "Q3", db_path=db_path)
    assign_tags_to_doc("default::budget_q3.pdf", [t1, t2], db_path=db_path)
    assign_tags_to_doc("default::budget_q4.pdf", [t1], db_path=db_path)
    docs = list_documents_by_tags(DEFAULT_DATABASE_ID, [t1, t2], db_path=db_path)
    assert len(docs) == 1
    assert docs[0]["source"] == "default::budget_q3.pdf"
```

- [ ] **Step 7: 跑测试**

Run: `cd /e && backend/.venv/Scripts/python -m pytest tests/unit/test_tags_api.py -v`
Expected: 5 passed

- [ ] **Step 8: Commit**

```bash
git add backend/core/sqlite.py tests/unit/test_tags_api.py
git commit -m "feat(tags): v1.1 PR#4 - tags/doc_tags tables + CRUD functions"
```

---

## Task 4.2: 后端 `tags.py` API 路由

**Files:**
- Create: `backend/api/tags.py`
- Modify: `backend/main.py`
- Test: `tests/unit/test_tags_api.py`

- [ ] **Step 1: 写失败测试 — 路由注册**

```python
# tests/unit/test_tags_api.py 加:
from fastapi.testclient import TestClient
from backend.main import app


def test_tags_endpoints_registered():
    """tags 路由应注册到 app。"""
    paths = {r.path for r in app.routes}
    assert "/api/knowledge/databases/{db_id}/tags" in paths
    assert "/api/knowledge/tags/{tag_id}" in paths
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /e && backend/.venv/Scripts/python -m pytest tests/unit/test_tags_api.py::test_tags_endpoints_registered -v`
Expected: AssertionError(路由尚未注册)

- [ ] **Step 3: 创建 `backend/api/tags.py`**

```python
"""Tags API (v1.1.0 PR#4):独立于 Database 的自由标签。

Endpoints:
  GET    /api/knowledge/databases/{db_id}/tags
  POST   /api/knowledge/databases/{db_id}/tags
  PATCH  /api/knowledge/tags/{tag_id}
  DELETE /api/knowledge/tags/{tag_id}
  POST   /api/knowledge/doc-tags
  DELETE /api/knowledge/doc-tags/{source}/{tag_id}
  GET    /api/knowledge/databases/{db_id}/documents?tag_ids=1,2,3
"""
from __future__ import annotations

import logging
from typing import Any, Dict, List, Optional

from fastapi import APIRouter, HTTPException, Query
from pydantic import BaseModel, Field, conlist

from backend.core.sqlite import (
    assign_tags_to_doc,
    create_tag,
    delete_tag,
    get_tag,
    list_documents_by_tags,
    list_tags,
    list_tags_for_doc,
    unassign_tag_from_doc,
    update_tag,
)

logger = logging.getLogger(__name__)

router = APIRouter()


class CreateTagRequest(BaseModel):
    name: str = Field(..., min_length=1, max_length=64)
    color: str = Field(default="#FF540E", pattern=r"^#[0-9A-Fa-f]{6}$")


class UpdateTagRequest(BaseModel):
    name: Optional[str] = Field(default=None, min_length=1, max_length=64)
    color: Optional[str] = Field(default=None, pattern=r"^#[0-9A-Fa-f]{6}$")


class AssignTagsRequest(BaseModel):
    source: str = Field(..., min_length=1, max_length=512)
    tag_ids: conlist(int, min_length=1, max_length=50)


@router.get("/knowledge/databases/{db_id}/tags")
def list_tags_endpoint(db_id: str) -> List[Dict[str, Any]]:
    return list_tags(db_id)


@router.post("/knowledge/databases/{db_id}/tags")
def create_tag_endpoint(db_id: str, body: CreateTagRequest) -> Dict[str, Any]:
    try:
        tag_id = create_tag(db_id, body.name, body.color)
    except ValueError as exc:
        raise HTTPException(status_code=409, detail=str(exc))
    return get_tag(tag_id) or {}


@router.patch("/knowledge/tags/{tag_id}")
def update_tag_endpoint(tag_id: int, body: UpdateTagRequest) -> Dict[str, Any]:
    if not get_tag(tag_id):
        raise HTTPException(status_code=404, detail=f"tag {tag_id} 不存在")
    try:
        update_tag(tag_id, name=body.name, color=body.color)
    except Exception as exc:  # noqa: BLE001
        raise HTTPException(status_code=400, detail=str(exc))
    return get_tag(tag_id) or {}


@router.delete("/knowledge/tags/{tag_id}")
def delete_tag_endpoint(tag_id: int) -> Dict[str, Any]:
    if not get_tag(tag_id):
        raise HTTPException(status_code=404, detail=f"tag {tag_id} 不存在")
    delete_tag(tag_id)
    return {"deleted": True, "tag_id": tag_id}


@router.post("/knowledge/doc-tags")
def assign_tags_endpoint(body: AssignTagsRequest) -> Dict[str, Any]:
    inserted = assign_tags_to_doc(body.source, body.tag_ids)
    return {
        "source": body.source,
        "assigned_count": inserted,
        "tag_ids": body.tag_ids,
        "tags": list_tags_for_doc(body.source),
    }


@router.delete("/knowledge/doc-tags/{source}/{tag_id}")
def unassign_tag_endpoint(source: str, tag_id: int) -> Dict[str, Any]:
    deleted = unassign_tag_from_doc(source, tag_id)
    return {"deleted": deleted, "source": source, "tag_id": tag_id}


@router.get("/knowledge/databases/{db_id}/documents")
def list_documents_by_tags_endpoint(
    db_id: str,
    tag_ids: str = Query(..., description="逗号分隔 tag id 列表,如 1,2,3"),
) -> List[Dict[str, Any]]:
    ids = [int(x) for x in tag_ids.split(",") if x.strip()]
    if not ids:
        raise HTTPException(status_code=400, detail="tag_ids 不能为空")
    return list_documents_by_tags(db_id, ids)
```

- [ ] **Step 4: 在 `backend/main.py` 注册**

```python
from backend.api import boot, chat, dashboard, databases, debug, eval, health, knowledge, sessions, shutdown, tags

# ... 现有 include_router:
app.include_router(tags.router, prefix="/api", tags=["tags"])
```

- [ ] **Step 5: 跑测试**

Run: `cd /e && backend/.venv/Scripts/python -m pytest tests/unit/test_tags_api.py -v`
Expected: 6 passed(5 CRUD + 1 路由注册)

- [ ] **Step 6: 写端到端集成测试**

```python
# tests/unit/test_tags_api.py 加:
def test_create_tag_via_api(tmp_path, monkeypatch):
    """走 FastAPI TestClient 完整端到端。"""
    import os
    db_path = tmp_path / "test.db"
    os.environ["KB_AI_DB_PATH"] = str(db_path)
    # 重新 init
    import importlib
    from backend.core import sqlite as bs
    importlib.reload(bs)
    bs.init_db(db_path)

    client = TestClient(app)
    resp = client.post(
        "/api/knowledge/databases/default/tags",
        json={"name": "财务", "color": "#FF540E"},
    )
    assert resp.status_code == 200
    tag_id = resp.json()["id"]
    # 重名
    resp2 = client.post(
        "/api/knowledge/databases/default/tags",
        json={"name": "财务"},
    )
    assert resp2.status_code == 409
    # 删除
    resp3 = client.delete(f"/api/knowledge/tags/{tag_id}")
    assert resp3.status_code == 200


def test_assign_doc_tags_via_api(tmp_path, monkeypatch):
    """POST /api/knowledge/doc-tags 端到端。"""
    import os
    db_path = tmp_path / "test.db"
    os.environ["KB_AI_DB_PATH"] = str(db_path)
    import importlib
    from backend.core import sqlite as bs
    importlib.reload(bs)
    bs.init_db(db_path)

    client = TestClient(app)
    t1 = client.post(
        "/api/knowledge/databases/default/tags",
        json={"name": "财务"},
    ).json()
    t2 = client.post(
        "/api/knowledge/databases/default/tags",
        json={"name": "Q3"},
    ).json()
    resp = client.post(
        "/api/knowledge/doc-tags",
        json={"source": "default::budget.pdf", "tag_ids": [t1["id"], t2["id"]]},
    )
    assert resp.status_code == 200
    assert resp.json()["assigned_count"] == 2
    # 按 tag 过滤
    resp2 = client.get(
        f"/api/knowledge/databases/default/documents?tag_ids={t1['id']},{t2['id']}",
    )
    assert resp2.status_code == 200
    docs = resp2.json()
    assert any(d["source"] == "default::budget.pdf" for d in docs)


def test_cascade_delete_tag_removes_doc_tags(tmp_path, monkeypatch):
    """删 tag → doc_tags 关联 CASCADE 删。"""
    import os
    db_path = tmp_path / "test.db"
    os.environ["KB_AI_DB_PATH"] = str(db_path)
    import importlib
    from backend.core import sqlite as bs
    importlib.reload(bs)
    bs.init_db(db_path)

    client = TestClient(app)
    tag = client.post(
        "/api/knowledge/databases/default/tags",
        json={"name": "临时"},
    ).json()
    client.post(
        "/api/knowledge/doc-tags",
        json={"source": "default::x.pdf", "tag_ids": [tag["id"]]},
    )
    client.delete(f"/api/knowledge/tags/{tag['id']}")
    # 查 doc_tags 应空
    docs = client.get(
        f"/api/knowledge/databases/default/documents?tag_ids={tag['id']}",
    )
    assert docs.json() == []


def test_cross_db_isolation(tmp_path):
    """同 tag 名跨 db 互不影响。"""
    import os
    db_path = tmp_path / "test.db"
    os.environ["KB_AI_DB_PATH"] = str(db_path)
    import importlib
    from backend.core import sqlite as bs
    importlib.reload(bs)
    bs.init_db(db_path)
    bs.create_database("db_a", "A")
    bs.create_database("db_b", "B")

    client = TestClient(app)
    r1 = client.post(
        "/api/knowledge/databases/db_a/tags",
        json={"name": "财务"},
    )
    r2 = client.post(
        "/api/knowledge/databases/db_b/tags",
        json={"name": "财务"},
    )
    assert r1.status_code == 200
    assert r2.status_code == 200
```

- [ ] **Step 7: 跑测试**

Run: `cd /e && backend/.venv/Scripts/python -m pytest tests/unit/test_tags_api.py -v`
Expected: 10 passed(5 CRUD + 1 路由 + 4 端到端)

- [ ] **Step 8: 跑全套 ruff**

Run: `cd /e && backend/.venv/Scripts/python -m ruff check backend/`
Expected: 全绿

- [ ] **Step 9: Commit**

```bash
git add backend/api/tags.py backend/main.py tests/unit/test_tags_api.py
git commit -m "feat(api): v1.1 PR#4 - tags routes (7 endpoints)"
```

---

## Task 4.3: 后端 `boot.py` 加 schema_migration 阶段

**Files:**
- Modify: `backend/api/boot.py`

- [ ] **Step 1: 读现有 boot.py 阶段**

Run: `grep -n "stage\|percent\|orphan_recovery" backend/api/boot.py | head -20`

- [ ] **Step 2: 在 orphan_recovery 之后加 schema_migration 阶段**

定位 orphan_recovery 阶段末尾(7% 处),在它之后追加(8%):

```python
        # v1.1.0 PR#4: schema migration — 仅 tags/doc_tags 表(history_limit 在 PR#2 init_db 已加)
        yield {
            "event": "boot_progress",
            "data": json.dumps({
                "stage": "schema_migration",
                "percent": 8,
                "label": "应用 v1.1.0 schema migration (tags/doc_tags)",
            }),
        }
        try:
            from backend.core.sqlite import init_db
            init_db()  # idempotent;CREATE IF NOT EXISTS + ALTER 跳过已存在的列
        except Exception as exc:  # noqa: BLE001
            logger.warning("schema migration 失败(已存在?): %s", exc)
```

- [ ] **Step 3: 跑 boot 端点(若服务在线)**

```bash
cd /e && curl http://localhost:8000/api/boot
```
Expected: SSE 流中包含 `schema_migration` 阶段(8%)

- [ ] **Step 4: 跑现有 boot 端点测试**

Run: `cd /e && backend/.venv/Scripts/python -m pytest tests/unit/ -k boot -v`
Expected: 不退化(若没 boot 单测,跳到 step 5)

- [ ] **Step 5: Commit**

```bash
git add backend/api/boot.py
git commit -m "feat(boot): v1.1 PR#4 - schema_migration stage"
```

---

## Task 4.4: 后端 sessions.py 排查/修复流式标题

**Files:**
- Modify: `backend/api/sessions.py`

- [ ] **Step 1: 读现有 sessions.py 标题生成逻辑**

Run: `grep -n "title\|stream" backend/api/sessions.py | head -20`

- [ ] **Step 2: 加 debug 日志(临时排查)**

定位标题生成函数,加 logger.debug:

```python
    logger.debug(
        "session_title_stream: session_id=%s triggered=%s current_title=%s",
        session_id, triggered, current_title,
    )
```

- [ ] **Step 3: 写测试 — 标题流式触发**

```python
# tests/unit/test_session_title.py
from backend.core.sqlite import init_db, create_session, get_session
from backend.api.sessions import generate_session_title_if_needed
import logging


def test_title_generated_after_3_messages(tmp_path, caplog):
    """≥ 3 条消息后应触发标题生成。"""
    db_path = tmp_path / "test.db"
    init_db(db_path)
    sid = create_session(db_path=db_path, title="新会话")

    with caplog.at_level(logging.DEBUG, logger="kb_ai.sessions"):
        # 模拟 3 条消息
        from backend.core.sqlite import save_message
        for i in range(3):
            save_message(session_id=sid, role="user", content=f"问题 {i}",
                         citations_json=None, db_path=db_path)

        # 触发
        try:
            generate_session_title_if_needed(sid, db_path=db_path)
        except Exception:
            pass  # LLM 调用可能失败;只验证 debug 日志

    # 检查日志是否记录触发状态
    debug_logs = [r for r in caplog.records if "session_title_stream" in r.message]
    assert len(debug_logs) >= 1


def test_title_persists_after_generation(tmp_path):
    """生成的标题应持久化到 sessions.title。"""
    from backend.core.sqlite import save_message
    db_path = tmp_path / "test.db"
    init_db(db_path)
    sid = create_session(db_path=db_path, title="新会话")

    # mock LLM 返回固定标题
    from unittest.mock import patch
    with patch("backend.api.sessions._call_llm_for_title", return_value="财务 Q3 复盘"):
        for i in range(3):
            save_message(session_id=sid, role="user", content=f"q{i}",
                         citations_json=None, db_path=db_path)
        try:
            generate_session_title_if_needed(sid, db_path=db_path)
        except Exception:
            pass

    s = get_session(sid, db_path=db_path)
    # 断言:mock LLM 返回固定标题,持久化后 sessions.title 应被覆盖
    assert s["title"] == "财务 Q3 复盘", f"expected title to be updated, got {s['title']!r}"


def test_title_generation_idempotent(tmp_path):
    """多次调用不应重复触发。"""
    from unittest.mock import patch
    from backend.core.sqlite import save_message
    db_path = tmp_path / "test.db"
    init_db(db_path)
    sid = create_session(db_path=db_path, title="已设标题")

    # 已有非默认标题 → 不再覆盖
    with patch("backend.api.sessions._call_llm_for_title") as mock_llm:
        generate_session_title_if_needed(sid, db_path=db_path)
        mock_llm.assert_not_called()
```

> **注**:上面测试依赖现有 `generate_session_title_if_needed` 函数签名。**实际编码时需先读 sessions.py 现有实现**,可能函数名 / 签名不同,按真实代码调整测试和实现。

- [ ] **Step 4: 跑测试**

Run: `cd /e && backend/.venv/Scripts/python -m pytest tests/unit/test_session_title.py -v`
Expected: 3 passed

- [ ] **Step 5: 若测试失败 → 修复 sessions.py**

按测试暴露的具体问题修。可能问题:
- 函数未在 chat handler 调用
- 触发条件不对(应 ≥ 3 条消息,但可能 ≥ 5 条)
- LLM 调用后未 `UPDATE sessions SET title`
- 流式标题未持久化(只在内存)

- [ ] **Step 6: Commit**

```bash
git add backend/api/sessions.py tests/unit/test_session_title.py
git commit -m "fix(session): v1.1 PR#4 - debug + ensure title generation persists"
```

---

## Task 4.5: 前端 TagChip + TagPicker 组件

**Files:**
- Create: `frontend/src/components/TagChip.tsx`
- Create: `frontend/src/components/TagPicker.tsx`
- Modify: `frontend/src/styles/global.css`
- Test: `frontend/src/__tests__/TagChip.test.tsx`(新)

- [ ] **Step 1: 写失败测试 — TagChip 渲染**

```typescript
// frontend/src/__tests__/TagChip.test.tsx
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { TagChip } from '../components/TagChip';

describe('TagChip', () => {
  it('renders name with color background', () => {
    render(<TagChip id={1} name="财务" color="#FF540E" mode="readonly" />);
    const chip = screen.getByText('财务');
    expect(chip).toBeInTheDocument();
    expect(chip.style.backgroundColor).toBe('rgb(255, 84, 14)');
  });

  it('renders remove button in removable mode', () => {
    const onRemove = vi.fn();
    render(<TagChip id={1} name="财务" color="#FF540E" mode="removable" onRemove={onRemove} />);
    const removeBtn = screen.getByRole('button', { name: /删除/ });
    fireEvent.click(removeBtn);
    expect(onRemove).toHaveBeenCalledWith(1);
  });

  it('does not show remove in readonly mode', () => {
    render(<TagChip id={1} name="财务" color="#FF540E" mode="readonly" />);
    expect(screen.queryByRole('button', { name: /删除/ })).not.toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /e/frontend && npx vitest run src/__tests__/TagChip.test.tsx`
Expected: ImportError / module not found

- [ ] **Step 3: 创建 `frontend/src/components/TagChip.tsx`**

```typescript
import { useState } from 'react';

interface TagChipProps {
  id: number;
  name: string;
  color: string;
  mode?: 'readonly' | 'editable' | 'removable';
  selected?: boolean;
  onRemove?: (id: number) => void;
  onToggle?: (id: number) => void;
}

export function TagChip({
  id,
  name,
  color,
  mode = 'readonly',
  selected = false,
  onRemove,
  onToggle,
}: TagChipProps) {
  const handleClick = () => {
    if (mode === 'editable' && onToggle) onToggle(id);
  };

  return (
    <span
      className={`tag-chip ${mode} ${selected ? 'selected' : ''}`}
      style={{ backgroundColor: color }}
      onClick={handleClick}
      data-testid={`tag-chip-${id}`}
    >
      <span className="tag-chip-name">{name}</span>
      {mode === 'removable' && (
        <button
          className="tag-chip-remove"
          onClick={(e) => {
            e.stopPropagation();
            onRemove?.(id);
          }}
          aria-label={`删除标签 ${name}`}
        >
          ×
        </button>
      )}
    </span>
  );
}
```

- [ ] **Step 4: 加 CSS**

```css
.tag-chip {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 4px 10px;
  border-radius: 12px;
  color: white;
  font-family: 'Inter', sans-serif;
  font-size: 12px;
  font-weight: 500;
  cursor: default;
  user-select: none;
}

.tag-chip.editable {
  cursor: pointer;
}

.tag-chip.editable:hover {
  filter: brightness(1.15);
}

.tag-chip.selected {
  outline: 2px solid #FAFAFA;
  outline-offset: 2px;
}

.tag-chip-remove {
  background: transparent;
  border: none;
  color: white;
  font-size: 14px;
  line-height: 1;
  cursor: pointer;
  padding: 0 2px;
}

.tag-chip-remove:hover {
  color: #FF540E;
}
```

- [ ] **Step 5: 跑测试**

Run: `cd /e/frontend && npx vitest run src/__tests__/TagChip.test.tsx`
Expected: 3 passed

- [ ] **Step 6: 创建 `frontend/src/components/TagPicker.tsx`**

```typescript
import { useState } from 'react';
import { TagChip } from './TagChip';

interface TagPickerProps {
  availableTags: Array<{ id: number; name: string; color: string }>;
  initialSelectedIds?: number[];
  onConfirm: (selectedIds: number[]) => void;
  onCancel: () => void;
}

export function TagPicker({
  availableTags,
  initialSelectedIds = [],
  onConfirm,
  onCancel,
}: TagPickerProps) {
  const [selected, setSelected] = useState<Set<number>>(new Set(initialSelectedIds));

  const toggle = (id: number) => {
    setSelected((s) => {
      const next = new Set(s);
      if (next.has(id)) next.delete(id);
      else next.add(id);
      return next;
    });
  };

  return (
    <div className="modal-backdrop" role="dialog">
      <div className="modal">
        <h3>选择标签</h3>
        <div className="tag-picker-grid">
          {availableTags.map((t) => (
            <TagChip
              key={t.id}
              id={t.id}
              name={t.name}
              color={t.color}
              mode="editable"
              selected={selected.has(t.id)}
              onToggle={toggle}
            />
          ))}
        </div>
        <div className="modal-actions">
          <button className="btn-secondary" onClick={onCancel}>取消</button>
          <button
            className="btn-primary"
            onClick={() => onConfirm(Array.from(selected))}
            disabled={selected.size === 0}
          >
            确认 ({selected.size})
          </button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 7: 加 picker CSS**

```css
.tag-picker-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  padding: 16px 0;
  max-height: 400px;
  overflow-y: auto;
}
```

- [ ] **Step 8: Commit**

```bash
git add frontend/src/components/TagChip.tsx frontend/src/components/TagPicker.tsx frontend/src/styles/global.css frontend/src/__tests__/TagChip.test.tsx
git commit -m "feat(ui): v1.1 PR#4 - TagChip + TagPicker components"
```

---

## Task 4.6: 前端 KnowledgePage 集成标签区 + 打标 + 过滤

**Files:**
- Modify: `frontend/src/pages/KnowledgePage.tsx`
- Modify: `frontend/src/lib/api.ts`
- Test: 手动验证(无 RTL)

- [ ] **Step 1: lib/api.ts 加 tag CRUD 函数**

```typescript
// frontend/src/lib/api.ts 加:
export interface Tag {
  id: number;
  database_id: string;
  name: string;
  color: string;
}

export async function listTags(databaseId: string): Promise<Tag[]> {
  return (await api.get(`/api/knowledge/databases/${databaseId}/tags`)).data;
}

export async function createTag(
  databaseId: string,
  name: string,
  color = '#FF540E',
): Promise<Tag> {
  return (await api.post(`/api/knowledge/databases/${databaseId}/tags`, { name, color })).data;
}

export async function deleteTagApi(tagId: number): Promise<void> {
  await api.delete(`/api/knowledge/tags/${tagId}`);
}

export async function assignDocTags(source: string, tagIds: number[]): Promise<void> {
  await api.post('/api/knowledge/doc-tags', { source, tag_ids: tagIds });
}

export async function unassignDocTag(source: string, tagId: number): Promise<void> {
  await api.delete(`/api/knowledge/doc-tags/${encodeURIComponent(source)}/${tagId}`);
}

export async function filterDocumentsByTags(
  databaseId: string,
  tagIds: number[],
): Promise<Array<{ source: string; tag_count: number }>> {
  return (await api.get(
    `/api/knowledge/databases/${databaseId}/documents?tag_ids=${tagIds.join(',')}`,
  )).data;
}
```

- [ ] **Step 2: KnowledgePage 加载 tags + 渲染 sidebar**

```typescript
// KnowledgePage.tsx 顶部:
import { TagChip } from '../components/TagChip';
import { TagPicker } from '../components/TagPicker';
import * as api from '../lib/api';

// state:
const [tags, setTags] = useState<api.Tag[]>([]);
const [filterTagIds, setFilterTagIds] = useState<number[]>([]);
const [pickerOpen, setPickerOpen] = useState<{ source: string } | null>(null);

// effect:
useEffect(() => {
  api.listTags(currentDatabaseId).then(setTags);
}, [currentDatabaseId]);

// sidebar 标签区(在 db 列表下方):
<div className="sidebar-tags-section">
  <div className="sidebar-tags-header">
    <span>标签</span>
    <button onClick={() => {/* 打开新建标签输入框 */}}>+ 新建</button>
  </div>
  <div className="sidebar-tags-list">
    {tags.map((t) => (
      <TagChip
        key={t.id}
        id={t.id}
        name={t.name}
        color={t.color}
        mode="removable"
        onRemove={async (id) => {
          await api.deleteTagApi(id);
          setTags((ts) => ts.filter((x) => x.id !== id));
        }}
      />
    ))}
  </div>
</div>
```

- [ ] **Step 3: 文档列表行加打标入口**

```typescript
// 文档行 JSX 加:
<button
  className="doc-tag-btn"
  onClick={() => setPickerOpen({ source: doc.source })}
>
  🏷
</button>

{pickerOpen && (
  <TagPicker
    availableTags={tags}
    onCancel={() => setPickerOpen(null)}
    onConfirm={async (selectedIds) => {
      await api.assignDocTags(pickerOpen.source, selectedIds);
      setPickerOpen(null);
    }}
  />
)}
```

- [ ] **Step 4: 文档列表顶部加过滤条**

```typescript
<div className="doc-filter-bar">
  {filterTagIds.length > 0 && (
    <>
      {filterTagIds.map((id) => {
        const tag = tags.find((t) => t.id === id);
        if (!tag) return null;
        return (
          <TagChip
            key={id}
            id={id}
            name={tag.name}
            color={tag.color}
            mode="removable"
            onRemove={() => setFilterTagIds((s) => s.filter((x) => x !== id))}
          />
        );
      })}
      <button onClick={() => setFilterTagIds([])}>清除</button>
    </>
  )}
  <button onClick={() => setPickerOpen({ source: '__filter__' })}>+ 加过滤</button>
</div>
```

(过滤生效:文档列表 `useMemo` 按 `filterTagIds` 走 `filterDocumentsByTags`)

- [ ] **Step 5: 跑 vite build**

Run: `cd /e/frontend && npm run build`
Expected: build 成功

- [ ] **Step 6: Commit**

```bash
git add frontend/src/pages/KnowledgePage.tsx frontend/src/lib/api.ts
git commit -m "feat(ui): v1.1 PR#4 - KnowledgePage tag integration"
```

---

## Task 4.7: 跑全套回归 + 更新 CHANGELOG + 发版

- [ ] **Step 1: 跑 run-checks.ps1**

Run: `cd /e && pwsh -File scripts/run-checks.ps1`
Expected: ruff ✅ / pytest ✅ / eslint ✅ / vite build ✅

- [ ] **Step 2: 跑 pytest 全套(目标 200)**

Run: `cd /e && backend/.venv/Scripts/python -m pytest tests/unit/ -q`
Expected: 200+ passed

- [ ] **Step 3: 跑 RTL 全套**

Run: `cd /e/frontend && npx vitest run`
Expected: 8 passed(3 TagChip + 5 MessageBubble)

- [ ] **Step 4: 跑 RAG eval**

```bash
cd /e && curl -X POST http://localhost:8000/api/eval/run -H "Content-Type: application/json" -d '{}'
```
Expected: ≥ 80% 通过

- [ ] **Step 5: 更新 CHANGELOG**

在 v1.1.0 PR #3 段后追加:

```markdown
### PR #4 tag-and-title(自由标签 + 标题修复)

- **#1 自由标签**:新表 `tags`(db 作用域,跨 db 同名允许)+ `doc_tags`(多对多);7 个新路由:
  - `GET/POST /api/knowledge/databases/{db_id}/tags`
  - `PATCH/DELETE /api/knowledge/tags/{tag_id}`
  - `POST /api/knowledge/doc-tags`
  - `DELETE /api/knowledge/doc-tags/{source}/{tag_id}`
  - `GET /api/knowledge/databases/{db_id}/documents?tag_ids=1,2,3`
- 前端 `TagChip` / `TagPicker` 组件 + KnowledgePage 集成 sidebar / 打标 / 过滤
- `boot.py` 加 `schema_migration` 阶段(8%,建表 + ALTER)
- **#7 历史会话标题 LLM 生成**:加 debug 日志 + 修复触发条件(测试覆盖 idempotent / persist / debug log)

新增测试:
- `tests/unit/test_tags_api.py` 10 测(CRUD + 端到端 + CASCADE + 跨 db 隔离)
- `tests/unit/test_session_title.py` 3 测(触发 / 持久化 / idempotent)
- `frontend/src/__tests__/TagChip.test.tsx` 3 测(RTL)

单测总数: 183 → 199(+16)
FastAPI 路由: 13 → 20
```

- [ ] **Step 6: 更新 AGENTS.md**

- §1 文件地图加 `backend/api/tags.py` + 新表 `tags` / `doc_tags`
- §6 测试矩阵 +16
- §13 变更记录摘要

- [ ] **Step 7: 更新 known-limitations.md**

§2 全部 10 项标 ✅ 移入"已修复"段(本节新增):

```markdown
## 2. 已知功能短板(v1.1.0 全部修复)

✅ #1 自由标签 — v1.1.0 修复,见 CHANGELOG PR #4
✅ #2 图片缩略图 — v1.1.0 修复,见 CHANGELOG PR #3
✅ #3 单图 20MB — v1.1.0 修复,见 CHANGELOG PR #2
✅ #4 多图 5 张 — v1.1.0 修复,见 CHANGELOG PR #2
✅ #5 多选题 UI — v1.1.0 修复,见 CHANGELOG PR #3
✅ #6 跳过反问 — v1.1.0 修复,见 CHANGELOG PR #3
✅ #7 历史标题 — v1.1.0 修复,见 CHANGELOG PR #4
✅ #8 50 轮上限 — v1.1.0 修复,见 CHANGELOG PR #2
✅ #9 级联删除 — v1.1.0 修复,见 CHANGELOG PR #1
✅ #11 Qdrant payload 重写 — v1.1.0 修复,见 CHANGELOG PR #1

❌ #10 启动 UI 暂停/继续 — 明确 N/A(不在 MVP 范围)
```

- [ ] **Step 8: 更新 acceptance-checklist.md**

加 v1.1.0 验证项:

```markdown
## v1.1.0 验证项

- [ ] 标签 CRUD:POST /api/knowledge/databases/default/tags 创建"财务",GET 列表包含
- [ ] 文档打标:POST /api/knowledge/doc-tags 打 2 个 tag,GET documents?tag_ids 返回该 source
- [ ] CASCADE:DELETE tag 后 doc_tags 自动清理
- [ ] 跨 db 隔离:db_a 和 db_b 可同名 tag
- [ ] 图片缩略图:MessageBubble 拿到 image_paths 渲染 80×80 网格
- [ ] 多选题 UI:options 数组渲染按钮,点击触发 onUserReply
- [ ] 跳过反问:点击"按原问题直接回答"按钮 → 后端 skip_clarification=true → 不走反问
- [ ] 单图超限:21MB 图片 POST /upload → 413
- [ ] 多图超限:6 张图片 POST /upload → 413
- [ ] 50 轮软警告:session 装 40 条消息,新问时 SSE 含 soft_warning
- [ ] PATCH session:PATCH /api/sessions/{id} body={history_limit:100} 持久化
- [ ] 级联 409:db 有 5 文档 → DELETE 不带 cascade → 409
- [ ] 级联删除:DELETE ?cascade=true → Qdrant collection 清理
- [ ] Qdrant 不可达降级:DELETE ?cascade=true + Qdrant down → 200 + warning
- [ ] bulk_assign Qdrant 重写:POST /assign 后 Qdrant payload.source 更新
```

- [ ] **Step 9: Commit 发版**

```bash
git add CHANGELOG.md AGENTS.md docs/known-limitations.md docs/acceptance-checklist.md
git commit -m "release: v1.1.0 - 10 known limitations fixed (4 PRs)"
```

- [ ] **Step 10: tag 标记(可选)**

```bash
git tag v1.1.0
```

---

**Status**:🟢 v1.1.0 全功能发布就绪 · 4 PRs 完成 + 测试 199 通过 + 文档同步