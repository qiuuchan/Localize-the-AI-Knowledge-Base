# PR #1 · data-integrity · Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 关闭 known-limitations.md §2 #9(数据库删除级联 Qdrant collection)+ #11(`bulk_assign_documents_to_database` 不重写 Qdrant payload)。

**Architecture:**
- 改 `backend/api/databases.py`:`DELETE /databases/{id}` 默认 `cascade=false`;有子文档 → 409;`?cascade=true` 串行清理 SQLite metadata + Qdrant collection
- 改 `backend/core/sqlite.py`:`bulk_assign_documents_to_database()` 加 Qdrant payload 同步步骤(旧 source → 新 source)+ 失败降级到 degradation_events
- 改 `frontend/src/pages/KnowledgePage.tsx`:`window.confirm` 升级为二次模态,显示子文档数

**Tech Stack:** FastAPI 0.111 / SQLite (sqlite3 stdlib) / Qdrant REST API / React 18 + TypeScript / XAIAgent 暗黑赛博风

## Global Constraints

- **版本**:v1.0.1 → v1.1.0;`version` 文件由最后任务统一更新
- **不修改**:`.env` / `package.bat` / `start.bat` / `stop.bat` / `architecture-validation-report.md` Part 1 / 容器镜像 tag
- **依赖**:不引入新第三方包;复用现有 fastapi / pydantic / sqlite3 / httpx
- **风格**:Python PEP-8 + ruff;TypeScript ESLint + 现有 XAIAgent 样式令牌
- **单测**:每个任务自带 pytest 测试;测试文件 `tests/unit/test_database_cascade.py`
- **降级**:Qdrant 不可达 → degradation_events(component='Vector') + 返回 200 + warning 字段(不阻断主流程)

## File Structure

| 文件 | 责任 |
|---|---|
| `backend/api/databases.py` | DELETE 端点:默认 cascade=false + 409 + cascade 全链路 |
| `backend/core/sqlite.py` | `bulk_assign_documents_to_database` 重写 + Qdrant 同步 |
| `backend/core/bulk_assign.py`(**新**) | Qdrant payload 批量重写(独立函数,便于测试 mock) |
| `tests/unit/test_database_cascade.py`(**新**) | 8 个测:有子文档 409 / cascade 全链路 / Qdrant 不可达降级 / 跨 db 标签清理(本 PR 不含 tag,这条移到 PR #4) |
| `tests/unit/test_bulk_assign_qdrant.py`(**新**) | 5 个测:成功重写 / 部分失败降级 / 0 chunk 不报错 / payload 内容正确 / keyword_index 同步 |
| `frontend/src/pages/KnowledgePage.tsx` | sidebar 删除按钮:模态二次确认 |
| `frontend/src/components/Modals.tsx` | 新增 `ConfirmCascadeDeleteModal` 组件 |

---

## Task 1.1: 后端 `bulk_assign_documents_to_database` Qdrant payload 同步

**Files:**
- Create: `backend/core/bulk_assign.py`
- Modify: `backend/core/sqlite.py:bulk_assign_documents_to_database`
- Test: `tests/unit/test_bulk_assign_qdrant.py`

**Interfaces:**
- Consumes: 现有 `bulk_assign_documents_to_database(db_id, sources) -> int`
- Produces: `_rewrite_qdrant_payloads(old_source, new_source, *, qdrant_url=None) -> dict` in `backend.core.bulk_assign`

- [ ] **Step 1: 写失败测试 — Qdrant payload 改写存在**

```python
# tests/unit/test_bulk_assign_qdrant.py
from backend.core.bulk_assign import _rewrite_qdrant_payloads


def test_rewrite_payloads_calls_set_payload(monkeypatch):
    """成功场景:Qdrant 应被调用 set_payload 批量改 source。"""
    calls = []

    def fake_set_payload(base_url, collection, payload):
        calls.append({"collection": collection, "payload": payload})

    monkeypatch.setattr(
        "backend.core.bulk_assign._qdrant_set_payload_batch",
        fake_set_payload,
    )
    monkeypatch.setattr(
        "backend.core.bulk_assign._scroll_points_by_source",
        lambda *a, **kw: [
            {"id": "p1", "payload": {"source": "old_db::foo.pdf"}},
            {"id": "p2", "payload": {"source": "old_db::foo.pdf"}},
        ],
    )

    result = _rewrite_qdrant_payloads(
        old_source="old_db::foo.pdf",
        new_source="new_db::foo.pdf",
        collection="kb_chunks_old_db",
    )

    assert result["rewritten"] == 2
    assert len(calls) == 1
    assert calls[0]["payload"]["source"] == "new_db::foo.pdf"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /e && backend/.venv/Scripts/python -m pytest tests/unit/test_bulk_assign_qdrant.py::test_rewrite_payloads_calls_set_payload -v`
Expected: `ModuleNotFoundError: No module named 'backend.core.bulk_assign'` or `ImportError`

- [ ] **Step 3: 实现 `backend/core/bulk_assign.py`**

```python
"""Qdrant payload 批量重写 — 用于跨 database 文档迁移。

设计原则:
- 与 keyword_index 同步,但失败降级(不阻断主流程)
- 走 Qdrant REST scroll + set_payload 批量 API
- 上限 100 条/批,避免单批过大超时
- 通过 degradation_events(component='Vector') 记录失败

仅供 backend/core/sqlite.py:bulk_assign_documents_to_database 调用,
不在 API 路由层暴露。
"""
from __future__ import annotations

import logging
from typing import Any, Dict, List, Optional

from backend.core.rag import qdrant_store as rag_qdrant

logger = logging.getLogger(__name__)

BATCH_SIZE = 100


def _scroll_points_by_source(
    collection: str, source: str, *, url: Optional[str] = None
) -> List[Dict[str, Any]]:
    """拉取 collection 内 payload.source = source 的所有 points。"""
    body = {
        "filter": {"must": [{"key": "source", "match": {"value": source}}]},
        "limit": BATCH_SIZE,
        "with_payload": True,
        "with_vector": False,
    }
    resp = rag_qdrant._request(  # noqa: SLF001 — 内部 helper,测试可 mock
        "POST",
        f"{rag_qdrant._base_url(url)}/collections/{collection}/points/scroll",  # noqa: SLF001
        data=body,
        timeout=30,
    )
    return resp.get("result", {}).get("points", [])


def _qdrant_set_payload_batch(
    collection: str, points: List[Dict[str, Any]], *, url: Optional[str] = None
) -> None:
    """批量更新 points 的 payload。"""
    if not points:
        return
    body = {"points": points}
    rag_qdrant._request(  # noqa: SLF001
        "PUT",
        f"{rag_qdrant._base_url(url)}/collections/{collection}/points/payload",  # noqa: SLF001
        data=body,
        timeout=60,
    )


def _rewrite_qdrant_payloads(
    old_source: str,
    new_source: str,
    *,
    collection: str,
    url: Optional[str] = None,
) -> Dict[str, Any]:
    """把 collection 内所有 payload.source = old_source 的 points 改为 new_source。

    Returns:
        {"rewritten": int, "warnings": List[str]}

    Raises:
        任何 Qdrant 异常都会包成 RuntimeError,调用方决定是否降级。
    """
    points = _scroll_points_by_source(collection, old_source, url=url)
    if not points:
        return {"rewritten": 0, "warnings": []}

    updates = [
        {"id": p["id"], "payload": {**p.get("payload", {}), "source": new_source}}
        for p in points
    ]

    written = 0
    warnings: List[str] = []
    for batch_start in range(0, len(updates), BATCH_SIZE):
        batch = updates[batch_start : batch_start + BATCH_SIZE]
        try:
            _qdrant_set_payload_batch(collection, batch, url=url)
            written += len(batch)
        except Exception as exc:  # noqa: BLE001
            warnings.append(f"batch {batch_start}-{batch_start + len(batch)} failed: {exc}")
            logger.warning("Qdrant payload rewrite partial failure: %s", exc)

    return {"rewritten": written, "warnings": warnings}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /e && backend/.venv/Scripts/python -m pytest tests/unit/test_bulk_assign_qdrant.py::test_rewrite_payloads_calls_set_payload -v`
Expected: PASS

- [ ] **Step 5: 改 `backend/core/sqlite.py` 调用新模块**

定位 `bulk_assign_documents_to_database` 函数(v0.8.11 实现),在 keyword_index 更新后追加:

```python
from backend.core.bulk_assign import _rewrite_qdrant_payloads
from backend.core.rag import qdrant_store as rag_qdrant

# 函数尾部,return affected 之前:
db_meta = get_database(db_id)
if db_meta and db_meta.get("collection"):
    # 旧 source prefix 是旧 db_id;新 source prefix 是 db_id
    old_db_id = ...  # 需从传入 sources 推断旧 db_id(见下面说明)
    for source in sources:
        old_source = source  # 已是旧 db_id 前缀
        new_source = _source_reprefix(source, db_id)  # 仅改前缀
        try:
            result = _rewrite_qdrant_payloads(
                old_source=old_source,
                new_source=new_source,
                collection=db_meta["collection"],
            )
            if result["warnings"]:
                save_degradation_event(
                    session_id=None,
                    query=None,
                    source=source,
                    reason=f"Qdrant payload rewrite partial: {result['warnings']}",
                    model=None,
                    component="Vector",
                )
        except Exception as exc:  # noqa: BLE001
            save_degradation_event(
                session_id=None,
                query=None,
                source=source,
                reason=f"Qdrant payload rewrite failed: {exc}",
                model=None,
                component="Vector",
            )
```

**注意**:旧 db_id 推断。看现有 `bulk_assign_documents_to_database` 是按 sources 列表批量迁移;需要从每个 source 的 `<db>::<filename>` 前缀推断旧 db_id。**实现方案**:对每个 source 单独调用 `_rewrite_qdrant_payloads`,新 source = `<new_db>::<filename>`。已在上面代码里体现。

- [ ] **Step 6: 写失败测试 — bulk_assign 调用 Qdrant rewrite**

```python
# tests/unit/test_bulk_assign_qdrant.py 加:
from backend.core.sqlite import bulk_assign_documents_to_database


def test_bulk_assign_calls_qdrant_rewrite(monkeypatch, tmp_path):
    """bulk_assign_documents_to_database 应触发 Qdrant payload rewrite。"""
    # 走完整 sqlite + 临时 db
    from backend.core import sqlite as bs
    bs.init_db(tmp_path / "test.db")
    bs.create_database("default", "默认")
    bs.create_database("new_db", "新库")
    bs.ensure_default_database()

    monkeypatch.setattr(
        "backend.core.sqlite._rewrite_qdrant_payloads",
        lambda **kw: {"rewritten": 5, "warnings": []},
    )

    # 先在 keyword_index 插一些"default::foo.pdf" 假数据
    conn = bs.get_connection(tmp_path / "test.db")
    conn.executemany(
        "INSERT INTO keyword_index (word, point_id, source, text) VALUES (?, ?, ?, ?)",
        [("hello", "p1", "default::foo.pdf", "..."),
         ("world", "p2", "default::foo.pdf", "...")],
    )
    conn.commit()
    conn.close()

    affected = bulk_assign_documents_to_database(
        db_id="new_db",
        sources=["default::foo.pdf"],
    )
    assert affected >= 1
```

- [ ] **Step 7: 跑测试确认通过**

Run: `cd /e && backend/.venv/Scripts/python -m pytest tests/unit/test_bulk_assign_qdrant.py -v`
Expected: 2 passed

- [ ] **Step 8: Commit**

```bash
git add backend/core/bulk_assign.py backend/core/sqlite.py tests/unit/test_bulk_assign_qdrant.py
git commit -m "fix(data-integrity): v1.1 PR#1 - bulk_assign rewrites Qdrant payload"
```

---

## Task 1.2: 后端 DELETE database 加 409 + cascade 全链路

**Files:**
- Modify: `backend/api/databases.py:118-144`
- Test: `tests/unit/test_database_cascade.py`

- [ ] **Step 1: 写失败测试 — 有子文档返回 409**

```python
# tests/unit/test_database_cascade.py
from fastapi.testclient import TestClient

from backend.main import app


def test_delete_database_with_children_returns_409(monkeypatch):
    """db 仍有子文档 → 默认 cascade=false 时返回 409,不删。"""
    client = TestClient(app)
    monkeypatch.setattr(
        "backend.core.sqlite.count_documents_by_database",
        lambda db_id: 5,
    )
    resp = client.delete("/api/knowledge/databases/my_db")
    assert resp.status_code == 409
    body = resp.json()
    assert body["child_documents"] == 5
    assert body["requires_cascade"] is True
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /e && backend/.venv/Scripts/python -m pytest tests/unit/test_database_cascade.py::test_delete_database_with_children_returns_409 -v`
Expected: 实际返回 200 或 422(因为还没加 409 逻辑)

- [ ] **Step 3: 改 `backend/api/databases.py:118-144`**

```python
@router.delete("/knowledge/databases/{db_id}")
def delete_database_endpoint(
    db_id: str,
    cascade: bool = Query(default=False, description="true 时同时删 Qdrant collection + keyword 行 + tag 关联"),
) -> Dict[str, Any]:
    if db_id == DEFAULT_DATABASE_ID:
        raise HTTPException(
            status_code=400,
            detail="'default' 数据库不可删除(承载存量数据,改用 assign 迁移)",
        )
    db = get_database(db_id)
    if not db:
        raise HTTPException(status_code=404, detail=f"database '{db_id}' 不存在")

    # v1.1.0 PR#1:FMEA 二级保护 — 有子文档时拒绝删,要求显式 cascade=true
    child_count = _count_docs_impl(db_id)
    if child_count > 0 and not cascade:
        raise HTTPException(
            status_code=409,
            detail={
                "message": f"database '{db_id}' 仍有 {child_count} 个文档,需 cascade=true 显式确认",
                "child_documents": child_count,
                "requires_cascade": True,
            },
        )

    # 先删 metadata,再按需 cascade
    deleted = _delete_db_impl(db_id, cascade=cascade)
    warnings: List[str] = []
    if cascade:
        try:
            rag_qdrant.delete_collection(db["collection"])
        except Exception as exc:  # noqa: BLE001
            logger.exception("Qdrant collection 清理失败 db_id=%s collection=%s", db_id, db["collection"])
            # 不阻断 200;降级路径写 degradation_events
            from backend.core.sqlite import save_degradation_event
            save_degradation_event(
                session_id=None,
                query=None,
                source=db_id,
                reason=f"cascade delete: Qdrant cleanup failed: {exc}",
                model=None,
                component="Vector",
            )
            warnings.append(f"Qdrant collection 清理失败: {exc}")

    return {
        "deleted": deleted,
        "database_id": db_id,
        "cascade": cascade,
        "warnings": warnings,
    }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /e && backend/.venv/Scripts/python -m pytest tests/unit/test_database_cascade.py::test_delete_database_with_children_returns_409 -v`
Expected: PASS

- [ ] **Step 5: 写失败测试 — cascade=false 但无子文档 → 200**

```python
# tests/unit/test_database_cascade.py 加:
def test_delete_database_no_children_succeeds(monkeypatch):
    """db 无子文档 → cascade=false 也能直接删(向后兼容)。"""
    client = TestClient(app)
    monkeypatch.setattr(
        "backend.core.sqlite.count_documents_by_database",
        lambda db_id: 0,
    )
    monkeypatch.setattr(
        "backend.core.sqlite.delete_database",
        lambda db_id, cascade: True,
    )
    resp = client.delete("/api/knowledge/databases/empty_db")
    assert resp.status_code == 200
```

- [ ] **Step 6: 跑测试**

Run: `cd /e && backend/.venv/Scripts/python -m pytest tests/unit/test_database_cascade.py -v`
Expected: 2 passed

- [ ] **Step 7: 写失败测试 — cascade=true Qdrant 不可达降级**

```python
# tests/unit/test_database_cascade.py 加:
def test_delete_database_cascade_qdrant_down_returns_warning(monkeypatch):
    """cascade=true 但 Qdrant 不可达 → 返回 200 + warning + degradation_events。"""
    client = TestClient(app)
    monkeypatch.setattr(
        "backend.core.sqlite.count_documents_by_database",
        lambda db_id: 0,
    )
    monkeypatch.setattr(
        "backend.core.sqlite.delete_database",
        lambda db_id, cascade: True,
    )
    monkeypatch.setattr(
        "backend.core.rag.qdrant_store.delete_collection",
        lambda *a, **kw: (_ for _ in ()).throw(RuntimeError("Qdrant offline")),
    )
    captured = []
    monkeypatch.setattr(
        "backend.core.sqlite.save_degradation_event",
        lambda **kw: captured.append(kw),
    )
    resp = client.delete("/api/knowledge/databases/some_db?cascade=true")
    assert resp.status_code == 200
    body = resp.json()
    assert body["warnings"]
    assert any(c.get("component") == "Vector" for c in captured)
```

- [ ] **Step 8: 跑测试**

Run: `cd /e && backend/.venv/Scripts/python -m pytest tests/unit/test_database_cascade.py -v`
Expected: 3 passed

- [ ] **Step 9: 跑全套 ruff + pytest**

Run: `cd /e && backend/.venv/Scripts/python -m ruff check backend/ tests/`
Run: `cd /e && backend/.venv/Scripts/python -m pytest tests/unit/ -q`
Expected: ruff 全绿;pytest 全过(原 167 + 2 新 = 169 测)

- [ ] **Step 10: Commit**

```bash
git add backend/api/databases.py tests/unit/test_database_cascade.py
git commit -m "fix(data-integrity): v1.1 PR#1 - cascade delete with 409 guard + Qdrant fallback"
```

---

## Task 1.3: 前端 sidebar 删除 db 二次模态

**Files:**
- Modify: `frontend/src/pages/KnowledgePage.tsx`
- Modify: `frontend/src/components/Modals.tsx`

- [ ] **Step 1: 读现有 KnowledgePage 删除入口**

Run: `grep -n "delete\|database" frontend/src/pages/KnowledgePage.tsx | head -30`

定位现有删除 db 的按钮 / onClick handler。

- [ ] **Step 2: 在 Modals.tsx 加新组件**

```typescript
// frontend/src/components/Modals.tsx 末尾追加:

interface ConfirmCascadeDeleteModalProps {
  open: boolean;
  databaseName: string;
  childDocuments: number;
  onCancel: () => void;
  onConfirm: () => void;
}

export function ConfirmCascadeDeleteModal({
  open,
  databaseName,
  childDocuments,
  onCancel,
  onConfirm,
}: ConfirmCascadeDeleteModalProps) {
  if (!open) return null;
  return (
    <div className="modal-backdrop" role="dialog" aria-modal="true">
      <div className="modal danger">
        <h3>确认级联删除?</h3>
        <p>
          数据库 <strong>{databaseName}</strong> 仍有 <strong>{childDocuments}</strong> 个文档。
        </p>
        <p className="warning">
          删除将同时清除该数据库的 Qdrant collection、keyword 索引、tag 关联、processing_state 记录。
          此操作不可恢复。
        </p>
        <div className="modal-actions">
          <button className="btn-secondary" onClick={onCancel}>取消</button>
          <button className="btn-danger" onClick={onConfirm}>确认级联删除</button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: 在 KnowledgePage.tsx 替换 `window.confirm`**

找到现有 `window.confirm("确认删除 db?")` 的位置(假设在 sidebar 里),替换为:

```typescript
import { ConfirmCascadeDeleteModal } from '../components/Modals';

// state:
const [cascadeTarget, setCascadeTarget] = useState<{
  dbId: string;
  dbName: string;
  childDocuments: number;
} | null>(null);

// onClick handler:
const handleDeleteClick = async (dbId: string, dbName: string) => {
  try {
    const resp = await api.deleteDatabase(dbId);
    // 200:直接删了
    refreshDatabases();
  } catch (err: any) {
    if (err.status === 409 && err.body?.requires_cascade) {
      setCascadeTarget({
        dbId,
        dbName,
        childDocuments: err.body.child_documents,
      });
    } else {
      throw err;
    }
  }
};

const handleCascadeConfirm = async () => {
  if (!cascadeTarget) return;
  try {
    await api.deleteDatabase(cascadeTarget.dbId, { cascade: true });
    setCascadeTarget(null);
    refreshDatabases();
  } catch (err) {
    // 错误展示由全局 error toast 处理
    setCascadeTarget(null);
  }
};

// JSX 末尾:
{cascadeTarget && (
  <ConfirmCascadeDeleteModal
    open={true}
    databaseName={cascadeTarget.dbName}
    childDocuments={cascadeTarget.childDocuments}
    onCancel={() => setCascadeTarget(null)}
    onConfirm={handleCascadeConfirm}
  />
)}
```

- [ ] **Step 4: 编译验证**

Run: `cd /e/frontend && npm run build`
Expected: build 成功,无 TS 报错

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/Modals.tsx frontend/src/pages/KnowledgePage.tsx
git commit -m "feat(ui): v1.1 PR#1 - cascade delete confirmation modal"
```

---

## Task 1.4: 跑回归 + RAG eval + 文档同步

**Files:**
- Modify: `version`
- Modify: `.kb-ai-root/version`
- Modify: `CHANGELOG.md`(prepend v1.1.0 段;不动 v1.0.1)

- [ ] **Step 1: 跑完整 ruff + pytest + eslint + vite build**

Run: `cd /e && pwsh -File scripts/run-checks.ps1`
Expected: ruff ✅ / pytest ✅ / eslint ✅ / vite build ✅

- [ ] **Step 2: 跑 RAG eval(若服务在线)**

```bash
cd /e && curl -X POST http://localhost:8000/api/eval/run -H "Content-Type: application/json" -d '{}'
```
Expected: `{"total": 20, "passed": 20, ...}`(目标 ≥ 80%)

- [ ] **Step 3: 跑 API 集成测试**

Run: `cd /e && backend/.venv/Scripts/python -m pytest tests/integration/api/test_api.py -v`
Expected: 6 passed

- [ ] **Step 4: 更新 version 文件**

Run: `cd /e && echo "1.1.0" > version && echo "1.1.0" > .kb-ai-root/version`

- [ ] **Step 5: 更新 CHANGELOG**

在 `CHANGELOG.md` 顶部(`## [1.0.1]` 之上)插入:

```markdown
## [1.1.0] - 2026-07-21

### 修复(v1.0 GA 已知 10 项功能短板收口 / PR #1 data-integrity)

#### 已知短板关闭(§2 #9 + #11)
- **#9 数据库删除级联保护**:默认 `cascade=false`;有子文档 → `409 {child_documents: N, requires_cascade: true}` 拒绝删;前端二次模态确认后才走 `?cascade=true` 串行清理 Qdrant collection + keyword 索引 + tag 关联 + processing_state。Qdrant 不可达时写 degradation_events(component='Vector') + 返回 200 + warning,不阻断主流程
- **#11 bulk_assign_documents_to_database 同步重写 Qdrant payload**:新增 `backend/core/bulk_assign.py:_rewrite_qdrant_payloads`,旧 source → 新 source 走 scroll + set_payload 批量(≤ 100/批);失败降级到 degradation_events(component='Vector'),与 keyword_index 同步不阻断主流程

#### 新增测试
- `tests/unit/test_database_cascade.py` 3 测:有子文档 409 / 无子文档 200 / Qdrant 不可达降级
- `tests/unit/test_bulk_assign_qdrant.py` 2 测:Qdrant set_payload 触发 / bulk_assign 集成调用

#### 单测总数
167 → 172(+5)

#### 不修改(系统策略)
- ❌ `.env` / `package.bat` / `start.bat` / `stop.bat` / 容器镜像 tag / `architecture-validation-report.md` Part 1
```

- [ ] **Step 6: Commit PR #1 收尾**

```bash
git add version .kb-ai-root/version CHANGELOG.md
git commit -m "chore(release): v1.1.0 PR#1 - data-integrity fixup"
```

---

**Status**:🟡 待用户 review · PR #1 完成后,执行 PR #2(limit-guard)/ PR #3(ui-feedback)/ PR #4(tag-and-title)各自独立 plan