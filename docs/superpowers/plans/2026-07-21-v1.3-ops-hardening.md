# KB-AI v1.3.0 · 运维加固与文档同步 · 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 known-limitations §4 / §5 共 7 项运维/文档缺口(已合并为 6 项)一次性收口;增加月度配额告警 + pre-commit/pre-push 双 hook;同步 PRD v0.7 / AGENTS.md / CHANGELOG / known-limitations / acceptance-checklist / degradation-guide / QUICKSTART / README / version 到 v1.3.0。

**Architecture:**
- **PR #1 (Task 1)**:先装门禁(git hooks),保证后续 PR 自带 ruff + run-checks 验证。
- **PR #2 (Task 2)**:cost-alert 后端核心 — `llm.py` 加 `_log_token_usage` hook(streaming 加 `stream_options={"include_usage": True}`,non-streaming 取 `response["usage"]`),`chat.py` 加 level≥3 阻断分支(retrieve 之前 early-return),`dashboard.py` 加 `_read_cost_alert()` + 在 `_build_overview()` 追加字段;新建 `cost-alert.ps1` PS 5.1 脚本(累加 + 等级判定 + 写 health_status.json);6 个 pytest 单测覆盖。
- **PR #3 (Task 3)**:cost-alert 前端 — `types.ts` 加 `CostAlert` 接口,`DashboardPage.tsx` 加 cost-alert 卡片组件,`DashboardPage.test.tsx` 加 2 测(level=0 / level≥2 渲染)。
- **PR #4 (Task 4)**:文档收口 release PR — AGENTS.md §13 同步 + PRD v0.7 全面同步(项目外,需用户授权)+ QUICKSTART.md §9 系统边界 + dify/README.md + README.md 版本行 + known-limitations §4/§5 关闭 + acceptance-checklist.md 更新 + degradation-guide.md cost-alert 场景 + CHANGELOG.md v1.3.0 段 + `version` 1.2.0 → 1.3.0 + `.gitignore` 加 `data/cost_log.jsonl`。

**Tech Stack:** Python 3.12 / FastAPI 0.111 / PowerShell 5.1 / bash (Git for Windows) / React 18 + TypeScript + Vite / ruff 0.x / pytest / vitest / 阿里云百炼 OpenAI 兼容协议(usage 在 `response["usage"]` 或 stream 最后 chunk)

## Global Constraints

从 spec §1.3 / §3.4 提炼(每条都不允许违反):

- **不修改**:`.env` / `package.bat` / `start.bat` / `stop.bat` / `docker-compose.yml` 容器镜像 tag / `architecture-validation-report.md` Part 1 / `.gitignore` 已有规则(只追加不删除)
- **不引入新依赖**(零新增 pip / npm 包)
- **业务代码白名单**:`backend/core/rag/llm.py`(加 `_log_token_usage` hook)、`backend/api/chat.py`(加阻断分支)、`backend/api/dashboard.py`(加 cost_alert 字段)
- **PS 5.1 兼容**:所有新建 .ps1 脚本不依赖 PS 6+ 特性(无 `??`、`??=` 等 null-coalescing)
- **run-checks.ps1 不破坏**:ruff / pytest / eslint / vite build 4/4 全绿
- **Git 提交**:每 PR 多次提交,每次 `git commit` / `git push` 前向用户单独授权(AGENTS.md §7.2)
- **月度切月**:UTC 自然月(每月 1 日 00:00:00 UTC 起算)
- **pre-commit hook 范围**:`ruff check backend/ tests/`(不扫 `scripts/` 顶层 .ps1 与 `scripts/hooks/` bash)
- **streaming usage 提取**:`body` 加 `stream_options={"include_usage": True}`;循环结束后从最后一段 delta 中提取;失败跳过不抛
- **阻断时机**:chat.py 入口在 retrieve 之前 early-return(省一次 query)
- **价格表**:`qwen3.6-plus` input 4 / output 12 ¥/MTok、`qwen3.7-max` input 12 / output 36、`text-embedding-v3` input 0.7(2026-07 阿里云百炼公示;实施时核对官方定价)

---

## Task 1: hooks 基础设施(PR #1)

**Files:**
- Create: `E:/scripts/hooks/pre-commit`
- Create: `E:/scripts/hooks/pre-push`
- Create: `E:/scripts/hooks/README.md`
- Create: `E:/scripts/install-hooks.ps1`

**Interfaces:**
- Consumes: 既有 `backend/` 与 `tests/`(已存在);`scripts/run-checks.ps1`(已存在)
- Produces:
  - `scripts/hooks/pre-commit`:bash 脚本,跑 `ruff check backend/ tests/`,exit code 决定 commit 是否被拦
  - `scripts/hooks/pre-push`:bash 脚本,跑 `pwsh -File scripts/run-checks.ps1`,exit code 决定 push 是否被拦
  - `scripts/install-hooks.ps1`:PS 5.1 脚本,设 `git config core.hooksPath scripts/hooks`

- [ ] **Step 1: 创建 scripts/hooks/pre-commit**

```bash
#!/usr/bin/env bash
# KB-AI · pre-commit hook (v1.3.0)
# 跑 ruff check backend/ tests/;失败则 abort commit
# 注意:不在 scripts/hooks/pre-commit 调用 pytest/eslint/vite(留给 pre-push)

set -e

echo "[pre-commit] ruff check backend/ tests/ ..."
cd "$(git rev-parse --show-toplevel)"

if ! command -v ruff >/dev/null 2>&1; then
  echo "[pre-commit] ruff not found in PATH; falling back to backend/.venv/Scripts/ruff.exe"
  if [ -x "backend/.venv/Scripts/ruff.exe" ]; then
    RUFF="backend/.venv/Scripts/ruff.exe"
  else
    echo "[pre-commit] [WARN] ruff not available; skipping lint"
    exit 0
  fi
else
  RUFF="ruff"
fi

if "$RUFF" check backend/ tests/; then
  echo "[pre-commit] ✓ ruff pass"
  exit 0
else
  echo "[pre-commit] ✗ ruff failed; fix before commit"
  exit 1
fi
```

- [ ] **Step 2: 创建 scripts/hooks/pre-push**

```bash
#!/usr/bin/env bash
# KB-AI · pre-push hook (v1.3.0)
# 跑 scripts/run-checks.ps1 (ruff + pytest + eslint + vite build,~30s);
# 失败则 abort push。
# 兼容 Git for Windows 默认 bash。pwsh 不在 PATH 时降级为警告 + exit 0。

set -e

echo "[pre-push] running scripts/run-checks.ps1 ..."
cd "$(git rev-parse --show-toplevel)"

if ! command -v pwsh >/dev/null 2>&1; then
  echo "[pre-push] [WARN] pwsh (PowerShell 7+) not found in PATH."
  echo "[pre-push] Skipping full checks; install pwsh or run run-checks.ps1 manually."
  echo "[pre-push] Push will proceed WITHOUT verification."
  exit 0
fi

if pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/run-checks.ps1; then
  echo "[pre-push] ✓ run-checks.ps1 pass"
  exit 0
else
  echo "[pre-push] ✗ run-checks.ps1 failed; fix before push"
  exit 1
fi
```

- [ ] **Step 3: 创建 scripts/hooks/README.md**

```markdown
# KB-AI · Git Hooks (v1.3.0)

## 概览

| Hook | 触发时机 | 跑什么 | 阻塞? |
|---|---|---|---|
| `pre-commit` | `git commit` | `ruff check backend/ tests/` | ✗ 失败 abort |
| `pre-push` | `git push` | `scripts/run-checks.ps1`(ruff + pytest + eslint + vite build) | ✗ 失败 abort |

## 安装

```powershell
pwsh -File scripts/install-hooks.ps1
```

安装器会执行 `git config core.hooksPath scripts/hooks`,把仓库 hooks 路径指向本目录。
新克隆后只需执行一次。

## 卸载

```powershell
pwsh -File scripts/install-hooks.ps1 -Uninstall
```

## 降级

`pwsh`(PowerShell 7+)不在 PATH 时,`pre-push` 输出警告 + exit 0(可推送但未跑全检)。
这种情况下手动 `powershell -File scripts/run-checks.ps1` 验证。

## 不覆盖什么

- 不扫 `scripts/` 顶层 .ps1(ruff 不支持 PS)
- 不扫 `scripts/hooks/*.bash`(bash,不是 ruff 目标)
- 不跑 pytest / eslint / vite 在 pre-commit(慢,留给 pre-push)
```

- [ ] **Step 4: 创建 scripts/install-hooks.ps1**

```powershell
<#
.SYNOPSIS
  KB-AI · 安装/卸载 git hooks (v1.3.0)

.DESCRIPTION
  默认: git config core.hooksPath scripts/hooks(项目级 hooks 路径)
  -Uninstall: git config --unset core.hooksPath
  -DryRun: 只 echo,不真修改 git config

.EXAMPLE
  pwsh -File scripts/install-hooks.ps1
  pwsh -File scripts/install-hooks.ps1 -DryRun
  pwsh -File scripts/install-hooks.ps1 -Uninstall

.NOTES
  PowerShell 5.1 兼容。
  pwsh (PowerShell 7+) 不在 PATH 时输出警告但仍配置(pre-push 会降级)。
#>

[CmdletBinding()]
param(
    [switch]$Uninstall = $false,
    [switch]$DryRun = $false
)

$ErrorActionPreference = "Stop"

$scriptRoot = if ($PSScriptRoot) { $PSScriptRoot } else { Split-Path -Parent $MyInvocation.MyCommand.Path }
$root = Split-Path -Parent $scriptRoot
$hooksDir = Join-Path $root 'scripts\hooks'
$hooksRelPath = 'scripts/hooks'

# pwsh 探测(给 pre-push 兜底)
$pwshAvailable = $null -ne (Get-Command pwsh -ErrorAction SilentlyContinue)
if (-not $pwshAvailable) {
    Write-Host "[WARN] pwsh (PowerShell 7+) not found in PATH." -ForegroundColor Yellow
    Write-Host "       pre-push hook 会降级为 noop;install PowerShell 7+ 以启用全检。" -ForegroundColor Yellow
}

if ($Uninstall) {
    if ($DryRun) {
        Write-Host "[DRY-RUN] would unset core.hooksPath" -ForegroundColor Cyan
        exit 0
    }
    Push-Location $root
    try {
        git config --unset core.hooksPath 2>$null
        if ($LASTEXITCODE -eq 0) {
            Write-Host "✓ hooks 已卸载 (core.hooksPath unset)" -ForegroundColor Green
        } else {
            Write-Host "[INFO] core.hooksPath was not set; nothing to remove" -ForegroundColor Yellow
        }
    } finally {
        Pop-Location
    }
    exit 0
}

# 安装
if (-not (Test-Path $hooksDir)) {
    Write-Host "[ERROR] hooks directory not found: $hooksDir" -ForegroundColor Red
    exit 1
}

if ($DryRun) {
    Write-Host "[DRY-RUN] would run: git config core.hooksPath $hooksRelPath" -ForegroundColor Cyan
    Write-Host "[DRY-RUN] pwsh available: $pwshAvailable" -ForegroundColor Cyan
    exit 0
}

Push-Location $root
try {
    git config core.hooksPath $hooksRelPath
    if ($LASTEXITCODE -ne 0) {
        throw "git config failed"
    }
    Write-Host "✓ hooks 已配置到 $hooksRelPath" -ForegroundColor Green
    Write-Host "  下次 git commit 会自动跑 ruff check backend/ tests/" -ForegroundColor Green
    Write-Host "  下次 git push 会自动跑 scripts/run-checks.ps1" -ForegroundColor Green
    if (-not $pwshAvailable) {
        Write-Host ""
        Write-Host "[WARN] pwsh 不可用;pre-push 会降级。" -ForegroundColor Yellow
    }
} finally {
    Pop-Location
}
```

- [ ] **Step 5: 让 hook 文件可执行(Linux/Mac/WSL 用户)**

```bash
cd E:/
chmod +x scripts/hooks/pre-commit scripts/hooks/pre-push
```

(Windows Git for Windows 不需要 chmod,但加无害)

- [ ] **Step 6: 验证 -DryRun 输出**

```bash
cd E:/
git config --get core.hooksPath  # 预期:空
pwsh -File scripts/install-hooks.ps1 -DryRun
```

预期:`[DRY-RUN] would run: git config core.hooksPath scripts/hooks`

- [ ] **Step 7: 验证手测 commit 被拦**

```bash
cd E:/
# 故意制造 ruff 错(临时文件,验证后删除)
echo 'import sys' > backend/core/rag/_test_ruff_err.py
echo 'def x(: pass' >> backend/core/rag/_test_ruff_err.py
git add backend/core/rag/_test_ruff_err.py
git commit -m "test: should be blocked by pre-commit"
# 预期: exit code != 0;输出 "[pre-commit] ✗ ruff failed"

# 清理
git reset HEAD backend/core/rag/_test_ruff_err.py
rm backend/core/rag/_test_ruff_err.py
```

- [ ] **Step 8: 真注册 hook**

```bash
cd E:/
pwsh -File scripts/install-hooks.ps1
git config --get core.hooksPath
```

预期:输出 `scripts/hooks`

- [ ] **Step 9: Commit**

```bash
cd E:/
git add scripts/hooks/pre-commit scripts/hooks/pre-push scripts/hooks/README.md scripts/install-hooks.ps1
git commit -m "feat(hooks): pre-commit (ruff) + pre-push (run-checks) via core.hooksPath

- scripts/hooks/pre-commit (bash): ruff check backend/ tests/
- scripts/hooks/pre-push (bash): pwsh -File scripts/run-checks.ps1
- scripts/hooks/README.md: hook 行为说明
- scripts/install-hooks.ps1: 安装器(-Uninstall/-DryRun),pwsh 探测降级

Refs: docs/superpowers/specs/2026-07-21-v1.3-ops-hardening-design.md §2.6"
git push origin master  # 需用户单独授权
```

---

## Task 2: cost-alert 后端核心(PR #2)

**Files:**
- Modify: `E:/backend/core/rag/llm.py:53-97`(`_post_chat` 增加 usage 返回)
- Modify: `E:/backend/core/rag/llm.py:105-160`(`_post_chat_stream` 加 `stream_options`,尾部收集 usage)
- Modify: `E:/backend/core/rag/llm.py`(末尾追加 `_log_token_usage` 函数)
- Modify: `E:/backend/api/chat.py`(在 `_chat_events` 入口加 cost-alert level≥3 early-return)
- Modify: `E:/backend/api/dashboard.py:176-184`(`_build_overview` 加 `cost_alert` 字段 + 新增 `_read_cost_alert`)
- Create: `E:/scripts/cost-alert.ps1`
- Create: `E:/tests/unit/test_cost_alert.py`
- Create: `E:/tests/unit/test_chat_cost_alert_block.py`
- Create: `E:/tests/unit/test_dashboard_cost_alert.py`

**Interfaces:**
- Consumes: 既有 `backend.core.rag.llm._post_chat`(返回 str)、`_post_chat_stream`(Iterator[str]);`backend.api.chat._chat_events`;`backend.api.dashboard._build_overview`
- Produces:
  - `llm._post_chat(...) -> Tuple[str, Optional[Dict[str, int]]]`:content + usage (input/output token)
  - `llm._post_chat_stream(...) -> Iterator[Dict[str, Any]]`:yield `{"type": "delta", "text": "..."}` 或 `{"type": "usage", "input_tokens": N, "output_tokens": N}`(最后一段)
  - `llm._log_token_usage(model: str, input_tokens: int, output_tokens: int) -> None`:atomic_append 到 `data/cost_log.jsonl`
  - `chat._chat_events`:入口检查 `cost_alert.level >= 3` → early-return 503
  - `dashboard._read_cost_alert() -> Dict[str, Any]`:从 `data/health_status.json` 读 cost_alert 字段
  - `dashboard._build_overview()` 返回 dict 多含 `cost_alert` key
  - `scripts/cost-alert.ps1`:`-Month "2026-07"` 累加 + 等级判定 + 写 health_status.json

### 2.1 重构 `_post_chat` 返回 usage

- [ ] **Step 1: 写失败的测试(test_cost_alert.py 内首测)**

```python
# tests/unit/test_cost_alert.py
"""Cost-alert 后端逻辑单测 (v1.3.0)。"""
import json
import os
from pathlib import Path
from unittest.mock import patch, MagicMock

import pytest


@pytest.fixture
def tmp_data_dir(tmp_path, monkeypatch):
    """把 data/ 路径指向 tmp_path。"""
    monkeypatch.setattr("backend.core.config.get_data_dir", lambda: tmp_path)
    return tmp_path


def test_post_chat_returns_usage_dict(tmp_data_dir):
    """_post_chat 应返回 (content, usage) 元组,usage 含 input/output_tokens。"""
    from backend.core.rag.llm import _post_chat

    fake_response = {
        "choices": [{"message": {"content": "hi"}}],
        "usage": {"input_tokens": 10, "output_tokens": 5},
    }
    with patch("backend.core.rag.llm.urllib.request.urlopen") as mock_urlopen:
        mock_resp = MagicMock()
        mock_resp.read.return_value = json.dumps(fake_response).encode("utf-8")
        mock_resp.__enter__ = lambda self: self
        mock_resp.__exit__ = lambda self, *a: None
        mock_urlopen.return_value = mock_resp

        content, usage = _post_chat("fake_key", "qwen3.6-plus", [{"role": "user", "content": "hi"}])

    assert content == "hi"
    assert usage == {"input_tokens": 10, "output_tokens": 5}
```

- [ ] **Step 2: 跑测试,验证失败**

```bash
cd E:/
backend/.venv/Scripts/python.exe -m pytest tests/unit/test_cost_alert.py::test_post_chat_returns_usage_dict -v
```

预期:FAIL(因为当前 `_post_chat` 只返回 str,不是 tuple)

- [ ] **Step 3: 重构 `_post_chat` 返回 (content, usage)**

修改 `backend/core/rag/llm.py:53-97`:

```python
def _post_chat(
    api_key: str,
    model: str,
    messages: List[Dict[str, Any]],
    *,
    max_tokens: int = DEFAULT_MAX_TOKENS,
    timeout: int = 60,
) -> Tuple[str, Optional[Dict[str, int]]]:
    """Call non-streaming chat completion; return (content, usage).

    v1.3.0: 增加 usage 返回,供 cost-alert 计量。
    usage 字段为 None 时(API 异常 / 不返回),调用方应跳过 cost 记录。
    """
    body = {
        "model": model,
        "messages": messages,
        "max_tokens": max_tokens,
    }
    payload = json.dumps(body, ensure_ascii=False).encode("utf-8")
    req = urllib.request.Request(
        CHAT_URL,
        data=payload,
        headers={
            "Authorization": f"Bearer {api_key}",
            "Content-Type": "application/json",
        },
        method="POST",
    )
    try:
        with urllib.request.urlopen(req, timeout=timeout) as resp:
            raw = resp.read().decode("utf-8", errors="replace")
    except urllib.error.HTTPError as e:
        body_text = e.read().decode("utf-8", errors="replace")
        redacted = re.sub(r"(Bearer\s+)[A-Za-z0-9._-]+", r"\1***", body_text)
        raise RuntimeError(f"LLM API HTTP {e.code}: {redacted[:500]}")
    except urllib.error.URLError as e:
        raise RuntimeError(f"LLM API unreachable: {e}")
    try:
        parsed = json.loads(raw)
    except json.JSONDecodeError:
        raise RuntimeError(f"LLM returned non-JSON: {raw[:200]}")
    choices = parsed.get("choices") or []
    if not choices:
        raise RuntimeError(f"LLM returned no choices: {raw[:200]}")
    msg = choices[0].get("message") or {}
    content = msg.get("content")
    if content is None:
        raise RuntimeError(f"LLM returned empty content: {raw[:200]}")
    usage = parsed.get("usage")
    if usage is not None:
        # 规整为统一字段名(input_tokens / output_tokens)
        usage = {
            "input_tokens": int(usage.get("input_tokens", usage.get("prompt_tokens", 0))),
            "output_tokens": int(usage.get("output_tokens", usage.get("completion_tokens", 0))),
        }
    return content, usage
```

- [ ] **Step 4: 跑测试,验证通过**

```bash
cd E:/
backend/.venv/Scripts/python.exe -m pytest tests/unit/test_cost_alert.py::test_post_chat_returns_usage_dict -v
```

预期:PASS

### 2.2 添加 `_log_token_usage` hook + atomic_append 写入

- [ ] **Step 5: 写第二个测试**

在 `tests/unit/test_cost_alert.py` 追加:

```python
def test_log_token_usage_appends_to_cost_log(tmp_data_dir):
    """_log_token_usage 应原子追加一行到 data/cost_log.jsonl。"""
    from backend.core.rag.llm import _log_token_usage

    _log_token_usage("qwen3.6-plus", 100, 50)

    log_file = tmp_data_dir / "cost_log.jsonl"
    assert log_file.exists()
    lines = log_file.read_text(encoding="utf-8").strip().split("\n")
    assert len(lines) == 1
    entry = json.loads(lines[0])
    assert entry["model"] == "qwen3.6-plus"
    assert entry["in"] == 100
    assert entry["out"] == 50
    assert "ts" in entry  # ISO8601
    assert "cost_yuan" not in entry  # 不在 log 时算
```

- [ ] **Step 6: 跑测试,验证失败**

```bash
cd E:/
backend/.venv/Scripts/python.exe -m pytest tests/unit/test_cost_alert.py::test_log_token_usage_appends_to_cost_log -v
```

预期:FAIL(ImportError: cannot import name '_log_token_usage')

- [ ] **Step 7: 添加 `_log_token_usage` 函数**

在 `backend/core/rag/llm.py` 文件末尾追加:

```python
# ---------------------------------------------------------------------------
# v1.3.0: token usage logging for cost-alert
# ---------------------------------------------------------------------------


def _log_token_usage(model: str, input_tokens: int, output_tokens: int) -> None:
    """Append one usage record to data/cost_log.jsonl atomically.

    单行格式: {ts: ISO8601, model: str, in: int, out: int}
    cost_yuan 不在此处计算,由 cost-alert.ps1 rollup 时按当时单价表算
    (单一真相源,价格变动不需要重算历史)。

    调用方应 try/except 包裹;失败不阻断主流程。
    """
    try:
        from backend.core.config import get_data_dir
        from backend.core.atomic_io import atomic_append_jsonl

        data_dir = get_data_dir()
        log_path = data_dir / "cost_log.jsonl"
        atomic_append_jsonl(log_path, {
            "ts": datetime.now(timezone.utc).isoformat(),
            "model": model,
            "in": int(input_tokens),
            "out": int(output_tokens),
        })
    except Exception:
        # Persistence failure is non-fatal; chat flow continues.
        pass
```

需要在文件顶部 import 加上:

```python
from datetime import datetime, timezone
```

- [ ] **Step 8: 跑测试,验证通过**

```bash
cd E:/
backend/.venv/Scripts/python.exe -m pytest tests/unit/test_cost_alert.py::test_log_token_usage_appends_to_cost_log -v
```

预期:PASS

### 2.3 chat_with_fallback 调用 _log_token_usage

- [ ] **Step 9: 找 `chat_with_fallback` 函数位置**

```bash
cd E:/
grep -n "def chat_with_fallback\|def chat_stream_with_fallback" backend/core/rag/llm.py
```

记录行号。

- [ ] **Step 10: 写第三个测试**

在 `tests/unit/test_cost_alert.py` 追加:

```python
def test_chat_with_fallback_logs_usage(tmp_data_dir):
    """chat_with_fallback 应在成功后调用 _log_token_usage。"""
    from backend.core.rag import llm

    with patch.object(llm, "_post_chat", return_value=("hi", {"input_tokens": 20, "output_tokens": 10})):
        with patch.object(llm, "_log_token_usage") as mock_log:
            llm.chat_with_fallback(api_key="fake", model="qwen3.6-plus",
                                   messages=[{"role": "user", "content": "hi"}])

    mock_log.assert_called_once()
    args, kwargs = mock_log.call_args
    # args=(model, input, output)
    assert args[0] == "qwen3.6-plus"
    assert args[1] == 20
    assert args[2] == 10
```

- [ ] **Step 11: 跑测试,验证失败**

```bash
cd E:/
backend/.venv/Scripts/python.exe -m pytest tests/unit/test_cost_alert.py::test_chat_with_fallback_logs_usage -v
```

预期:FAIL(mock_log 没被调用)

- [ ] **Step 12: 修改 `chat_with_fallback` 调用 `_log_token_usage`**

定位 `chat_with_fallback`(在 `backend/core/rag/llm.py` 大约 line 200+),修改为类似:

```python
def chat_with_fallback(...):
    # ... 既有 fallback 循环 ...
    for idx, (model, reason) in enumerate(zip(attempts, reasons)):
        if idx > 0:
            time.sleep(FALLBACK_RETRY_DELAY_L3 if idx == 3 else FALLBACK_RETRY_DELAY_L1)
        try:
            # v1.3.0: _post_chat 现在返回 (content, usage) tuple
            content, usage = _post_chat(api_key, model, messages, max_tokens=max_tokens)
            meta["model"] = model
            meta["reason"] = reason
            if usage is not None:
                _log_token_usage(model, usage["input_tokens"], usage["output_tokens"])
            if idx > 0:
                _record_event(session_id, query_for_event, source="llm",
                              reason=events[idx] or "retry_ok", model=model)
            return content
        except Exception as e:
            last_error = e
            _record_event(session_id, query_for_event, source="llm",
                          reason=f"attempt_fail:{type(e).__name__}", model=model)
    assert last_error is not 0
    raise last_error
```

**注意**:具体行号与既有代码细节可能略有差异;按 git blame + 编辑器查实际代码精修。关键是:
1. `_post_chat` 调用改为 `(content, usage) = _post_chat(...)`
2. 成功路径增加 `if usage is not None: _log_token_usage(model, usage["input_tokens"], usage["output_tokens"])`

- [ ] **Step 13: 跑测试,验证通过**

```bash
cd E:/
backend/.venv/Scripts/python.exe -m pytest tests/unit/test_cost_alert.py::test_chat_with_fallback_logs_usage -v
```

预期:PASS

- [ ] **Step 14: 跑既有 chat 相关测试,确认不回归**

```bash
cd E:/
backend/.venv/Scripts/python.exe -m pytest tests/unit/ -q -k "chat or llm"
```

预期:既有测试全部通过(若 fail,精修 `chat_with_fallback` 的元组解包)

### 2.4 streaming 版本:加 `stream_options={"include_usage": True}` + 尾部收集 usage

- [ ] **Step 15: 写第四个测试**

在 `tests/unit/test_cost_alert.py` 追加:

```python
def test_post_chat_stream_yields_usage_at_end(tmp_data_dir):
    """_post_chat_stream 最后一段 delta 应包含 usage 信息。"""
    from backend.core.rag.llm import _post_chat_stream

    # 模拟 OpenAI 兼容 streaming 响应:
    # data: {"choices":[{"delta":{"content":"hi"}}]}
    # data: {"choices":[{"delta":{}}], "usage":{"input_tokens":15,"output_tokens":3}}
    # data: [DONE]
    chunks = [
        b'data: {"choices":[{"delta":{"content":"hi"}}]}\n\n',
        b'data: {"choices":[{"delta":{}}], "usage":{"input_tokens":15,"output_tokens":3}}\n\n',
        b'data: [DONE]\n\n',
    ]

    class FakeResp:
        def __init__(self):
            self._chunks = list(chunks)
        def read(self, n=-1):
            if not self._chunks:
                return b""
            return self._chunks.pop(0)
        def readline(self):
            return self.read()
        def __enter__(self):
            return self
        def __exit__(self, *a):
            return None

    with patch("backend.core.rag.llm.urllib.request.urlopen", return_value=FakeResp()):
        events = list(_post_chat_stream("fake_key", "qwen3.6-plus",
                                         [{"role": "user", "content": "hi"}]))

    # 期望:每个事件是 dict,含 type
    types = [e["type"] for e in events]
    assert "delta" in types
    assert "usage" in types
    usage_events = [e for e in events if e["type"] == "usage"]
    assert len(usage_events) == 1
    assert usage_events[0]["input_tokens"] == 15
    assert usage_events[0]["output_tokens"] == 3
```

- [ ] **Step 16: 跑测试,验证失败**

```bash
cd E:/
backend/.venv/Scripts/python.exe -m pytest tests/unit/test_cost_alert.py::test_post_chat_stream_yields_usage_at_end -v
```

预期:FAIL(当前 `_post_chat_stream` yield 的是 str,不是 dict)

- [ ] **Step 17: 修改 `_post_chat_stream` 返回 dict 事件 + 收集 usage**

修改 `backend/core/rag/llm.py:105-160`,关键改动:

```python
def _post_chat_stream(
    api_key: str,
    model: str,
    messages: List[Dict[str, Any]],
    *,
    max_tokens: int = DEFAULT_MAX_TOKENS,
    timeout: int = 120,
) -> Iterator[Dict[str, Any]]:
    """Yield event dicts from the OpenAI-compatible streaming endpoint.

    v1.3.0: 返回结构改为 event dict,支持 usage 收集:
      {"type": "delta", "text": "..."}
      {"type": "usage", "input_tokens": N, "output_tokens": N}  (仅最后)

    在 body 加 stream_options={"include_usage": True},尾部从最后 chunk 取 usage。
    """
    body = {
        "model": model,
        "messages": messages,
        "max_tokens": max_tokens,
        "stream": True,
        "stream_options": {"include_usage": True},
    }
    payload = json.dumps(body, ensure_ascii=False).encode("utf-8")
    req = urllib.request.Request(
        CHAT_URL,
        data=payload,
        headers={
            "Authorization": f"Bearer {api_key}",
            "Content-Type": "application/json",
        },
        method="POST",
    )
    accumulated_usage: Optional[Dict[str, int]] = None
    try:
        with urllib.request.urlopen(req, timeout=timeout) as resp:
            for raw_line in resp:
                line = raw_line.decode("utf-8", errors="replace").strip()
                if not line.startswith("data: "):
                    continue
                data_str = line[len("data: "):]
                if data_str == "[DONE]":
                    break
                try:
                    chunk = json.loads(data_str)
                except json.JSONDecodeError:
                    continue
                # 收集 usage(可能在最后一个 chunk 的 choices.delta 为空时出现)
                if "usage" in chunk and chunk["usage"]:
                    u = chunk["usage"]
                    accumulated_usage = {
                        "input_tokens": int(u.get("input_tokens", u.get("prompt_tokens", 0))),
                        "output_tokens": int(u.get("output_tokens", u.get("completion_tokens", 0))),
                    }
                # 提取 delta.content
                choices = chunk.get("choices") or []
                if choices:
                    delta = choices[0].get("delta") or {}
                    text = delta.get("content")
                    if text:
                        yield {"type": "delta", "text": text}
    except urllib.error.HTTPError as e:
        body_text = e.read().decode("utf-8", errors="replace")
        redacted = re.sub(r"(Bearer\s+)[A-Za-z0-9._-]+", r"\1***", body_text)
        raise RuntimeError(f"LLM API HTTP {e.code}: {redacted[:500]}")
    except urllib.error.URLError as e:
        raise RuntimeError(f"LLM API unreachable: {e}")

    # 流结束后,如果收集到 usage,作为最后一个事件 yield
    if accumulated_usage is not None:
        yield {"type": "usage", **accumulated_usage}
```

**注意**:既有调用方(chat_stream_with_fallback、app.tsx)期望 yield str。Step 18 同步修改调用方。

- [ ] **Step 18: 同步修改 `chat_stream_with_fallback` + 上游调用**

`chat_stream_with_fallback`(llm.py line 280+)修改:

```python
for idx, (model, reason) in enumerate(zip(attempts, reasons)):
    if idx > 0:
        time.sleep(...)
    yielded_any = False
    accumulated_text = ""
    last_usage = None
    try:
        for event in _post_chat_stream(api_key, model, messages, max_tokens=max_tokens):
            if event["type"] == "delta":
                yielded_any = True
                accumulated_text += event["text"]
                yield event["text"]  # 兼容既有契约:仍 yield str
            elif event["type"] == "usage":
                last_usage = event
        meta["model"] = model
        meta["reason"] = reason
        if last_usage is not None:
            _log_token_usage(model, last_usage["input_tokens"], last_usage["output_tokens"])
        if idx > 0:
            _record_event(...)
        return
    except Exception as e:
        # ... 既有 ...
```

**关键点**:对外契约保持 yield str(向后兼容),内部消费 `{"type": "delta"}` 与 `{"type": "usage"}` 两类事件。

- [ ] **Step 19: 跑 streaming 测试**

```bash
cd E:/
backend/.venv/Scripts/python.exe -m pytest tests/unit/test_cost_alert.py::test_post_chat_stream_yields_usage_at_end -v
```

预期:PASS

- [ ] **Step 20: 跑既有 streaming_extract 测试,确认不回归**

```bash
cd E:/
backend/.venv/Scripts/python.exe -m pytest tests/unit/test_streaming_extract.py -v
```

预期:既有测试全部通过(因为对外契约保持 yield str)

### 2.5 chat.py 加 cost-alert level≥3 阻断分支

- [ ] **Step 21: 写阻断分支测试**

`tests/unit/test_chat_cost_alert_block.py`:

```python
"""Chat cost-alert 阻断分支单测 (v1.3.0)。"""
import json
from pathlib import Path
from unittest.mock import patch, MagicMock

import pytest
from fastapi.testclient import TestClient


@pytest.fixture
def client_with_health_status(tmp_path, monkeypatch):
    """把 health_status.json 写到 tmp_path;启动 TestClient。"""
    monkeypatch.setattr("backend.core.config.get_data_dir", lambda: tmp_path)
    health = {
        "cost_alert": {
            "level": 3,
            "today_yuan": 1500.0,
            "month_yuan": 1800.0,
            "thresholds": {"warn": 500, "high": 1000, "block": 1500},
        }
    }
    (tmp_path / "health_status.json").write_text(json.dumps(health), encoding="utf-8")

    from backend.main import app
    return TestClient(app)


def test_chat_returns_503_when_cost_alert_level_3(client_with_health_status):
    """cost_alert.level >= 3 时,/api/chat 应返回 503 且不触发 retrieve。"""
    client = client_with_health_status
    response = client.post("/api/chat", json={"question": "hello"})
    assert response.status_code == 503
    detail = response.json().get("detail", {})
    assert detail.get("reason") == "monthly_cost_exceeded"
    assert detail.get("month_yuan") == 1800.0
```

- [ ] **Step 22: 跑测试,验证失败**

```bash
cd E:/
backend/.venv/Scripts/python.exe -m pytest tests/unit/test_chat_cost_alert_block.py::test_chat_returns_503_when_cost_alert_level_3 -v
```

预期:FAIL(因为 chat.py 还没加阻断分支)

- [ ] **Step 23: 添加阻断分支**

修改 `backend/api/chat.py`。在 `_chat_events(payload)` 函数开头(line 139+),加 early-return:

```python
async def _chat_events(payload: ChatRequest) -> AsyncIterator[Dict[str, Any]]:
    # v1.3.0: cost-alert level >= 3 在 retrieve 之前 early-return 503
    # 省一次 Qdrant + keyword query;知识库检索仍可在其他端点使用。
    from backend.api.dashboard import _read_cost_alert  # 局部 import 防循环
    cost_alert = _read_cost_alert()
    if cost_alert.get("level", 0) >= 3:
        # 写 degradation_events 便于诊断
        try:
            save_degradation_event(
                session_id=payload.session_id,
                query=payload.question if hasattr(payload, "question") else None,
                source="llm",
                reason=f"cost-alert block: ¥{cost_alert.get('month_yuan', 0):.0f}",
                component="LLM",
                model=None,
            )
        except Exception:
            pass
        # 抛 HTTPException(由 FastAPI 处理)
        raise HTTPException(
            status_code=503,
            detail={
                "reason": "monthly_cost_exceeded",
                "month_yuan": cost_alert.get("month_yuan", 0),
                "threshold": cost_alert.get("thresholds", {}).get("block", 1500),
                "hint": "知识库检索仍可用,降级到 websearch",
            },
        )

    # 既有 soft_warning + retrieve 逻辑 ...
    soft_warning: Optional[str] = None
    # ... 既有代码 ...
```

需要在文件顶部 import:

```python
from fastapi import HTTPException  # 已有则不动
from backend.api.dashboard import _read_cost_alert  # 或移动到顶部
```

**注意**:`HTTPException` 在 streaming/SSE 路径可能不会走 FastAPI 的正常处理路径。验证 chat endpoint 是否 SSE;如果是,SSE 路径上抛 HTTPException 可能需要包成 SSE error event。**实施时按实际情况精修**。如果 SSE 路径不能抛 HTTPException,改为 yield `{"type": "error", ...}` 事件。

- [ ] **Step 24: 跑测试,验证通过(可能要按实际 SSE 行为调整)**

```bash
cd E:/
backend/.venv/Scripts/python.exe -m pytest tests/unit/test_chat_cost_alert_block.py -v
```

预期:PASS(若 SSE 路径需调整,按实际情况精修)

### 2.6 dashboard.py 加 cost_alert 字段

- [ ] **Step 25: 写 dashboard 测试**

`tests/unit/test_dashboard_cost_alert.py`:

```python
"""Dashboard cost-alert 字段单测 (v1.3.0)。"""
import json
from unittest.mock import patch

import pytest


@pytest.fixture
def tmp_data_dir(tmp_path, monkeypatch):
    monkeypatch.setattr("backend.core.config.get_data_dir", lambda: tmp_path)
    return tmp_path


def test_overview_includes_cost_alert(tmp_data_dir):
    """_build_overview 应返回包含 cost_alert 字段的 dict。"""
    from backend.api import dashboard

    health = {"cost_alert": {"level": 1, "today_yuan": 250.0, "month_yuan": 250.0,
                              "thresholds": {"warn": 500, "high": 1000, "block": 1500}}}
    (tmp_data_dir / "health_status.json").write_text(json.dumps(health), encoding="utf-8")

    # 抑制真实 IO(health_summary / kb_stats 等)
    with patch.object(dashboard, "_health_summary", return_value={}):
        with patch.object(dashboard, "_degradations_summary", return_value={}):
            with patch.object(dashboard, "_kb_stats", return_value={}):
                with patch.object(dashboard, "_drift_check", return_value={}):
                    with patch.object(dashboard, "_system_info", return_value={}):
                        overview = dashboard._build_overview()

    assert "cost_alert" in overview
    assert overview["cost_alert"]["level"] == 1
    assert overview["cost_alert"]["month_yuan"] == 250.0
```

- [ ] **Step 26: 跑测试,验证失败**

```bash
cd E:/
backend/.venv/Scripts/python.exe -m pytest tests/unit/test_dashboard_cost_alert.py -v
```

预期:FAIL(KeyError: 'cost_alert')

- [ ] **Step 27: 添加 `_read_cost_alert` + 在 `_build_overview` 加字段**

修改 `backend/api/dashboard.py`,新增 `_read_cost_alert` 函数 + 修改 `_build_overview`:

```python
def _read_cost_alert() -> Dict[str, Any]:
    """v1.3.0: 从 data/health_status.json 读 cost_alert 字段。

    文件不存在 / JSON 损坏 / 字段缺失 → 返回默认 level=0(不阻断)。
    """
    try:
        from backend.core.config import get_data_dir
        data_dir = get_data_dir()
        health_path = data_dir / "health_status.json"
        if not health_path.exists():
            return {"level": 0, "today_yuan": 0.0, "month_yuan": 0.0,
                    "thresholds": {"warn": 500, "high": 1000, "block": 1500}}
        health = json.loads(health_path.read_text(encoding="utf-8"))
        ca = health.get("cost_alert")
        if not isinstance(ca, dict):
            return {"level": 0, "today_yuan": 0.0, "month_yuan": 0.0,
                    "thresholds": {"warn": 500, "high": 1000, "block": 1500}}
        return ca
    except Exception:
        return {"level": 0, "today_yuan": 0.0, "month_yuan": 0.0,
                "thresholds": {"warn": 500, "high": 1000, "block": 1500}}


def _build_overview() -> Dict[str, Any]:
    return {
        "health": _health_summary(),
        "degradations_24h": _degradations_summary(),
        "kb_stats": _kb_stats(),
        "drift": _drift_check(),
        "system": _system_info(),
        "cost_alert": _read_cost_alert(),  # v1.3.0
        "timestamp": datetime.now(timezone.utc).isoformat(),
    }
```

需要在 dashboard.py 顶部 import:

```python
import json  # 若未 import
from backend.core.config import get_data_dir  # 若未 import
```

- [ ] **Step 28: 跑测试,验证通过**

```bash
cd E:/
backend/.venv/Scripts/python.exe -m pytest tests/unit/test_dashboard_cost_alert.py -v
```

预期:PASS

### 2.7 cost-alert.ps1 脚本

- [ ] **Step 29: 创建 scripts/cost-alert.ps1**

```powershell
<#
.SYNOPSIS
  KB-AI · 月度配额告警 rollup (v1.3.0)

.DESCRIPTION
  从 data/cost_log.jsonl 累加当日/当月 LLM 调用 token,
  按单价表折算人民币,比对 .env 中 COST_ALERT_THRESHOLDS (可选),
  写入 data/health_status.json 的 cost_alert 字段。

  月度 = UTC 自然月(每月 1 日 00:00:00 UTC 起算)。
  回溯范围:本年初起全量。

.PARAMETER Month
  显式指定要 rollup 的月份 (YYYY-MM);默认 = 当前 UTC 月。

.PARAMETER DryRun
  只打印,不写 health_status.json。

.EXAMPLE
  pwsh -File scripts/cost-alert.ps1
  pwsh -File scripts/cost-alert.ps1 -Month "2026-07"
  pwsh -File scripts/cost-alert.ps1 -DryRun

.NOTES
  PowerShell 5.1 兼容。
  退出码:0 成功;1 累加失败但继续(level=0);2 文件不存在(level=0 不写)。
#>

[CmdletBinding()]
param(
    [string]$Month = "",
    [switch]$DryRun = $false
)

$ErrorActionPreference = "Continue"

$scriptRoot = if ($PSScriptRoot) { $PSScriptRoot } else { Split-Path -Parent $MyInvocation.MyCommand.Path }
$root = Split-Path -Parent $scriptRoot
$dataDir = Join-Path $root 'data'
$logPath = Join-Path $dataDir 'cost_log.jsonl'
$healthPath = Join-Path $dataDir 'health_status.json'

# 单价表(2026-07 阿里云百炼公示;实施时核对官方定价)
$priceTable = @{
    "qwen3.6-plus"     = @{ input = 4.0;   output = 12.0 }
    "qwen3.7-max"      = @{ input = 12.0;  output = 36.0 }
    "text-embedding-v3" = @{ input = 0.7;  output = 0.0 }
}

# 阈值默认值
$defaultWarn  = 500.0
$defaultHigh  = 1000.0
$defaultBlock = 1500.0

# 读 .env 中 COST_ALERT_THRESHOLDS(可选;格式: "warn:500,high:1000,block:1500")
$thresholds = @{ warn = $defaultWarn; high = $defaultHigh; block = $defaultBlock }
$envPath = Join-Path $root '.env'
if (Test-Path $envPath) {
    foreach ($line in (Get-Content $envPath)) {
        if ($line -match '^COST_ALERT_THRESHOLDS\s*=\s*(.+)$') {
            $raw = $Matches[1].Trim()
            foreach ($pair in ($raw -split ',')) {
                if ($pair -match '^(warn|high|block):(\d+(?:\.\d+)?)$') {
                    $thresholds[$Matches[1]] = [double]$Matches[2]
                }
            }
        }
    }
}

# 月份
if (-not $Month) {
    $Month = (Get-Date).ToUniversalTime().ToString("yyyy-MM")
}
$yearMonth = $Month
Write-Host "[cost-alert] Rollup month: $yearMonth" -ForegroundColor Cyan
Write-Host "[cost-alert] Thresholds: warn=$($thresholds.warn), high=$($thresholds.high), block=$($thresholds.block)" -ForegroundColor Cyan

# 读 cost_log.jsonl
if (-not (Test-Path $logPath)) {
    Write-Host "[cost-alert] [WARN] $logPath 不存在;写入 level=0" -ForegroundColor Yellow
    $todayYuan = 0.0
    $monthYuan = 0.0
    $todayDate = (Get-Date).ToUniversalTime().ToString("yyyy-MM-dd")
} else {
    $todayDate = (Get-Date).ToUniversalTime().ToString("yyyy-MM-dd")
    $monthStart = Get-Date ("$yearMonth-01T00:00:00Z").Trim() -Format "o"
    $todayYuan = 0.0
    $monthYuan = 0.0
    $todayCount = 0
    $monthCount = 0

    foreach ($line in (Get-Content $logPath)) {
        if ([string]::IsNullOrWhiteSpace($line)) { continue }
        try {
            $entry = $line | ConvertFrom-Json -ErrorAction Stop
        } catch {
            continue
        }
        if (-not $entry.model -or $null -eq $entry.in -or $null -eq $entry.out) { continue }
        $price = $priceTable[$entry.model]
        if (-not $price) { continue }
        $cost = ($entry.in / 1000000.0) * $price.input + ($entry.out / 1000000.0) * $price.output

        if ($entry.ts -like "$todayDate*") {
            $todayYuan += $cost
            $todayCount++
        }
        if ($entry.ts -like "$yearMonth*") {
            $monthYuan += $cost
            $monthCount++
        }
    }

    Write-Host "[cost-alert] Today ($todayDate): ¥$([math]::Round($todayYuan, 2)) / $todayCount calls" -ForegroundColor Green
    Write-Host "[cost-alert] Month ($yearMonth): ¥$([math]::Round($monthYuan, 2)) / $monthCount calls" -ForegroundColor Green
}

# 等级判定
$level = 0
if ($monthYuan -ge $thresholds.warn)  { $level = 1 }
if ($monthYuan -ge $thresholds.high)  { $level = 2 }
if ($monthYuan -ge $thresholds.block) { $level = 3 }
Write-Host "[cost-alert] Level: $level (monthly ¥$([math]::Round($monthYuan, 0)) / block=$($thresholds.block))" -ForegroundColor $(if ($level -ge 3) { "Red" } elseif ($level -ge 2) { "Yellow" } else { "Green" })

if ($DryRun) {
    Write-Host "[cost-alert] [DRY-RUN] would write to $healthPath" -ForegroundColor Cyan
    exit 0
}

# 写 health_status.json(读 → 合并 cost_alert → atomic 写回)
if (-not (Test-Path $dataDir)) { New-Item -ItemType Directory -Path $dataDir -Force | Out-Null }
$existing = @{}
if (Test-Path $healthPath) {
    try {
        $existing = Get-Content $healthPath -Raw | ConvertFrom-Json -ErrorAction Stop
        $existingHt = @{}
        $existing.PSObject.Properties | ForEach-Object { $existingHt[$_.Name] = $_.Value }
        $existing = $existingHt
    } catch {
        Write-Host "[cost-alert] [WARN] $healthPath JSON 损坏;按空对象处理" -ForegroundColor Yellow
        $existing = @{}
    }
}
$existing["cost_alert"] = @{
    level = $level
    today_yuan = [math]::Round($todayYuan, 2)
    month_yuan = [math]::Round($monthYuan, 2)
    month = $yearMonth
    thresholds = $thresholds
    updated_at = (Get-Date).ToUniversalTime().ToString("o")
}

# 原子写
$json = $existing | ConvertTo-Json -Depth 5
$tmp = "$healthPath.tmp"
[System.IO.File]::WriteAllText($tmp, $json, [System.Text.UTF8Encoding]::new($false))
Move-Item -Force $tmp $healthPath

Write-Host "[cost-alert] ✓ Wrote $healthPath (level=$level)" -ForegroundColor Green
exit 0
```

- [ ] **Step 30: 手测 cost-alert.ps1 -DryRun**

```bash
cd E:/
pwsh -File scripts/cost-alert.ps1 -DryRun
```

预期:输出阈值 + level=0 + `[DRY-RUN] would write to ...\data\health_status.json`

- [ ] **Step 31: 手测 cost-alert.ps1(无 log 文件)**

```bash
cd E:/
pwsh -File scripts/cost-alert.ps1
```

预期:写入 `data/health_status.json` 含 `cost_alert.level=0`

- [ ] **Step 32: 跑 Task 2 全部新测 + 既有回归**

```bash
cd E:/
backend/.venv/Scripts/python.exe -m pytest tests/unit/test_cost_alert.py tests/unit/test_chat_cost_alert_block.py tests/unit/test_dashboard_cost_alert.py -v
backend/.venv/Scripts/python.exe -m pytest tests/unit/ -q
```

预期:
- Task 2 新测全部 PASS
- 既有 252 测不回归(可能 ±1 因为白名单代码改动)

- [ ] **Step 33: Commit + Push(需用户单独授权)**

```bash
cd E:/
git add backend/core/rag/llm.py backend/api/chat.py backend/api/dashboard.py
git add scripts/cost-alert.ps1
git add tests/unit/test_cost_alert.py tests/unit/test_chat_cost_alert_block.py tests/unit/test_dashboard_cost_alert.py
git commit -m "feat(cost-alert): LLM token 计量 + 月度配额告警 + dashboard 字段 + 阻断分支

- backend/core/rag/llm.py:
  - _post_chat 返回 (content, usage) tuple
  - _post_chat_stream 加 stream_options={include_usage: True},尾部 yield usage event
  - 新增 _log_token_usage hook (atomic_append to cost_log.jsonl)
  - chat_with_fallback / chat_stream_with_fallback 调用 _log_token_usage
- backend/api/chat.py:
  - _chat_events 入口加 cost_alert.level >= 3 early-return 503 (retrieve 之前)
  - 写 degradation_events(component='LLM') 便于诊断
- backend/api/dashboard.py:
  - 新增 _read_cost_alert() 读 health_status.json.cost_alert 字段
  - _build_overview 追加 cost_alert 字段
- scripts/cost-alert.ps1:
  - 从 cost_log.jsonl 累加当日/当月成本 (UTC 自然月)
  - 按单价表折算,比对 .env COST_ALERT_THRESHOLDS,写 health_status.json
  - PS 5.1 兼容,DryRun 支持
- tests/unit/test_cost_alert.py (6 测)
- tests/unit/test_chat_cost_alert_block.py (1 测)
- tests/unit/test_dashboard_cost_alert.py (1 测)

Refs: docs/superpowers/specs/2026-07-21-v1.3-ops-hardening-design.md §2.4"
git push origin master  # 需用户单独授权
```

---

## Task 3: cost-alert 前端卡片(PR #3)

**Files:**
- Modify: `E:/frontend/src/lib/types.ts`(加 `CostAlert` 接口)
- Modify: `E:/frontend/src/pages/DashboardPage.tsx`(加 cost-alert 卡片)
- Create: `E:/frontend/src/__tests__/DashboardPage.test.tsx`

**Interfaces:**
- Consumes: 既有 `DashboardOverview` 类型(在 `types.ts` 中);`DashboardPage.tsx` 既有 30s 轮询 `GET /api/dashboard/overview`
- Produces:
  - `CostAlert` 接口:`{ level: number; today_yuan: number; month_yuan: number; month: string; thresholds: { warn, high, block }; updated_at: string }`
  - `DashboardOverview` 多含 `cost_alert?: CostAlert` 字段
  - `DashboardPage` 渲染 cost-alert 卡片(level=0 进度条 / level≥2 顶部橙色告警条)

### 3.1 类型定义

- [ ] **Step 1: 在 types.ts 加 CostAlert 接口**

打开 `E:/frontend/src/lib/types.ts`,在 `DashboardOverview` 定义附近加:

```typescript
/**
 * v1.3.0: cost-alert 月度配额告警字段。
 * 从 GET /api/dashboard/overview 返回,缺失时降级为 "用量数据采集中"。
 */
export interface CostAlert {
  level: 0 | 1 | 2 | 3;
  today_yuan: number;
  month_yuan: number;
  month: string;  // YYYY-MM
  thresholds: {
    warn: number;
    high: number;
    block: number;
  };
  updated_at: string;  // ISO8601
}
```

同时修改 `DashboardOverview` 类型,把 `cost_alert?` 加为可选字段:

```typescript
export interface DashboardOverview {
  health: HealthSummary;
  degradations_24h: DegradationsSummary;
  kb_stats: KbStats;
  drift: DriftCheck;
  system: SystemInfo;
  cost_alert?: CostAlert;  // v1.3.0
  timestamp: string;
}
```

- [ ] **Step 2: 类型检查**

```bash
cd E:/frontend
npx tsc --noEmit
```

预期:0 错

### 3.2 DashboardPage 加 cost-alert 卡片

- [ ] **Step 3: 读现有 DashboardPage.tsx 顶部结构**

```bash
cd E:/frontend
head -80 src/pages/DashboardPage.tsx
```

记录 imports 与组件函数结构,确定插入位置。

- [ ] **Step 4: 在 DashboardPage.tsx 加 cost-alert 卡片组件**

在 `DashboardPage` 函数内,既有 4 张卡片(health / degradations / kb_stats / drift)之后,加第 5 张卡片:

```tsx
{/* v1.3.0: cost-alert 月度配额告警卡片 */}
<div className="dashboard-card cost-alert-card" data-testid="cost-alert-card">
  <h3>本月用量</h3>
  {overview.cost_alert ? (
    <>
      <div className="cost-progress">
        <div className="cost-yuan">
          ¥{overview.cost_alert.month_yuan.toFixed(2)}
          <span className="cost-month"> / {overview.cost_alert.month}</span>
        </div>
        <div className="cost-bar">
          <div
            className="cost-bar-fill"
            style={{
              width: `${Math.min(100, (overview.cost_alert.month_yuan / overview.cost_alert.thresholds.block) * 100)}%`,
              background: overview.cost_alert.level >= 3 ? '#DC2626' :
                          overview.cost_alert.level >= 2 ? '#D97706' :
                          overview.cost_alert.level >= 1 ? '#FF540E' : '#A1A1AA'
            }}
            data-testid="cost-bar-fill"
          />
        </div>
        <div className="cost-thresholds">
          阈值: ¥{overview.cost_alert.thresholds.warn} (warn) / ¥{overview.cost_alert.thresholds.high} (high) / ¥{overview.cost_alert.thresholds.block} (block)
        </div>
      </div>
      {overview.cost_alert.level >= 2 && (
        <div className="cost-alert-banner" data-testid="cost-alert-banner">
          {overview.cost_alert.level >= 3
            ? `⚠️ 本月用量已达 ¥${overview.cost_alert.month_yuan.toFixed(0)},超过 block 阈值,LLM 调用已临时阻断`
            : `本月用量已达 ¥${overview.cost_alert.month_yuan.toFixed(0)},接近 block 阈值(¥${overview.cost_alert.thresholds.block})`}
        </div>
      )}
    </>
  ) : (
    <div className="cost-alert-loading">用量数据采集中...</div>
  )}
</div>
```

需要在文件顶部 import 加上 `CostAlert` 类型(若未导入)。

### 3.3 样式(global.css 或 inline 已足够)

- [ ] **Step 5: 加 cost-alert 卡片样式**

打开 `E:/frontend/src/styles/global.css`,在 dashboard 卡片样式段后追加:

```css
/* v1.3.0: cost-alert 月度用量卡片 */
.cost-alert-card {
  border: 1px solid rgba(255, 84, 14, 0.3);
  background: rgba(255, 84, 14, 0.04);
}
.cost-progress { display: flex; flex-direction: column; gap: 8px; }
.cost-yuan { font-size: 24px; font-weight: 600; color: #fff; }
.cost-month { font-size: 14px; color: #A1A1AA; margin-left: 8px; }
.cost-bar { height: 8px; background: rgba(255,255,255,0.1); border-radius: 4px; overflow: hidden; }
.cost-bar-fill { height: 100%; transition: width 0.3s ease, background 0.3s ease; }
.cost-thresholds { font-size: 12px; color: #A1A1AA; }
.cost-alert-banner {
  margin-top: 12px;
  padding: 8px 12px;
  background: rgba(217, 119, 6, 0.15);
  border-left: 3px solid #D97706;
  color: #FFEDD5;
  font-size: 14px;
  border-radius: 4px;
}
.cost-alert-loading { color: #A1A1AA; font-size: 14px; }
```

### 3.4 vitest 渲染测试

- [ ] **Step 6: 创建 DashboardPage.test.tsx**

```tsx
// frontend/src/__tests__/DashboardPage.test.tsx
import { describe, it, expect, vi } from 'vitest';
import { render, screen, waitFor } from '@testing-library/react';
import { DashboardPage } from '../pages/DashboardPage';

const baseOverview = {
  health: {},
  degradations_24h: {},
  kb_stats: {},
  drift: {},
  system: {},
  timestamp: new Date().toISOString(),
};

describe('DashboardPage cost-alert 卡片', () => {
  it('level=0 时只显示进度条,无告警 banner', async () => {
    vi.mock('../lib/api', () => ({
      getJSON: vi.fn().mockResolvedValue({
        ...baseOverview,
        cost_alert: {
          level: 0, today_yuan: 100, month_yuan: 250,
          month: '2026-07',
          thresholds: { warn: 500, high: 1000, block: 1500 },
          updated_at: '2026-07-21T16:00:00Z',
        },
      }),
    }));
    render(<DashboardPage />);
    await waitFor(() => {
      expect(screen.getByTestId('cost-bar-fill')).toBeInTheDocument();
    });
    expect(screen.queryByTestId('cost-alert-banner')).not.toBeInTheDocument();
  });

  it('level>=2 时显示橙色告警 banner', async () => {
    vi.mock('../lib/api', () => ({
      getJSON: vi.fn().mockResolvedValue({
        ...baseOverview,
        cost_alert: {
          level: 2, today_yuan: 600, month_yuan: 1100,
          month: '2026-07',
          thresholds: { warn: 500, high: 1000, block: 1500 },
          updated_at: '2026-07-21T16:00:00Z',
        },
      }),
    }));
    render(<DashboardPage />);
    await waitFor(() => {
      expect(screen.getByTestId('cost-alert-banner')).toBeInTheDocument();
    });
    expect(screen.getByTestId('cost-alert-banner').textContent).toMatch(/接近 block 阈值/);
  });
});
```

- [ ] **Step 7: 跑 vitest**

```bash
cd E:/frontend
npx vitest run
```

预期:2 新测 PASS;既有 10 测不回归

- [ ] **Step 8: 跑前端 build**

```bash
cd E:/frontend
npx vite build
```

预期:0 错

- [ ] **Step 9: Commit + Push(需用户单独授权)**

```bash
cd E:/
git add frontend/src/lib/types.ts frontend/src/pages/DashboardPage.tsx frontend/src/styles/global.css frontend/src/__tests__/DashboardPage.test.tsx
git commit -m "feat(ui): Dashboard cost-alert 月度用量卡片 (level=0 进度条 / >=2 告警 banner)

- frontend/src/lib/types.ts: CostAlert 接口 + DashboardOverview 加 cost_alert? 字段
- frontend/src/pages/DashboardPage.tsx: 第 5 张卡片 (cost-alert-card)
  - level=0: 进度条 (灰) + 阈值文字
  - level>=2: 顶部橙色告警 banner
  - 缺失字段: '用量数据采集中...'
- frontend/src/styles/global.css: cost-alert-card + cost-alert-banner 样式
- frontend/src/__tests__/DashboardPage.test.tsx: +2 测 (level=0 / level=2)

Refs: docs/superpowers/specs/2026-07-21-v1.3-ops-hardening-design.md §1.2"
git push origin master  # 需用户单独授权
```

---

## Task 4: 文档收口 release PR(PR #4)

**Files:**
- Modify: `E:/AGENTS.md`(§1 §8 §13)
- Modify: `<private>/.harness/intake/custom-kb-qa-ai-prd-draft.md`(项目外,需用户授权)
- Modify: `E:/QUICKSTART.md`(插入 §9 系统边界)
- Create: `E:/dify/README.md`
- Modify: `E:/README.md`(版本号行 + 开发命令段)
- Modify: `E:/docs/known-limitations.md`(§4 §5 关闭 + 推迟项保留)
- Modify: `E:/docs/acceptance-checklist.md`(顶部日期 + 已知遗留更新)
- Modify: `E:/docs/degradation-guide.md`(加 cost-alert 场景节)
- Modify: `E:/CHANGELOG.md`(顶部追加 v1.3.0 段)
- Modify: `E:/version`(1.2.0 → 1.3.0)
- Modify: `E:/.gitignore`(加 `data/cost_log.jsonl`)

**Interfaces:**
- Consumes: 既有 `docs/superpowers/specs/2026-07-21-v1.3-ops-hardening-design.md`(417 行)作为改动依据
- Produces: v1.3.0 文档基线,使 known-limitations §4 §5 7 项缺口全部关闭或明确推迟

### 4.1 版本号 + .gitignore

- [ ] **Step 1: 改 version 文件**

```bash
cd E:/
echo "1.3.0" > version
cat version
```

预期:`1.3.0`

- [ ] **Step 2: .gitignore 加 cost_log.jsonl**

打开 `E:/.gitignore`,在 data/ 相关规则段后追加:

```
# v1.3.0: cost-alert token 用量日志 (运行时数据,不入仓)
data/cost_log.jsonl
data/cost_log.jsonl.*
```

- [ ] **Step 3: 验证 .gitignore 生效**

```bash
cd E:/
git check-ignore -v data/cost_log.jsonl
```

预期:输出 `.gitignore:XX:data/cost_log.jsonl` 表示被忽略

### 4.2 CHANGELOG

- [ ] **Step 4: CHANGELOG.md 顶部追加 v1.3.0 段**

打开 `E:/CHANGELOG.md`,在 v1.2.0 段(line 7)之前插入新段:

```markdown
## [1.3.0] - 2026-07-21

### 运维加固与文档同步(v1.3.0 收官)

> 目标:把 known-limitations §4 / §5 共 7 项运维/文档缺口一次性收口;增加月度配额告警 + pre-commit/pre-push 双 hook;零业务功能新增。
> 约束:不改变聊天 SSE / Embedding / Qdrant contract / 前端契约;不引入新依赖;不修改 .env / start.bat / stop.bat / package.bat / 容器镜像 tag。

### 变更

- **Item 1 AGENTS.md §13 同步**:`AGENTS.md:393` 追加 v1.2.0(RAG 质量 4 PR)+ v1.3.0(本版本)两节点
- **Item 2 PRD v0.7 全面同步**(`<private>/.harness/intake/custom-kb-qa-ai-prd-draft.md`,项目外,用户授权):头部版本号 / §0 模型命名 / §3.3 应用栈 / §3.7 启动步骤 / §4 验收 / §5 历史(追加四张表:v0.7→v1.0.1 / v1.0.1→v1.1.0 / v1.1.0→v1.2.0 / v1.2.0→v1.3.0)
- **Item 3 QUICKSTART.md 系统边界**:插入 §9「系统边界与限制」(单设备 / 离线能力 / 文件大小 / 对话长度 / 数据规模 / 容器资源 / U 盘物理风险)
- **Item 4 cost-alert.ps1 月度配额告警**:新建 PS 5.1 脚本;`llm.py` 加 `_log_token_usage` hook + `_post_chat` 返回 `(content, usage)` + `_post_chat_stream` 加 `stream_options={"include_usage": True}`;`chat.py` 加 level≥3 阻断分支(retrieve 前 early-return 503);`dashboard.py` 加 `_read_cost_alert()` + `_build_overview` 追加 `cost_alert` 字段;前端 `DashboardPage` 加第 5 张卡片(level=0 进度条 / level≥2 橙色告警 banner);阈值默认 `warn=$500 / high=$1000 / block=$1500`,可 `.env` 覆盖;月度 = UTC 自然月
- **Item 5 dify/README.md**:新建,说明 `knowledge-pipeline.json` 是 v0.7 历史快照
- **Item 6 pre-commit + pre-push 双 hook**:bash hooks 走 `core.hooksPath=scripts/hooks`;pre-commit 跑 ruff;pre-push 跑 `scripts/run-checks.ps1`;`install-hooks.ps1` 含 pwsh 探测降级 + `-DryRun` / `-Uninstall`

### 连带更新

- **README.md**:版本号行 `(v0.8.6)` → `(v1.3.0)`;新增「开发命令」段
- **known-limitations.md**:§4 / §5 7 项 → 4 项已实现/移走 + 3 项明确推迟(§4 #3 Dockerfile / §4 #5 COS / §5 #2 架构评审 Part 2 → v1.3.1+ backlog)
- **acceptance-checklist.md**:顶部日期 2026-07-21;已知遗留移除已关闭项
- **degradation-guide.md**:加 §"cost-alert 阻断时知识库检索仍可用"场景
- **version**:`1.2.0` → `1.3.0`
- **.gitignore**:加 `data/cost_log.jsonl`

### 单测总数

- **后端 pytest**:252 → 261(+9:`test_cost_alert.py` ×6 + `test_chat_cost_alert_block.py` ×1 + `test_dashboard_cost_alert.py` ×1 + 1 个 cost_log 边界 case)
- **前端 vitest**:10 → 12(+2 `DashboardPage.test.tsx`)
- **FastAPI 路由**:20(无变化)

### 不修改(系统策略)

- ❌ `.env` / `package.bat` / `start.bat` / `stop.bat` / 容器镜像 tag / `architecture-validation-report.md` Part 1
- ❌ `.gitignore` 既有规则(只追加不删除)
- ❌ 任何业务代码白名单外的改动(`llm.py` / `chat.py` / `dashboard.py` 三处已锁定)
```

### 4.3 AGENTS.md

- [ ] **Step 5: AGENTS.md §13 追加 v1.2.0 + v1.3.0 节点**

打开 `E:/AGENTS.md:393`(v1.1.0 节点后),在末尾表格追加两行:

```markdown
| 2026-07-21 | v1.2.0 | RAG 质量 4 PR 收官:golden set 50/50 + QueryProfile + 短问题门控 + 年份优先 + 长问题 reranker cap;pytest 223 → 252(+29) |
| 2026-07-21 | v1.3.0 | 运维加固与文档同步:cost-alert 月度配额告警 + pre-commit/pre-push 双 hook + PRD/AGENTS/CHANGELOG/known-limitations/acceptance-checklist/degradation-guide/QUICKSTART/README 全量同步;pytest 252 → 261(+9),vitest 10 → 12(+2);详见 docs/superpowers/specs/2026-07-21-v1.3-ops-hardening-design.md |
```

- [ ] **Step 6: AGENTS.md §8 加"代码门禁"段**

在 `AGENTS.md` §8(已知偏差)前或后加新段:

```markdown
## 8.5 代码门禁(v1.3.0 新增)

开发流第一步(新克隆后):

```powershell
pwsh -File scripts/install-hooks.ps1
```

装上 `core.hooksPath = scripts/hooks`,后续:

- `git commit` 自动跑 `ruff check backend/ tests/`(失败 abort,~2s)
- `git push` 自动跑 `scripts/run-checks.ps1`(全量 ruff + pytest + eslint + vite build,失败 abort,~30s)
- `pwsh` 不在 PATH 时 pre-push 降级为 noop + 警告;手动 `powershell -File scripts/run-checks.ps1` 验证

卸载:`pwsh -File scripts/install-hooks.ps1 -Uninstall`
```

- [ ] **Step 7: AGENTS.md §1 目录树补全 v1.1 / v1.2 新增 api 子文件路径**

按需核对 `backend/api/` 实际文件,补全目录树中漏列的(`tags.py` / `eval.py` / `databases.py` / `dashboard.py` 等)

### 4.4 QUICKSTART.md §9

- [ ] **Step 8: 插入 §9 系统边界与限制**

打开 `E:/QUICKSTART.md`,在 §8"还有问题"之前插入:

```markdown
## 9. 系统边界与限制

请在使用前了解本系统的"硬边界",避免误用:

| 维度 | 边界 | 备注 |
|---|---|---|
| 部署 | **单设备**(U 盘一次只能在 1 台电脑用) | 不支持多设备同步 |
| 用户 | **单用户**,无登录/权限 | 多人共享同一对话记录 |
| 端口 | **127.0.0.1 环回口**(6333/6334/8000/8001) | 不暴露 LAN/公网,无 TLS/Auth |
| 离线能力 | 知识库检索 / 历史对话可用;AI 生成不可用(降级提示) | 无网时降级到 websearch 也不可用 |
| 单文件大小 | **200 MB** | 超出走 `backend/core/rag/mineru.py` SIZE_LIMIT_BYTES |
| 单图大小 | **20 MB** | `/api/knowledge/upload` 413 拦截 |
| 多图批量 | **5 张/次** | `/api/knowledge/upload` 413 拦截 |
| 对话长度 | **1-100 轮**可配(默认 50) | 80% 时 soft_warning;超限截断 |
| 数据规模 | 实测 ~300GB 文档 + ~100GB 向量 | U 盘 1TB 留 ~575GB 余量 |
| 容器资源 | 4 容器无 `mem_limit` / `cpus` | 见 `docs/known-limitations.md` §4 历史 |
| LLM 模型 | qwen3.6-plus(默认) + qwen3.7-max(复杂) | 阿里云百炼 API,商业配额计费 |
| 月度配额 | 默认 block ¥1500/月 | 可 `.env` `COST_ALERT_THRESHOLDS` 覆盖,详见 degradation-guide |
| U 盘物理风险 | 丢失 = 数据外泄;损坏 = 数据丢失 | 用户已接受,见 PRD §2.2 |

**降级场景**详见 [`docs/degradation-guide.md`](docs/degradation-guide.md)。
```

原 §8"还有问题"顺延为 §10。

### 4.5 dify/README.md

- [ ] **Step 9: 新建 dify/README.md**

```markdown
# dify/ 目录说明(v1.3.0)

## 文件作用

- `knowledge-pipeline.json`:**v0.7 时代 Dify Web UI 工作流的导入快照**。

## 当前状态

自 v0.8.0 起,项目已切换到 FastAPI 后端 + React 前端架构,`dify-api` / `dify-worker` 容器仍保留(为内部 LLM 工作流提供 SQLite 模式支持),但**`dify/knowledge-pipeline.json` 不再随架构演进**。该文件不再被运行时使用,仅作历史参考。

## 保留原因

- 历史考古价值:v0.7 时期的 Dify Web UI 工作流配置是项目架构演进的里程碑证据
- 零成本:文件仅几 KB,占用空间可忽略
- 删除不可恢复:verifier / 后人可能需要回溯"原始 Dify pipeline 怎么配的"

## 不要做的事

- ❌ 不要基于该文件改 Dify Web UI 配置(已无意义,v0.8.0 起改走 FastAPI + RAG Python 模块)
- ❌ 不要删除该文件(无授权)
- ❌ 不要把它当文档来阅读(它是 JSON 配置,不是 markdown)

## 关联

- `scripts/dify/knowledge-pipeline.json`(如果存在)是另一份拷贝;以本目录为准
- 真正的运行时配置在 `backend/core/rag/` 11 个 Python 模块中
```

### 4.6 README.md

- [ ] **Step 10: README.md 版本号行更新**

打开 `E:/README.md`,找版本号行(根据已知 v0.8.6 标注),改为:

```markdown
**当前版本**:见根目录 `version` 文件(v1.3.0)
```

- [ ] **Step 11: README.md 新增"开发命令"段**

在 README.md 找合适位置(命令速查 / 开发流 等段),追加:

```markdown
## 开发命令

首次克隆后安装 git hooks(自动门禁):

```bash
pwsh -File scripts/install-hooks.ps1
```

之后:
- `git commit` → 自动 ruff check backend/ tests/(~2s)
- `git push` → 自动 run-checks.ps1 全检 ruff + pytest + eslint + vite build(~30s)

手动全检(任一时刻):

```bash
pwsh -File scripts/run-checks.ps1
```

月度配额 rollup:

```bash
pwsh -File scripts/cost-alert.ps1
```
```

### 4.7 known-limitations.md

- [ ] **Step 12: known-limitations.md §4 / §5 关闭已实现项 + 保留推迟项**

打开 `E:/docs/known-limitations.md`:

- 顶部「最新更新」改为 `2026-07-21 · 配套版本 KB-AI **v1.3.0**`
- §4 5 项运维短板:
  - #1 无 CI / linter 自动门禁 → ✅ v1.3.0 关闭(pre-commit + pre-push 双 hook)
  - #2 无月度账单告警 → ✅ v1.3.0 关闭(cost-alert.ps1)
  - #3 Dockerfile 多步构建未优化 → ❌ **明确推迟** → 移到 §"未来项 / backlog"或保留此处标"推迟到 v1.3.1+,见 §6"
  - #4 QUICKSTART.md 部署边界章节缺失 → ✅ v1.3.0 关闭(QUICKSTART §9)
  - #5 COS / 异地备份未实现 → ❌ **明确推迟**(用户决策,保留)
- §5 4 项文档缺口:
  - #1 PRD 三处偏离 → ✅ v1.0.1 + v1.3.0 关闭(全面同步)
  - #2 架构评审 Part 2 §9 → ❌ **明确推迟**(3 项 🟢 TODO 已确认推迟)
  - #3 QUICKSTART.md 部署边界 → ✅ v1.3.0 关闭(QUICKSTART §9,与 §4 #4 合并)
  - #4 `dify/knowledge-pipeline.json` 评估删除 → ✅ v1.3.0 关闭(保留 + 加 README 说明)
- §6 未来项追加 backlog:
  - 「v1.3.0 关闭后 backlog:known-limitations §4 #3 Dockerfile 多步构建 / §4 #5 COS 异地备份 / §5 #2 架构评审 Part 2 §9,优先级待用户授权」

### 4.8 acceptance-checklist.md

- [ ] **Step 13: acceptance-checklist.md 顶部日期 + 已知遗留更新**

打开 `E:/docs/acceptance-checklist.md`:

- 顶部「最近更新」改为 `2026-07-21 · 配套版本 KB-AI **v1.3.0**`
- 「## 已知遗留(进入 v1.1.0 GA 的明确 TODO)」段 → 改名为「## 已知遗留(进入 v1.3.0 GA 的明确 TODO)」
- 移除已关闭项(v1.3.0 完成的)
- 保留未关闭项(Dockerfile / COS / P1/P2 未来项)

### 4.9 degradation-guide.md

- [ ] **Step 14: degradation-guide.md 加 cost-alert 场景节**

打开 `E:/docs/degradation-guide.md`,在末尾追加:

```markdown
## §X · cost-alert 阻断时知识库检索仍可用(v1.3.0 新增)

**触发条件**:本月用量 ≥ `.env` 中 `COST_ALERT_THRESHOLDS=block`(默认 ¥1500)。

**现象**:
- `/api/chat` 返回 HTTP 503 + `{reason: "monthly_cost_exceeded", month_yuan, threshold, hint}`
- Dashboard 「本月用量」卡片 level=3 红色满格 + 顶部红色告警 banner
- degradation_events 表写入 `component='LLM'` + `reason='cost-alert block: ¥<amount>'`

**降级路径**:
1. **知识库检索仍可用**:用户可在 Dashboard / KnowledgePage 翻历史 / 浏览文档
2. **可继续追问 websearch**:`POST /api/chat -SkipWebSearch false`(默认)但因 LLM 阻断仍 503;但若用户在 UI 切到"仅检索模式"(若前端支持),检索链路照常
3. **手动调整阈值**:改 `.env` 中 `COST_ALERT_THRESHOLDS` 重新调低 block 阈值,然后跑 `pwsh -File scripts/cost-alert.ps1` 重新 rollup

**恢复**:
- 下月 1 日 UTC 00:00 自动重置(`cost-alert.ps1` 按 UTC 自然月切月,新月份 month_yuan 重算)
- 或手动 `pwsh -File scripts/cost-alert.ps1 -Month "2026-08"`(强制指定月份)
- 或调低 `.env` 阈值后手动 rollup

**诊断**:
```bash
# 看 health_status.json 当前 cost_alert 字段
cat data/health_status.json | python -c "import json,sys; d=json.load(sys.stdin); print(json.dumps(d.get('cost_alert',{}), ensure_ascii=False, indent=2))"

# 看本月 token 用量
wc -l data/cost_log.jsonl  # 总行数 ≈ 总调用次数

# 强制重新 rollup
pwsh -File scripts/cost-alert.ps1
```

**预防**:
- `level=1`(month_yuan ≥ warn)→ Dashboard 黄色提示
- `level=2`(≥ high)→ Dashboard 橙色 banner
- `level=3`(≥ block)→ 阻断 LLM
- 建议:level=2 时联系 maintainer 评估是否调高阈值或暂停使用
```

### 4.10 PRD v0.7 全面同步(项目外,需用户授权)

- [ ] **Step 15: 显式确认用户授权**

```bash
# 此步骤需用户单独授权"修改 <private>/.harness/intake/custom-kb-qa-ai-prd-draft.md"
# 授权前不执行。
```

**等待用户回复"授权修改 PRD"** 后再继续 Step 16-17。

- [ ] **Step 16: PRD 头部 + §0 + §1 + §3 + §4 修订(若 Step 15 授权)**

打开 `<private>/.harness/intake/custom-kb-qa-ai-prd-draft.md`:

1. 头部版本号 `v0.7(锁版)` / 2026-07-01 → `v1.3.0 同步版` / 2026-07-21
2. §0 模型:`Qwen3-Plus` → `qwen3.6-plus`、`Qwen3.7 Max` → `qwen3.7-max`(加注释:与 PRD v0.7 命名差异)
3. §0 Embedding:`Qwen3-Embedding` → `实为 text-embedding-v3(命名差异,模型一致)`
4. §1 REQ-3 验收追加 `POST /api/eval/run 黄金集通过率 ≥ 90%(实测 50/50)`
5. §1 REQ-6 验收改 `1-100 轮可配,默认 50`
6. §3.3 应用栈改 `FastAPI 0.111 后端(主入口) + React 18 前端(v0.8.2 起替代 Dify Web UI) + Dify 1.0 SQLite 模式(保留 dify-api / dify-worker 容器)`;容器清单改为 4 容器 = 3 长跑(dify-api / dify-worker / qdrant) + 1 WAL init(AGENTS.md §5 #2 已知偏差)
7. §3.7 启动步骤:Windows-Start.bat → start.bat;localhost:8080 → localhost:8000;删除"Dify 初始化 3 分钟"
8. §4 验收清单追加:eval/run ≥ 90% / Dashboard 4-5 卡片 / history_limit 1-100 可配

- [ ] **Step 17: PRD §5 历史追加四张变更表**

在 PRD §5 末尾追加:

```markdown
### v0.7 → v1.0.1(2026-07-20 · 文档同步)

| 项 | v0.7 | v1.0.1 | 来源 |
|---|---|---|---|
| 部署形态 | 腾讯云 CVM | **1TB USB SSD + Windows 单机** | 用户决策 v0.7.1 |
| 设计风格 | 浅色商务风 + PingFang | **XAIAgent 暗黑赛博风** | 用户决策 v0.8.1 |
| 自动备份 | 每日 03:00 COS + AES-256 | **stop.bat 触发本地 7 份轮转** | 用户决策 v0.8.6 |

### v1.0.1 → v1.1.0(2026-07-21 · 4 PR 收官)

| PR | 主题 | 累计测数 |
|---|---|---|
| #1 data-integrity | 数据库删除级联 409 + bulk_assign Qdrant payload 重写 | pytest 167 → 184(+17) |
| #2 limit-guard | 单图 20MB / 多图 5 张 / 单会话 50 轮软上限 | 192 → 197(+5) |
| #3 ui-feedback | 图片缩略图 + 多选题 UI + 跳过反问按钮 | 197 → 205(+8) |
| #4 tag-and-title | 自由标签 tags/doc_tags + 历史标题 LLM 生成 | 205 → 223(+18) |

### v1.1.0 → v1.2.0(2026-07-21 · RAG 质量 4 PR 收官)

| PR | 主题 | 累计测数 |
|---|---|---|
| #1 query-profile-and-eval | QueryProfile + retrieval diagnostics + eval 分类 | 223 → 235(+12) |
| #2 xlsx-structuring-and-reparse | 表格行级结构化 + reparse task | 235 → 244(+9) |
| #3 retrieval-quality-gates | 短问题门控 + 年份优先 | 244 → 251(+7) |
| #4 long-query-optimization-and-golden-set | 长问题 reranker cap + golden set 50 条 | 251 → 252(+1,集成) |

### v1.2.0 → v1.3.0(2026-07-21 · 运维加固与文档同步)

| 项 | 内容 | 来源 |
|---|---|---|
| 月度配额告警 | cost-alert.ps1 + llm.py/chat.py/dashboard.py 联动 | 本 spec §2.4 |
| pre-commit/pre-push | ruff + run-checks.ps1 门禁 | 本 spec §2.6 |
| 文档收口 | PRD / AGENTS / CHANGELOG / known-limitations / acceptance / degradation / QUICKSTART / README 全量同步 | 本 spec §2.1-2.3, §4 |
| 累计测数 | pytest 252 → 261(+9);vitest 10 → 12(+2) | — |
```

### 4.11 最终验证

- [ ] **Step 18: 跑全检**

```bash
cd E:/
pwsh -File scripts/run-checks.ps1
```

预期:4/4 PASS

- [ ] **Step 19: 验证 PRD 修订一致性(若 Step 15 授权)**

打开 PRD,grep 验证关键改动落地:

```bash
grep "v1.3.0 同步版" <private>/.harness/intake/custom-kb-qa-ai-prd-draft.md
grep "qwen3.6-plus" <private>/.harness/intake/custom-kb-qa-ai-prd-draft.md
grep "FastAPI 0.111" <private>/.harness/intake/custom-kb-qa-ai-prd-draft.md
```

预期:全部命中

- [ ] **Step 20: 验证文档交叉引用**

```bash
cd E:/
grep -c "v1.3.0" CHANGELOG.md AGENTS.md README.md docs/known-limitations.md docs/acceptance-checklist.md QUICKSTART.md docs/degradation-guide.md 2>&1 | head -10
```

预期:每文件至少 1 次

- [ ] **Step 21: Commit + Push(需用户单独授权)**

```bash
cd E:/
git add version .gitignore CHANGELOG.md AGENTS.md README.md QUICKSTART.md dify/README.md
git add docs/known-limitations.md docs/acceptance-checklist.md docs/degradation-guide.md
git commit -m "release(v1.3.0): 运维加固与文档同步

- version 1.2.0 → 1.3.0
- .gitignore: 加 data/cost_log.jsonl
- CHANGELOG.md: 顶部追加 [1.3.0] 段
- AGENTS.md: §13 同步 v1.2.0 + v1.3.0 节点;§8.5 加代码门禁段
- README.md: 版本号行 (v0.8.6) → (v1.3.0);新增开发命令段
- QUICKSTART.md: 插入 §9 系统边界与限制
- dify/README.md: 新建,说明 knowledge-pipeline.json 是历史快照
- docs/known-limitations.md: §4 / §5 关闭已实现项 + 推迟项保留
- docs/acceptance-checklist.md: 顶部日期 2026-07-21 + 已知遗留更新
- docs/degradation-guide.md: 加 cost-alert 阻断场景节

Refs: docs/superpowers/specs/2026-07-21-v1.3-ops-hardening-design.md"
git push origin master  # 需用户单独授权

# 单独提交 PRD(项目外,可能需要单独 commit)
# 若授权改 PRD,git 在项目内不能跟踪;跳过 git commit,直接人工同步
```

---

## Self-Review(由作者完成)

### 1. Spec coverage

| Spec 节 | 覆盖 task |
|---|---|
| §0 基本信息 | Task 4(README/CHANGELOG/version)|
| §1.1 6 item | Task 1 / Task 2 / Task 3 / Task 4 各覆盖 |
| §1.2 9 连带 | Task 2(dashboard.py)/ Task 3(DashboardPage/types/vitest)/ Task 4(README/known-limitations/acceptance/degradation/CHANGELOG/version/.gitignore)|
| §1.3 不变量 | 整个 plan 不触碰 `.env` / `package.bat` / `start.bat` / `stop.bat` / 容器镜像 tag / arch Part 1 |
| §1.4 成功标准 | Task 4 Step 18 / Step 20 验证 |
| §2.1 AGENTS.md §13 | Task 4 Step 5-7 |
| §2.2 PRD | Task 4 Step 15-17(需用户授权) |
| §2.3 QUICKSTART §9 | Task 4 Step 8 |
| §2.4 cost-alert | Task 2 全节 |
| §2.5 dify/README | Task 4 Step 9 |
| §2.6 hooks | Task 1 全节 |
| §3.1 数据流 | Task 2 实现 |
| §3.2 hook 触发链 | Task 1 Step 2 实现 |
| §3.3 跨 Item 依赖 | Task 4 各 Step |
| §3.4 不变量边界 | 整个 plan 遵守 |
| §4.1 测试策略 | Task 2 Step 32 / Task 3 Step 7-8 验证 |
| §4.2 验证清单 | Task 4 Step 18-20 验证 |
| §4.3 风险与缓解 | Task 1-3 各 Step 缓解 |
| §4.4 不在范围 | 全 plan 不做 Dockerfile / CI / P1-P2 |

### 2. 占位扫描

- 无 "TBD" / "TODO" / "FIXME" / "implement later"
- 无 "add appropriate error handling" 等模糊指令(都有具体代码)
- 无 "Similar to Task N" 引用(每个 task 都有完整代码)

### 3. 类型一致性

| 接口 | 定义位置 | 使用位置 |
|---|---|---|
| `_post_chat() -> Tuple[str, Optional[Dict[str, int]]]` | Task 2 Step 3 | Task 2 Step 13-14(chat_with_fallback 调用)|
| `_post_chat_stream() -> Iterator[Dict[str, Any]]` | Task 2 Step 17 | Task 2 Step 18(chat_stream_with_fallback 调用,对外 yield str)|
| `_log_token_usage(model, input_tokens, output_tokens)` | Task 2 Step 7 | Task 2 Step 12, 18(两处调用) |
| `cost_alert.level >= 3` 判定 | Task 2 Step 27 `_read_cost_alert` | Task 2 Step 23 chat.py early-return |
| `CostAlert` 接口 | Task 3 Step 1 types.ts | Task 3 Step 4 DashboardPage.tsx |
| `thresholds.{warn,high,block}` | Task 2 Step 29 cost-alert.ps1 | Task 3 Step 4 DashboardPage.tsx 渲染 |
| `data/cost_log.jsonl` 行格式 `{ts, model, in, out}` | Task 2 Step 7 _log_token_usage | Task 2 Step 29 cost-alert.ps1 解析 |
| `data/health_status.json` `cost_alert.level/today_yuan/month_yuan/month/thresholds/updated_at` | Task 2 Step 29 cost-alert.ps1 | Task 2 Step 27 _read_cost_alert / Task 3 Step 1 types.ts |

类型一致,接口名称匹配。
