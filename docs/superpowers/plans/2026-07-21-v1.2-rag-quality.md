# v1.2 RAG 质量提升 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不改变聊天 SSE、Embedding/Qdrant contract 和前端契约的前提下，收口 xlsx 召回、极短问题噪声、长问题检索延迟和同文档跨年份混淆四类 RAG 短板。

**Architecture:** 先建立纯本地 `QueryProfile` 和可重复的检索评测/耗时基线，再把 xlsx 转成带工作表、列名、行值和年份元数据的 chunk。检索层根据 query profile 做短问题门控、明确年份优先和长问题 reranker 自适应；旧 xlsx 通过一个显式、路径受限的 reparse task 重新走完整入库链路，现有 reembed 语义保持不变。

**Tech Stack:** Python 3.12、FastAPI、pytest、SQLite、Qdrant 1.7 REST、openpyxl（现有可选依赖）、PowerShell 5.1、现有 `text-embedding-v3` 和 cross-encoder。

## Global Constraints

- PowerShell 脚本必须兼容 Windows PowerShell 5.1，不使用 `??` 等 PowerShell 6+ 语法。
- 不修改 `.env`、`package.bat`、`start.bat`、`stop.bat`、受保护的架构报告 Part 1 或容器镜像 tag。
- 不新增第三方依赖，不替换 Embedding 模型、Qdrant collection、向量维度或距离。
- 不自动启动全量 reparse；旧文档只通过用户显式执行 `scripts/reparse-rag.ps1` 重建。
- 普通 Markdown/TXT/DOCX 的 chunk 行为保持不变；聊天 SSE、citations 和前端上传契约保持不变。
- 新 diagnostics 不记录 API key、绝对路径、完整外部错误响应或完整用户问题。
- 每个任务先写失败测试，再写最小实现，再运行任务级测试；提交动作必须等用户逐次授权，不能自行执行 `git commit`。
- 每个阶段完成后运行 `backend/.venv/Scripts/python -m pytest tests/unit/ -q`；最终还要运行 `scripts/run-checks.ps1` 和 `docker compose config`，不运行 `docker compose up`。

## File Map

### 新建文件

- `backend/core/rag/query_profile.py`：纯本地 query profile、显式年份提取和 reranker query 压缩。
- `tests/unit/test_query_profile.py`：query profile 的边界、年份和压缩测试。
- `tests/unit/test_xlsx_structuring.py`：openpyxl 假对象驱动的 xlsx 结构化测试。
- `tests/unit/test_retrieval_quality.py`：短问题门控、年份优先和长问题 rerank 策略测试。
- `tests/unit/test_reparse_task.py`：reparse 路径校验、前置失败保留旧索引和成功替换测试。
- `scripts/reparse-rag.ps1`：读取显式 JSON manifest，调用本地 reparse route，轮询 task 状态。

### 修改文件

- `backend/core/rag/mineru.py`：增加 xlsx 工作表/行记录格式化，保留 `parse_to_markdown` 返回契约。
- `backend/core/rag/metadata.py`：增加四位年份提取纯函数。
- `backend/core/rag/chunker.py`：扩展 Chunk metadata，识别 xlsx row group，并保持普通文档兼容。
- `backend/core/rag/retriever.py`：接入 query profile、短问题策略、年份优先、长 query 和阶段计时。
- `backend/core/rag/qdrant_store.py`：增加按 point IDs 删除的最小公开包装。
- `backend/core/rag/keyword_index.py`：复用现有 schema，补充目标 source 的一致性辅助查询（如实现需要）。
- `backend/core/sqlite.py`：把 `processing_state.operation` 文档和测试覆盖扩展到 `reparse`。
- `backend/api/knowledge.py`：payload 新字段和本地 `POST /api/knowledge/documents/{source}/reparse` task。
- `backend/api/debug.py`：更新 diagnostics 字段说明，不移除旧字段。
- `tests/unit/test_chunking_atomic.py`：扩展 xlsx chunk 和 metadata 默认值测试。
- `tests/unit/test_knowledge_metadata.py`：扩展 Qdrant payload 字段测试。
- `tests/unit/test_retriever_fallback.py`：保留并扩展 fallback 不回归测试。
- `tests/unit/test_qdrant_public_api.py`：增加 `delete_by_ids` HTTP contract 测试。
- `tests/unit/test_eval_route.py`：覆盖分类字段、期望年份/chunk 类型和延迟统计。
- `tests/eval/run_eval.py`：扩展黄金集字段、分类统计、p95 统计，保持现有 `run_case` 兼容包装。
- `tests/eval/README.md`：记录 v1.2 数据集字段、warm-up 和长问题 p95 跑法。
- `tests/eval/golden-qa.example.jsonl`：增加四类字段的最小格式示例，不伪造真实知识库 source。
- `docs/known-limitations.md`：发布完成后把四条已修短板从“已观察，接受”迁入 CHANGELOG 对应版本；实现阶段先不提前宣称已修复。

---

## PR1：评测基线与 Query Profile

### Task 1：为 QueryProfile 写失败测试

**Files:**
- Create: `tests/unit/test_query_profile.py`
- Create: `backend/core/rag/query_profile.py`（先保持不存在，验证测试失败）

**Interfaces:**
- Produces `QueryProfile`：不可变 dataclass，字段为 `char_count: int`、`meaningful_token_count: int`、`explicit_years: tuple[int, ...]`、`is_short: bool`、`is_long: bool`。
- Produces `build_query_profile(query: str) -> QueryProfile`。
- Produces `extract_explicit_years(text: str) -> tuple[int, ...]`。
- Produces `compress_for_rerank(query: str, *, max_chars: int = 512) -> str`。

- [ ] **Step 1: 写失败测试**

```python
from backend.core.rag.query_profile import (
    build_query_profile,
    compress_for_rerank,
    extract_explicit_years,
)


def test_profile_counts_non_whitespace_and_effective_tokens():
    profile = build_query_profile("  2026 年 门店进度  ")

    assert profile.char_count == len("2026年门店进度")
    assert profile.meaningful_token_count >= 2
    assert profile.explicit_years == (2026,)
    assert profile.is_short is False
    assert profile.is_long is False


def test_profile_short_boundary_uses_or_rule():
    assert build_query_profile("进度").is_short is True
    assert build_query_profile("一二三四").is_short is True
    assert build_query_profile("一二三四五").is_short is False


def test_profile_long_boundary_is_over_two_hundred_non_whitespace_chars():
    assert build_query_profile("甲" * 200).is_long is False
    assert build_query_profile("甲" * 201).is_long is True


def test_extract_years_is_unique_sorted_and_rejects_longer_numbers():
    assert extract_explicit_years("预算 2026 和 2025") == (2025, 2026)
    assert extract_explicit_years("编号 12026 不算年份") == ()
    assert extract_explicit_years("没有年份") == ()


def test_compress_for_rerank_preserves_edges_and_explicit_year():
    query = "2026 年" + "前半段" * 120 + "结尾要求"
    compressed = compress_for_rerank(query, max_chars=80)

    assert len(compressed) <= 80
    assert "2026" in compressed
    assert compressed.startswith("年份:")
    assert "结尾要求" in compressed
```

- [ ] **Step 2: 运行失败测试**

Run:

```bash
backend/.venv/Scripts/python -m pytest tests/unit/test_query_profile.py -q
```

Expected: FAIL，因为 `backend.core.rag.query_profile` 尚不存在。

- [ ] **Step 3: 实现最小纯函数模块**

Create `backend/core/rag/query_profile.py`，使用现有 tokenizer，不调用网络：

```python
from __future__ import annotations

import re
from dataclasses import dataclass

from .tokenizer import get_top_tokens

_YEAR_RE = re.compile(r"(?<!\d)(20\d{2})(?!\d)")


@dataclass(frozen=True)
class QueryProfile:
    char_count: int
    meaningful_token_count: int
    explicit_years: tuple[int, ...]
    is_short: bool
    is_long: bool


def extract_explicit_years(text: str) -> tuple[int, ...]:
    years = {int(value) for value in _YEAR_RE.findall(text or "")}
    return tuple(sorted(years))


def build_query_profile(query: str) -> QueryProfile:
    value = query or ""
    compact = re.sub(r"\s+", "", value)
    token_count = len(get_top_tokens(value, max_n=20))
    return QueryProfile(
        char_count=len(compact),
        meaningful_token_count=token_count,
        explicit_years=extract_explicit_years(value),
        is_short=len(compact) <= 4 or token_count <= 2,
        is_long=len(compact) > 200,
    )


def compress_for_rerank(query: str, *, max_chars: int = 512) -> str:
    value = query or ""
    if max_chars <= 0:
        return ""
    if len(value) <= max_chars:
        return value

    years = extract_explicit_years(value)
    marker = f"年份:{','.join(str(year) for year in years)}\n" if years else ""
    body_budget = max_chars - len(marker)
    if body_budget <= 0:
        return marker[:max_chars]
    head_len = body_budget // 2
    tail_len = body_budget - head_len
    return marker + value[:head_len] + value[-tail_len:]
```

- [ ] **Step 4: 运行任务测试**

Run:

```bash
backend/.venv/Scripts/python -m pytest tests/unit/test_query_profile.py -q
```

Expected: 全部 PASS。

- [ ] **Step 5: 提交检查点**

先运行 `git diff --check`。提交需要用户本次明确授权；获得授权后再使用：

```bash
git add backend/core/rag/query_profile.py tests/unit/test_query_profile.py
git commit -m "feat(rag): add deterministic query profile"
```

### Task 2：增加检索 diagnostics 基线而不改变排序

**Files:**
- Create: `tests/unit/test_retrieval_diagnostics.py`
- Modify: `backend/core/rag/retriever.py:1-724`
- Modify: `backend/api/debug.py:17-49`
- Test: `tests/unit/test_retriever_fallback.py`

**Interfaces:**
- `retrieve(..., diagnostics: Optional[Dict[str, Any]] = None)` 原签名继续有效。
- `retrieve_debug(...)` 返回旧字段，并在返回体新增 `diagnostics.query_profile`、`diagnostics.embed_ms`、`diagnostics.rerank_ms`、`diagnostics.retrieve_ms`。
- 计时使用 `time.perf_counter()`，数值为非负、四舍五入到毫秒。

- [ ] **Step 1: 写失败测试**

```python
from backend.core.rag import retriever


def test_retrieve_debug_reports_profile_and_non_negative_timings(monkeypatch):
    monkeypatch.setattr(retriever, "rewrite_query", lambda query, **kwargs: (query, {}))
    monkeypatch.setattr(
        retriever,
        "embed_query",
        lambda query, api_key=None: [0.0] * 1024,
    )
    monkeypatch.setattr(
        retriever,
        "search",
        lambda vector, *, name, limit, url=None, with_payload=True: [
            {"id": "v1", "score": 0.8, "payload": {"source": "a.md", "text": "2026 进度"}}
        ],
    )
    monkeypatch.setattr(retriever, "search_by_tokens", lambda tokens, *, limit, db_path=None: [])

    result = retriever.retrieve_debug("2026 进度", rerank_top_n=0)
    diagnostics = result["diagnostics"]

    assert diagnostics["query_profile"]["explicit_years"] == [2026]
    assert diagnostics["embed_ms"] >= 0
    assert diagnostics["rerank_ms"] >= 0
    assert diagnostics["retrieve_ms"] >= 0
    assert "query" not in diagnostics
```

- [ ] **Step 2: 运行失败测试**

Run:

```bash
backend/.venv/Scripts/python -m pytest tests/unit/test_retrieval_diagnostics.py -q
```

Expected: FAIL，因为 diagnostics 尚无 query profile 和计时字段。

- [ ] **Step 3: 接入 profile 和阶段计时**

在 `retriever.py` 导入 `time` 和 `build_query_profile`，在 `retrieve()` 与 `retrieve_debug()` 入口分别创建 profile。使用统一小辅助，避免把完整 query 放入日志：

```python
from time import perf_counter

from .query_profile import build_query_profile


def _elapsed_ms(started: float) -> float:
    return round((perf_counter() - started) * 1000, 2)


def _profile_dict(profile) -> dict[str, object]:
    return {
        "char_count": profile.char_count,
        "meaningful_token_count": profile.meaningful_token_count,
        "explicit_years": list(profile.explicit_years),
        "is_short": profile.is_short,
        "is_long": profile.is_long,
    }
```

在每次 embedding/vector leg、rerank 和完整 retrieve 流程周围写入 diagnostics；已有 `_set_diagnostics()` 的 retrieval mode/degradations 字段不能删除。将 fallback logger 从完整 `%r` query 改为 profile 和结果数量，例如：

```python
logger.warning(
    "Retrieval fallback triggered: results=%d profile=%s",
    len(results),
    _profile_dict(profile),
)
```

- [ ] **Step 4: 运行回归测试**

Run:

```bash
backend/.venv/Scripts/python -m pytest tests/unit/test_retrieval_diagnostics.py tests/unit/test_retriever_fallback.py tests/unit/test_debug_api.py -q
```

Expected: PASS，原有 fallback 和 debug 字段不回归。

- [ ] **Step 5: 提交检查点**

提交需要用户授权；授权后：

```bash
git add backend/core/rag/retriever.py backend/api/debug.py tests/unit/test_retrieval_diagnostics.py tests/unit/test_retriever_fallback.py
 git commit -m "feat(rag): expose retrieval diagnostics baseline"
```

### Task 3：扩展黄金集执行器和分类/p95 统计

**Files:**
- Modify: `tests/eval/run_eval.py:35-205`
- Modify: `tests/unit/test_eval_route.py:17-140`
- Modify: `tests/eval/README.md:1-42`
- Modify: `tests/eval/golden-qa.example.jsonl:1-3`

**Interfaces:**
- 兼容 `run_case(base_url, case, top_k, no_rerank) -> tuple[bool, str]`。
- 新增内部 `run_case_detailed(...) -> tuple[bool, str, dict[str, Any]]`，第三项至少含 `elapsed_ms`、`category`。
- `run_evaluation()` 保留 `total/passed/failed/pass_rate/results`，新增 `category_stats` 和 `latency_ms`。
- 可选 case 字段：`category`、`expect_year`、`expect_chunk_type`、`max_retrieval_ms`。

- [ ] **Step 1: 写失败测试**

在 `test_eval_route.py` 增加：

```python
def test_run_evaluation_reports_category_and_latency_stats(tmp_path):
    from tests.eval.run_eval import run_evaluation

    dataset = tmp_path / "v12.jsonl"
    dataset.write_text(
        "\n".join(
            [
                json.dumps({"question": "短", "expect_source": "a.md", "category": "short"}),
                json.dumps({"question": "2026 进度", "expect_source": "a.md", "category": "year", "expect_year": 2026}),
            ]
        )
        + "\n",
        encoding="utf-8",
    )

    def fake_urlopen(url, timeout):
        body = json.dumps(
            {
                "reranked_hits": [
                    {"source": "a.md", "text": "2026 进度", "year_mentions": [2026]}
                ]
            }
        ).encode()
        response = MagicMock()
        response.read.return_value = body
        response.__enter__ = lambda value: value
        response.__exit__ = lambda value, *args: None
        return response

    with patch("urllib.request.urlopen", side_effect=fake_urlopen):
        result = run_evaluation(dataset_path=str(dataset))

    assert result["passed"] == 2
    assert result["category_stats"]["short"]["passed"] == 1
    assert result["category_stats"]["year"]["passed"] == 1
    assert result["latency_ms"]["count"] == 0
```

- [ ] **Step 2: 运行失败测试**

Run:

```bash
backend/.venv/Scripts/python -m pytest tests/unit/test_eval_route.py::test_run_evaluation_reports_category_and_latency_stats -q
```

Expected: FAIL，因为执行器尚未返回分类统计。

- [ ] **Step 3: 实现兼容包装和 p95**

保留现有 `run_case`，把 HTTP 请求、校验和计时放到以下详细函数；`_run_case_http_and_validate` 是本任务新增的内部函数，不是外部依赖：

```python
def _run_case_http_and_validate(base_url: str, case: dict, top_k: int, no_rerank: bool):
    params = {"question": case["question"], "top_k": top_k}
    if no_rerank:
        params["rerank_top_n"] = 0
    url = f"{base_url}/api/debug/retrieval?{urllib.parse.urlencode(params)}"
    try:
        with urllib.request.urlopen(url, timeout=240) as response:
            data = json.loads(response.read().decode("utf-8"))
    except TimeoutError:
        return False, "请求超时(240s)", {}
    except urllib.error.HTTPError as exc:
        detail = exc.read().decode("utf-8", "ignore")[:200]
        return False, f"HTTP {exc.code}: {detail}", {}

    hits = data.get("reranked_hits") or data.get("rrf_hits") or []
    hits_json = json.dumps(hits, ensure_ascii=False)
    matching = [
        hit for hit in hits
        if case["expect_source"] in json.dumps(hit, ensure_ascii=False)
    ]
    failures = []
    if not hits:
        failures.append("召回为空")
    if not matching:
        failures.append(f"未召回期望文档 '{case['expect_source']}'")
    for keyword in case.get("expect_keywords", []):
        if keyword not in hits_json:
            failures.append(f"召回文本缺少关键词 '{keyword}'")
    if "expect_year" in case and not any(
        int(case["expect_year"]) in [int(value) for value in hit.get("year_mentions") or []]
        for hit in matching
    ):
        failures.append(f"期望年份未命中: {case['expect_year']}")
    if "expect_chunk_type" in case and not any(
        hit.get("chunk_type") == case["expect_chunk_type"] for hit in matching
    ):
        failures.append(f"chunk_type 未命中: {case['expect_chunk_type']}")
    return not failures, "; ".join(failures), data


def _p95(values: list[float]) -> float | None:
    if not values:
        return None
    ordered = sorted(values)
    index = max(0, math.ceil(len(ordered) * 0.95) - 1)
    return round(ordered[index], 2)


def run_case_detailed(base_url: str, case: dict, top_k: int, no_rerank: bool = False):
    started = time.perf_counter()
    ok, detail, data = _run_case_http_and_validate(base_url, case, top_k, no_rerank)
    elapsed_ms = round((time.perf_counter() - started) * 1000, 2)
    limit = case.get("max_retrieval_ms")
    if limit is not None and elapsed_ms > float(limit):
        ok = False
        detail = "; ".join(part for part in (detail, f"检索耗时 {elapsed_ms}ms 超过 {limit}ms") if part)
    return ok, detail, {
        "elapsed_ms": elapsed_ms,
        "category": case.get("category", "uncategorized"),
        "data": data,
    }


def run_case(base_url: str, case: dict, top_k: int, no_rerank: bool = False):
    ok, detail, _meta = run_case_detailed(base_url, case, top_k, no_rerank)
    return ok, detail
```

`_run_case_http_and_validate` 必须继续检查 `expect_source` 和所有 `expect_keywords`；有 `expect_year` 时检查命中的 hit 的 `year_mentions`；有 `expect_chunk_type` 时检查命中 hit 的 `chunk_type`。`run_evaluation` 只把 `category=long` 的 elapsed 值放入 p95 汇总，结果结构如下：

```python
"category_stats": {
    "short": {"total": 10, "passed": 9, "failed": 1, "pass_rate": 0.9}
},
"latency_ms": {"count": 10, "p50": 120.0, "p95": 280.0, "max": 310.0}
```

- [ ] **Step 4: 更新 README 和示例**

在 README 说明四类各至少 10 条、warm-up、`expect_year`、`expect_chunk_type` 和长问题 p95。示例只扩展为真实 schema，不写入不存在的 production source。

- [ ] **Step 5: 运行执行器测试**

Run:

```bash
backend/.venv/Scripts/python -m pytest tests/unit/test_eval_route.py -q
```

Expected: 全部 PASS。

- [ ] **Step 6: 提交 PR1 检查点**

先运行：

```bash
backend/.venv/Scripts/python -m pytest tests/unit/test_query_profile.py tests/unit/test_retrieval_diagnostics.py tests/unit/test_eval_route.py -q
```

提交需要用户授权；授权后：

```bash
git add tests/eval/run_eval.py tests/eval/README.md tests/eval/golden-qa.example.jsonl tests/unit/test_eval_route.py
git commit -m "feat(eval): add v1.2 retrieval quality baseline"
```

---

## PR2：xlsx 结构化、Chunk 元数据与显式 reparse

### Task 4：为 xlsx 行结构化写失败测试

**Files:**
- Create: `tests/unit/test_xlsx_structuring.py`
- Modify: `backend/core/rag/mineru.py:87-117`

**Interfaces:**
- Private helper `format_xlsx_sheet(title: str, rows: list[tuple[object, ...]]) -> str` 可直接单测。
- `_read_xlsx(path: Path) -> str` 继续返回完整 markdown string。
- 结构化文本必须包含 `# 工作表：<title>`、`表头：...` 和 `第 N 行：字段=值`。

- [ ] **Step 1: 写失败测试**

```python
import sys
from types import SimpleNamespace

from backend.core.rag import mineru


class FakeWorksheet:
    title = "门店进度"

    def iter_rows(self, values_only=True):
        assert values_only is True
        return iter(
            [
                ("姓名", "门店", "进度", "日期"),
                ("张三", "一店", "完成", "2026-06-30"),
                (None, None, None, None),
                ("李四", "二店", "待确认", None),
            ]
        )


class FakeWorkbook:
    worksheets = [FakeWorksheet()]


def test_format_xlsx_sheet_emits_header_and_key_value_rows():
    text = mineru.format_xlsx_sheet(
        "门店进度",
        [
            ("姓名", "门店", "进度"),
            ("张三", "一店", "完成"),
        ],
    )

    assert "# 工作表：门店进度" in text
    assert "表头：姓名 | 门店 | 进度" in text
    assert "第 2 行：姓名=张三；门店=一店；进度=完成" in text
    assert "None" not in text


def test_read_xlsx_uses_data_only_read_only(monkeypatch, tmp_path):
    calls = {}

    def load_workbook(path, data_only, read_only):
        calls.update(path=str(path), data_only=data_only, read_only=read_only)
        return FakeWorkbook()

    monkeypatch.setitem(
        sys.modules,
        "openpyxl",
        SimpleNamespace(load_workbook=load_workbook),
    )
    path = tmp_path / "progress.xlsx"
    path.write_bytes(b"xlsx")

    text = mineru._read_xlsx(path)

    assert "工作表：门店进度" in text
    assert calls["data_only"] is True
    assert calls["read_only"] is True
```

- [ ] **Step 2: 运行失败测试**

Run:

```bash
backend/.venv/Scripts/python -m pytest tests/unit/test_xlsx_structuring.py -q
```

Expected: FAIL，因为 `format_xlsx_sheet` 尚不存在。

- [ ] **Step 3: 实现结构化 formatter**

在 `mineru.py` 增加以下无网络 helper；保留 `_read_xlsx` 的 openpyxl 加载模式：

```python
def _display_cell(value: object) -> str:
    if value is None:
        return ""
    return str(value).strip()


def _column_label(index: int) -> str:
    value = index + 1
    letters = ""
    while value:
        value, remainder = divmod(value - 1, 26)
        letters = chr(ord("A") + remainder) + letters
    return f"列 {letters}"


def format_xlsx_sheet(title: str, rows: list[tuple[object, ...]]) -> str:
    normalized = [[_display_cell(value) for value in row] for row in rows]
    non_empty = [row for row in normalized if any(row)]
    if not non_empty:
        return ""

    width = max(len(row) for row in non_empty)
    header = non_empty[0] + [""] * (width - len(non_empty[0]))
    headers = [value or _column_label(index) for index, value in enumerate(header)]
    output = [f"# 工作表：{title}", f"表头：{' | '.join(headers)}"]
    original_row = 0
    for raw_index, row in enumerate(normalized, start=1):
        if not any(row) or raw_index == next(
            index for index, candidate in enumerate(normalized, start=1)
            if any(candidate)
        ):
            continue
        cells = row + [""] * (width - len(row))
        fields = [
            f"{headers[index]}={cells[index]}"
            for index in range(width)
            if cells[index]
        ]
        if fields:
            original_row = raw_index
            output.append(f"第 {original_row} 行：{'；'.join(fields)}")
    return "\n\n".join(output)
```

实现时将首个有效行单独保存，避免在循环中重复查找；上面测试要求的外部文本格式必须保持不变。`_read_xlsx` 对每个 worksheet 调 `format_xlsx_sheet`，跳过空结果，用空行连接工作表。

- [ ] **Step 4: 运行 xlsx 测试和现有 MinerU 测试**

Run:

```bash
backend/.venv/Scripts/python -m pytest tests/unit/test_xlsx_structuring.py tests/unit/test_rag_core.py -q
```

Expected: PASS。

### Task 5：扩展 Chunk、年份和 Qdrant payload

**Files:**
- Modify: `backend/core/rag/metadata.py:1-131`
- Modify: `backend/core/rag/chunker.py:59-337,418-533`
- Modify: `backend/api/knowledge.py:155-185,224-241`
- Modify: `tests/unit/test_chunking_atomic.py`
- Modify: `tests/unit/test_knowledge_metadata.py`

**Interfaces:**
- `extract_year_mentions(text: str) -> tuple[int, ...]`：返回唯一升序年份。
- `Chunk` 新字段：`year_mentions: list[int]`、`sheet_name: str`、`row_start: Optional[int]`、`row_end: Optional[int]`、`columns: list[str]`。
- `split_into_chunks(..., document_type: str = "") -> List[Chunk]`；旧调用不传新参数时行为不变。
- `_build_point()` 在 payload 中增加 `year_mentions`、`sheet_name`、`row_start`、`row_end`、`columns`。

- [ ] **Step 1: 写 metadata 和 payload 失败测试**

在 `test_chunking_atomic.py` 增加：

```python
from backend.core.rag.metadata import extract_year_mentions


def test_year_mentions_are_unique_sorted_and_bounded():
    assert extract_year_mentions("2026 预算、2025 预算、2026") == (2025, 2026)
    assert extract_year_mentions("编号 12026") == ()
```

在 `test_knowledge_metadata.py` 增加：

```python
def test_build_point_includes_structured_xlsx_metadata():
    chunk = Chunk(
        id="00000000-0000-0000-0000-000000000003",
        text="# 工作表：门店\n\n第 2 行：年份=2026",
        source="progress.xlsx",
        section=1,
        header_path="工作表：门店",
        header_level=1,
        tokens=["年份"],
        chunk_type="xlsx_row_group",
        year_mentions=[2026],
        sheet_name="门店",
        row_start=2,
        row_end=2,
        columns=["年份"],
    )

    payload = _build_point(chunk, "progress.xlsx", [0.0])["payload"]

    assert payload["chunk_type"] == "xlsx_row_group"
    assert payload["year_mentions"] == [2026]
    assert payload["sheet_name"] == "门店"
    assert payload["row_start"] == 2
    assert payload["row_end"] == 2
    assert payload["columns"] == ["年份"]
```

- [ ] **Step 2: 运行失败测试**

Run:

```bash
backend/.venv/Scripts/python -m pytest tests/unit/test_chunking_atomic.py tests/unit/test_knowledge_metadata.py -q
```

Expected: FAIL，因为新 Chunk 字段和 payload 字段尚不存在。

- [ ] **Step 3: 实现年份和 Chunk metadata**

在 `metadata.py` 使用与 query profile 相同边界：

```python
_YEAR_RE = re.compile(r"(?<!\d)(20\d{2})(?!\d)")


def extract_year_mentions(text: str) -> tuple[int, ...]:
    return tuple(sorted({int(value) for value in _YEAR_RE.findall(text or "")}))
```

在 `Chunk` dataclass 中为新字段提供空/`None`/空列表默认值；在 `_make_chunk()` 中调用 `extract_year_mentions(text)`。对 `document_type == "xlsx"` 的文本 chunk：

- 从 `# 工作表：...` 提取 `sheet_name`；
- 从 `表头：A | B` 提取 `columns`；
- 从 `第 N 行：` 标记提取最小/最大 `row_start`/`row_end`；
- `chunk_type` 统一使用 `xlsx_row_group`，包括只有表头或单行记录的工作表；空工作表不产生 chunk。

给 `split_into_chunks()` 增加末尾默认参数：

```python
def split_into_chunks(
    markdown: str,
    source: str,
    max_len: int = MAX_LEN_DEFAULT,
    overlap: int = OVERLAP_DEFAULT,
    header_aware: bool = True,
    document_date: str = "",
    document_mtime: Optional[float] = None,
    document_type: str = "",
) -> List[Chunk]:
```

在 `knowledge.py` 调用处传入 `document_type=saved_path.suffix.lower().lstrip(".")`；在 `_build_point()` 以 JSON 可序列化的默认值写出新字段：

```python
"year_mentions": list(chunk.year_mentions),
"sheet_name": chunk.sheet_name,
"row_start": chunk.row_start,
"row_end": chunk.row_end,
"columns": list(chunk.columns),
```

更新既有 `test_build_point_keeps_legacy_fields_and_adds_metadata` 的 exact key 集合，明确旧 Chunk 默认新字段为空。

- [ ] **Step 4: 运行 chunk、metadata、知识上传测试**

Run:

```bash
backend/.venv/Scripts/python -m pytest tests/unit/test_chunking_atomic.py tests/unit/test_metadata.py tests/unit/test_knowledge_metadata.py tests/unit/test_rag_core.py -q
```

Expected: 全部 PASS。

- [ ] **Step 5: 提交 PR2 的纯数据结构检查点**

提交需要用户授权；授权后：

```bash
git add backend/core/rag/mineru.py backend/core/rag/metadata.py backend/core/rag/chunker.py backend/api/knowledge.py tests/unit/test_xlsx_structuring.py tests/unit/test_chunking_atomic.py tests/unit/test_knowledge_metadata.py
git commit -m "feat(rag): structure xlsx rows and year metadata"
```

### Task 6：实现显式 reparse task 和 staged source replacement

**Files:**
- Modify: `backend/core/rag/qdrant_store.py:160-200`
- Modify: `backend/core/sqlite.py:96-110`
- Modify: `backend/api/knowledge.py:20-29,103-147,188-365,671-702`
- Create: `backend/api/knowledge.py` route in the existing router
- Create: `tests/unit/test_reparse_task.py`
- Modify: `tests/unit/test_qdrant_public_api.py`
- Create: `scripts/reparse-rag.ps1`

**Interfaces:**
- `delete_by_ids(ids: Sequence[str], *, name: str = DEFAULT_COLLECTION, url: Optional[str] = None) -> Dict[str, Any]`。
- `_prepare_document(saved_path: Path, source: str, database_id: str) -> PreparedDocument`，只在解析、切片、embedding、向量维度校验成功后返回。
- `_replace_source(prepared: PreparedDocument, collection: str) -> Dict[str, Any]`，返回 `old_count`、`new_count`、`deleted_count`、`keyword_rows`。
- `POST /api/knowledge/documents/{source}/reparse?database_id=default&upload_path=<relative>` 返回与普通 task 一致的 `{task_id, source, status, database_id}`。
- `scripts/reparse-rag.ps1 -ManifestPath <json> [-BaseUrl <url>] [-DatabaseId <id>]`。

- [ ] **Step 1: 写 Qdrant contract 失败测试**

在 `test_qdrant_public_api.py` 增加：

```python
def test_delete_by_ids_posts_point_ids():
    with patch.object(rag_qdrant, "_request", return_value={"result": {"status": "acknowledged"}}) as request:
        result = rag_qdrant.delete_by_ids(["p1", "p2"], name="kb_ai_chunks")

    assert result["result"]["status"] == "acknowledged"
    assert request.call_args.args[0] == "POST"
    assert request.call_args.args[1].endswith("/collections/kb_ai_chunks/points/delete")
    assert request.call_args.kwargs["data"] == {"points": ["p1", "p2"]}
```

- [ ] **Step 2: 运行失败测试**

Run:

```bash
backend/.venv/Scripts/python -m pytest tests/unit/test_qdrant_public_api.py::test_delete_by_ids_posts_point_ids -q
```

Expected: FAIL，因为 `delete_by_ids` 尚不存在。

- [ ] **Step 3: 实现 Qdrant 删除包装和准备/替换边界**

`qdrant_store.py` 增加：

```python
def delete_by_ids(
    ids: Sequence[str],
    *,
    name: str = DEFAULT_COLLECTION,
    url: Optional[str] = None,
) -> Dict[str, Any]:
    if not ids:
        return {"result": {"status": "empty"}, "noop": True}
    return _request(
        "POST",
        f"{_base_url(url)}/collections/{name}/points/delete",
        data={"points": [str(value) for value in ids]},
        timeout=60,
    )
```

在 `knowledge.py` 把解析、切片和 embedding 的公共部分收敛到 `_prepare_document()`；返回对象至少包含 `points`、`keyword_rows`、`point_ids` 和 `source`。`_replace_source()` 的顺序固定为：

1. `scroll_by_source()` 读取旧 IDs；
2. 先验证新 chunks 非空、向量数量等于 chunks 数、每个向量维度为 1024、point IDs 唯一；
3. upsert 新 points；
4. `delete_by_ids(old_ids - new_ids)` 清理旧 points；
5. 删除并重建该 source 的 keyword rows；
6. 只有全部步骤成功才把 task 标记 done。

如果第 1-2 步失败，旧 Qdrant 和 keyword index 不动；upsert/清理/SQLite 任一步失败则 task 为 failed，写 `degradation_events(component="Retrieval")` 或相应组件，并保留可重跑状态。不要调用现有 `delete_by_source()` 清掉新旧混合 source。

更新 `processing_state` 的 SQL 注释为 `upload | reembed | reparse`，`upsert_processing()` 和 `finish_processing()` 的状态契约不变。

- [ ] **Step 4: 写 reparse route 失败测试**

`test_reparse_task.py` 使用 conftest.py 提供 `client` 和 `app_router` 夹具，**不**直接构造 `TestClient`：

```python
import pytest
from fastapi.testclient import TestClient

from backend.api import knowledge as knowledge_api
from backend.main import app


@pytest.fixture
def client():
    return TestClient(app)


def test_reparse_rejects_path_traversal(client):
    response = client.post(
        "/api/knowledge/documents/progress.xlsx/reparse",
        params={"database_id": "default", "upload_path": "../db.sqlite"},
    )

    assert response.status_code == 400
    assert "upload" in response.json()["detail"]


def test_reparse_rejects_missing_upload(client):
    response = client.post(
        "/api/knowledge/documents/progress.xlsx/reparse",
        params={"database_id": "default", "upload_path": "missing.xlsx"},
    )

    assert response.status_code == 404


def test_reparse_enqueues_task_for_safe_file(client, monkeypatch, tmp_path):
    upload = tmp_path / "stored_progress.xlsx"
    upload.write_bytes(b"xlsx")
    monkeypatch.setattr(knowledge_api, "_UPLOAD_DIR", tmp_path)
    monkeypatch.setattr(
        knowledge_api,
        "_resolve_database",
        lambda database_id: {"collection": "kb_ai_chunks"},
    )
    captured = []

    def fake_add_task(func, *args, **kwargs):
        captured.append({"func": func.__name__, "args": args, "kwargs": kwargs})

    from fastapi import BackgroundTasks

    monkeypatch.setattr(BackgroundTasks, "add_task", fake_add_task)

    response = client.post(
        "/api/knowledge/documents/progress.xlsx/reparse",
        params={"database_id": "default", "upload_path": upload.name},
    )

    assert response.status_code == 200
    body = response.json()
    assert body["status"] == "pending"
    assert body["source"] == "progress.xlsx"
    assert captured
    assert captured[0]["func"] == "_process_reparse"
```

测试 background task 时使用现有 FastAPI `TestClient` 的 task 执行行为和 monkeypatch，不能触碰真实 `E:/data`、Qdrant 或 Aliyun。

- [ ] **Step 5: 实现受限 route 和 PowerShell wrapper**

Route 必须按以下逻辑校验路径：

```python
upload_root = _UPLOAD_DIR.resolve()
try:
    candidate = (upload_root / upload_path).resolve()
except (OSError, RuntimeError):
    raise HTTPException(status_code=400, detail="upload_path 无效")
if candidate == upload_root or upload_root not in candidate.parents:
    raise HTTPException(status_code=400, detail="upload_path 必须位于 data/uploads 内")
if not candidate.is_file():
    raise HTTPException(status_code=404, detail="upload 文件不存在")
```

route 用 `_resolve_database()` 获取 collection，创建 `_task()`，调用 `_persist_stage(..., "reparse", ...)`，通过 `background_tasks.add_task(_process_reparse, ...)` 入队。`source` 不从 `upload_path` 推导。

`scripts/reparse-rag.ps1` 读取以下 manifest 形状：

```json
[
  {"source":"progress.xlsx","upload_path":"1717000000_0_progress.xlsx"},
  {"source":"budget.xlsx","upload_path":"1717000000_1_budget.xlsx"}
]
```

脚本使用 Windows PowerShell 5.1 可用的 `Get-Content -Raw | ConvertFrom-Json`、`Invoke-WebRequest -Method Post`、`[uri]::EscapeDataString` 和 `Start-Sleep`；每条 task 轮询 `/api/knowledge/tasks/{task_id}`，遇到 `done` 记录 chunk_count，遇到 `failed` 记录 error，最后以失败数量作为退出码。脚本不生成或修改 `.env`，不删除 `data/`。

- [ ] **Step 6: 运行 reparse 测试**

Run:

```bash
backend/.venv/Scripts/python -m pytest tests/unit/test_reparse_task.py tests/unit/test_qdrant_public_api.py tests/unit/test_knowledge_metadata.py -q
powershell -ExecutionPolicy Bypass -File scripts/reparse-rag.ps1 -? 
```

Expected: Python 测试全部 PASS；PowerShell 帮助正常显示，不能启动实际迁移。

- [ ] **Step 7: 提交 PR2 检查点**

提交需要用户授权；授权后：

```bash
git add backend/core/rag/qdrant_store.py backend/core/sqlite.py backend/api/knowledge.py scripts/reparse-rag.ps1 tests/unit/test_reparse_task.py tests/unit/test_qdrant_public_api.py
git commit -m "feat(knowledge): add explicit reparse task"
```

---

## PR3：极短问题门控与年份优先

### Task 7：为短问题策略写失败测试

**Files:**
- Create: `tests/unit/test_retrieval_quality.py`
- Modify: `backend/core/rag/retriever.py:105-116,375-472,490-584`
- Test: `tests/unit/test_retriever_fallback.py`

**Interfaces:**
- `_filter_short_keyword_hits(query: str, hits: Sequence[dict[str, Any]]) -> tuple[list[dict[str, Any]], bool]`。
- 每个 keyword hit 增加内部 `keyword_overlap: int`，不改变旧 `score` 含义。
- `retrieve()` 和 `retrieve_debug()` 共享同一个短问题 helper，不能出现一条路径修复、另一条路径仍旧行为。

- [ ] **Step 1: 写失败测试**

```python
from backend.core.rag.retriever import _filter_short_keyword_hits


def test_short_query_requires_phrase_or_two_token_overlap():
    hits = [
        {"text": "进度已经完成", "source": "a.md", "score": 1},
        {"text": "门店进度表：一店完成", "source": "b.md", "score": 2},
    ]

    filtered, used_fallback = _filter_short_keyword_hits("进度表", hits)

    assert used_fallback is False
    assert [hit["source"] for hit in filtered] == ["b.md"]
    assert filtered[0]["keyword_overlap"] >= 2


def test_short_query_falls_back_when_strict_gate_would_be_empty():
    hits = [{"text": "单字命中", "source": "a.md", "score": 1}]

    filtered, used_fallback = _filter_short_keyword_hits("表", hits)

    assert used_fallback is True
    assert filtered == hits


def test_long_or_normal_query_does_not_use_short_gate():
    hits = [{"text": "进度", "source": "a.md", "score": 1}]

    filtered, used_fallback = _filter_short_keyword_hits("门店进度如何", hits)

    assert used_fallback is False
    assert filtered == hits
```

- [ ] **Step 2: 运行失败测试**

Run:

```bash
backend/.venv/Scripts/python -m pytest tests/unit/test_retrieval_quality.py -q
```

Expected: FAIL，因为短问题 helper 尚不存在。

- [ ] **Step 3: 实现短问题 helper 和融合接入**

使用 `build_query_profile()` 判断 short；用 `get_top_tokens()` 计算 hit 的 distinct token overlap；一个有效 query token 只有完整短语存在时才通过严格门控：

```python
def _filter_short_keyword_hits(query: str, hits: Sequence[Dict[str, Any]]):
    profile = build_query_profile(query)
    if not profile.is_short:
        return [dict(hit) for hit in hits], False

    query_tokens = set(get_top_tokens(query, max_n=20))
    strict = []
    for raw in hits:
        hit = dict(raw)
        text = str(hit.get("text") or "")
        hit_tokens = set(get_top_tokens(text, max_n=20))
        overlap = len(query_tokens & hit_tokens)
        phrase_match = query.strip() in text
        hit["keyword_overlap"] = overlap
        required = 2 if len(query_tokens) >= 2 else 1
        if phrase_match or overlap >= required:
            strict.append(hit)

    if strict:
        return strict, False
    return [dict(hit) for hit in hits], bool(hits)
```

在 `_safe_keyword_leg()` 归一化后调用 helper；`_retrieve_once()` 将 fallback 标记写入 diagnostics。RRF、vector leg、reranker 和已有 retrieval fallback 不删除。严格过滤只影响 keyword leg，避免把 vector-only 的专有名词结果过滤掉。

- [ ] **Step 4: 运行短问题与旧 fallback 测试**

Run:

```bash
backend/.venv/Scripts/python -m pytest tests/unit/test_retrieval_quality.py tests/unit/test_retriever_fallback.py -q
```

Expected: PASS，短问题新增测试和既有 7 个 fallback 场景全部通过。

- [ ] **Step 5: 提交短问题检查点**

提交需要用户授权；授权后：

```bash
git add backend/core/rag/retriever.py tests/unit/test_retrieval_quality.py tests/unit/test_retriever_fallback.py
git commit -m "feat(rag): gate noisy short queries"
```

### Task 8：为年份优先排序写失败测试并实现

**Files:**
- Modify: `tests/unit/test_retrieval_quality.py`
- Modify: `backend/core/rag/retriever.py:327-372,475-584,649-724`
- Modify: `backend/core/rag/metadata.py`

**Interfaces:**
- `_apply_year_priority(results: Sequence[dict[str, Any]], profile: QueryProfile, diagnostics: Optional[dict[str, Any]] = None) -> list[dict[str, Any]]`。
- `year_match` 为 bool；`year_match_miss` 只在 query 有明确年份但候选无命中时写入 diagnostics。
- `retrieve()`、`retrieve_debug()` 都在最终结果路径调用该 helper。

- [ ] **Step 1: 写失败测试**

```python
from backend.core.rag.query_profile import build_query_profile
from backend.core.rag.retriever import _apply_year_priority


def test_explicit_year_moves_matching_chunks_ahead_of_newer_nonmatching_chunk():
    profile = build_query_profile("2025 预算")
    results = [
        {"id": "new", "score": 0.99, "text": "预算 2026", "year_mentions": [2026]},
        {"id": "old", "score": 0.40, "text": "预算 2025", "year_mentions": [2025]},
    ]

    ordered = _apply_year_priority(results, profile, {})

    assert [item["id"] for item in ordered] == ["old", "new"]
    assert ordered[0]["year_match"] is True
    assert ordered[1]["year_match"] is False


def test_year_priority_keeps_results_when_no_candidate_matches():
    profile = build_query_profile("2025 预算")
    diagnostics = {}
    results = [{"id": "new", "text": "预算 2026", "year_mentions": [2026]}]

    ordered = _apply_year_priority(results, profile, diagnostics)

    assert ordered[0]["id"] == "new"
    assert diagnostics["year_match_miss"] is True


def test_multiple_explicit_years_all_count_as_matches():
    profile = build_query_profile("比较 2025 和 2026")
    results = [
        {"id": "2026", "score": 0.2, "year_mentions": [2026]},
        {"id": "2024", "score": 0.9, "year_mentions": [2024]},
        {"id": "2025", "score": 0.1, "year_mentions": [2025]},
    ]

    ordered = _apply_year_priority(results, profile, {})

    assert {ordered[0]["id"], ordered[1]["id"]} == {"2025", "2026"}
```

- [ ] **Step 2: 运行失败测试**

Run:

```bash
backend/.venv/Scripts/python -m pytest tests/unit/test_retrieval_quality.py -q
```

Expected: FAIL，因为年份排序 helper 尚不存在。

- [ ] **Step 3: 实现年份排序**

实现固定为“有任何命中才分组，无命中不清空”：

```python
def _apply_year_priority(results, profile, diagnostics=None):
    diagnostics = diagnostics if diagnostics is not None else {}
    if not profile.explicit_years:
        return [dict(item) for item in results]

    wanted = set(profile.explicit_years)
    marked = []
    for item in results:
        current = dict(item)
        mentions = {int(value) for value in current.get("year_mentions") or []}
        current["year_match"] = bool(wanted & mentions)
        marked.append(current)

    if not any(item["year_match"] for item in marked):
        # v1.2 决策:仅在 diagnostics 记志,不动排序、不写 degradation_events
        diagnostics["year_match_miss"] = True
        return marked
    return [item for item in marked if item["year_match"]] + [
        item for item in marked if not item["year_match"]
    ]
```

在 `_finish_retrieval()` 返回后调用；年份分组内部保留现有 rerank/temporal 顺序。`retrieve_debug()` 的 `rrf_hits`、`reranked_hits` 都要在对应阶段显示 `year_match`，不删除 `vector_hits`/`keyword_hits` 旧字段。旧 payload 缺少 `year_mentions` 时按空列表处理。

- [ ] **Step 4: 运行年份、temporal 和 debug 回归测试**

Run:

```bash
backend/.venv/Scripts/python -m pytest tests/unit/test_retrieval_quality.py tests/unit/test_temporal_weighting.py tests/unit/test_debug_api.py tests/unit/test_retriever_fallback.py -q
```

Expected: PASS。

- [ ] **Step 5: 提交 PR3 检查点**

提交需要用户授权；授权后：

```bash
git add backend/core/rag/metadata.py backend/core/rag/retriever.py tests/unit/test_retrieval_quality.py
git commit -m "feat(rag): prioritize explicit document years"
```

---

## PR4：长问题 reranker 优化、黄金集与发布验证

### Task 9：为长问题 rerank 自适应写失败测试

**Files:**
- Modify: `tests/unit/test_retrieval_quality.py`
- Modify: `backend/core/rag/retriever.py:206-216,340-372,490-584`
- Modify: `backend/core/rag/reranker.py:61-117`（仅在公共参数需要兼容注释时修改）

**Interfaces:**
- `_effective_rerank_top_n(rerank_top_n: Optional[int], profile: QueryProfile) -> int`。
- `_finish_retrieval(..., rerank_query: Optional[str] = None)`，旧调用不传时使用原 query。
- 长 query 默认 rerank top N 为 5；显式 `rerank_top_n=0` 仍完全禁用 rerank；显式正数不能被长问题策略提高，但可被降到 5。

- [ ] **Step 1: 写失败测试**

```python
from backend.core.rag.query_profile import build_query_profile
from backend.core.rag import retriever


def test_long_profile_caps_default_rerank_candidates_at_five():
    profile = build_query_profile("甲" * 201)

    assert retriever._effective_rerank_top_n(None, profile) == 5
    assert retriever._effective_rerank_top_n(10, profile) == 5
    assert retriever._effective_rerank_top_n(3, profile) == 3


def test_explicit_disable_rerank_wins_for_long_query():
    profile = build_query_profile("甲" * 201)

    assert retriever._effective_rerank_top_n(0, profile) == 0


def test_finish_retrieval_passes_compressed_query_to_reranker(monkeypatch):
    captured = {}

    def fake_rerank(query, candidates, *, top_k, model_name=None):
        captured["query"] = query
        return list(candidates)

    monkeypatch.setattr(retriever, "rerank", fake_rerank)
    original = "2026 年" + "背景" * 180 + "最终要求"
    results = retriever._finish_retrieval(
        original,
        [{"id": "1", "text": "命中", "score": 0.8}],
        top_k=5,
        rerank_top_n=1,
        rerank_model=None,
        retrieval_mode="hybrid",
        rerank_query="年份:2026\n压缩后的查询",
    )

    assert results
    assert captured["query"] == "年份:2026\n压缩后的查询"
```

- [ ] **Step 2: 运行失败测试**

Run:

```bash
backend/.venv/Scripts/python -m pytest tests/unit/test_retrieval_quality.py -q
```

Expected: FAIL，因为 `_effective_rerank_top_n` 尚不接受 profile，`_finish_retrieval` 尚不接受 `rerank_query`。

- [ ] **Step 3: 实现长问题自适应**

保持环境变量 `RERANK_TOP_N` 的现有优先级，新增常量：

```python
DEFAULT_LONG_QUERY_RERANK_TOP_N = 5


def _effective_rerank_top_n(
    rerank_top_n: Optional[int],
    profile: QueryProfile,
) -> int:
    configured = _configured_rerank_top_n(rerank_top_n)
    if configured <= 0:
        return configured
    if profile.is_long:
        return min(configured, DEFAULT_LONG_QUERY_RERANK_TOP_N)
    return configured
```

`_finish_retrieval()` 增加 keyword-only `rerank_query`，调用 `rerank(rerank_query or query, ...)`；原 query 仍传给 `_annotate_results()`，保证 token count/confidence 语义不变。`retrieve()` 和 `retrieve_debug()` 使用 `compress_for_rerank(rewritten_query)`，只在 `profile.is_long` 时传压缩值。fallback 第二次检索也必须使用同一压缩 query和候选上限。

不要在 `reranker.py` 内再次截断 query；该模块继续只截断 candidate text 到现有 500 字，并保持模型不可用时原序返回。

- [ ] **Step 4: 运行长问题和 reranker 回归测试**

Run:

```bash
backend/.venv/Scripts/python -m pytest tests/unit/test_retrieval_quality.py tests/unit/test_reranker.py tests/unit/test_retriever_fallback.py -q
```

Expected: PASS。

### Task 10：完成四类黄金集并验证执行器

**Files:**
- Modify: `tests/eval/run_eval.py`
- Modify: `tests/unit/test_eval_route.py`
- Modify: `tests/eval/README.md`
- Modify/Create: `tests/eval/golden-qa.jsonl`（仅使用真实知识库中已验证的非敏感 source）

**Interfaces:**
- 数据集至少 40 条：`xlsx`、`short`、`long`、`year` 各至少 10 条。
- 每条至少有 `question`、`expect_source`；相关类别增加 `expect_keywords`、`expect_year`、`expect_chunk_type`。
- `run_evaluation()` 输出整体 pass rate、category stats、long p95 和每条失败 detail。

- [ ] **Step 1: 从当前知识库建立 source 清单**

在后端和 Qdrant 已由用户正常启动的环境中，只读调用：

```bash
curl "http://127.0.0.1:8000/api/knowledge/documents?database_id=default"
```

将实际存在且允许进入测试资产的文件名记录下来。不得创建不存在的 `menu.md`、`progress.xlsx` 等假 production source；如果知识库没有四类素材，先由用户提供对应非敏感样本，不修改 `.env`。

- [ ] **Step 2: 写 40 条黄金集**

每一类至少 10 条，格式固定：

```json
{"question":"2026 年门店进度如何？","expect_source":"真实文件名.xlsx","expect_keywords":["完成"],"category":"year","expect_year":2026,"expect_chunk_type":"xlsx_row_group"}
```

长问题样本必须超过 200 个非空白字符；极短样本必须覆盖 1 token、2 token 和完整短语；xlsx 样本必须覆盖工作表名、列名和值；年份样本必须包含至少一个有同文档其他年份竞争的 query。

- [ ] **Step 3: 先跑无 rerank 的召回基线**

Run:

```bash
backend/.venv/Scripts/python tests/eval/run_eval.py --dataset tests/eval/golden-qa.jsonl --top-k 5 --no-rerank
```

Expected: 输出四类分类统计；记录 v1.1 baseline，不把该次结果直接写成 v1.2 通过结论。

- [ ] **Step 4: 跑 warm-up 后的完整评测**

先执行一次任意 debug query 触发模型 warm-up，再运行：

```bash
backend/.venv/Scripts/python tests/eval/run_eval.py --dataset tests/eval/golden-qa.jsonl --top-k 5
```

Expected：整体通过率至少 0.90；报告 `category_stats`、长问题 `latency_ms.p95` 和所有失败 detail。长问题 p95 超过 3000ms 时不能标记发布通过。

- [ ] **Step 5: 提交评测资产检查点**

提交需要用户授权；授权后：

```bash
git add tests/eval/run_eval.py tests/eval/README.md tests/eval/golden-qa.jsonl tests/unit/test_eval_route.py
git commit -m "test(eval): add v1.2 RAG quality golden set"
```

### Task 11：更新发布文档并完成全量验证

**Files:**
- Modify: `docs/known-limitations.md`
- Modify: `CHANGELOG.md`
- Modify: `docs/acceptance-checklist.md`（只增加已验证的 v1.2 项）
- No change: `.env`, `package.bat`, `start.bat`, `stop.bat`

- [ ] **Step 1: 先运行后端专项测试**

```bash
backend/.venv/Scripts/python -m pytest tests/unit/ -q
backend/.venv/Scripts/python -m pytest tests/integration/api/test_api.py -q
```

Expected：unit 全绿，API integration 6 tests 全绿；任一失败先修复，不更新文档为“已完成”。

- [ ] **Step 2: 运行静态检查和前端回归**

```bash
powershell -ExecutionPolicy Bypass -File scripts/run-checks.ps1
```

Expected：ruff、pytest、eslint、frontend build 四项均 PASS。此阶段不需要启动容器。

- [ ] **Step 3: 验证 Compose 配置而不启动服务**

```bash
docker compose config
```

Expected：配置渲染成功；不执行 `docker compose up`、`down -v` 或任何会修改运行时数据的命令。

- [ ] **Step 4: 更新限制和 CHANGELOG**

仅在专项测试、黄金集和性能门槛全部通过后：

- 在 `docs/known-limitations.md` 的 RAG 表中把四条从“已观察，接受”迁入 v1.2 已修复记录；
- 在 `CHANGELOG.md` 增加 v1.2.0 RAG 质量条目，引用具体测试命令和通过数字；
- 在 `docs/acceptance-checklist.md` 只勾选有命令输出证据的条目；
- 记录 reparse 实际成功/失败数量、chunk 数和 warning，不写“全部完成”这类无证据描述。

- [ ] **Step 5: 最终变更检查**

```bash
git status --short
git diff --check
git diff --stat
```

Expected：只包含本阶段允许文件；`.env`、`data/`、`vectors/` 和受保护入口没有被改动；`.gitignore` 既存改动必须保持原样，不能被本阶段顺手处理。

- [ ] **Step 6: 提交 PR4/发布检查点**

提交、打 tag 或 push 都需要分别得到用户明确授权。未得到授权时只保留工作树变更和验证输出，不执行 git mutation。

---

## Dependency Order and Review Gates

1. Task 1 先定义 QueryProfile 类型，Task 2 才能把 profile 写入 diagnostics。
2. Task 3 依赖 diagnostics 字段，先于任何行为优化建立 baseline。
3. Task 4/5 先完成 xlsx 文本和 payload，Task 6 才能安全实现 reparse replacement。
4. Task 7/8 共享 Task 1 的 profile，且必须在 Task 9 的长 query 变化前通过普通检索回归。
5. Task 10 只能在 PR1-PR4 行为代码完成后录入最终黄金集结果。
6. Task 11 是唯一发布文档更新任务；测试未全绿时不更新“已修复”状态。
7. 每个任务的代码 review 需要检查：新字段是否有旧值默认、旧 endpoint 是否保持、fallback 是否保留、敏感信息是否进入日志、PowerShell 是否仍为 5.1 语法。

## Completion Contract

实现计划完成的可验证条件：

- `tests/unit/` 全部通过；
- `tests/integration/api/test_api.py` 全部通过；
- `scripts/run-checks.ps1` 四项全绿；
- `docker compose config` 成功；
- 黄金集至少 40 条、四类各至少 10 条、整体通过率至少 90%；
- 长问题 warm-up 后检索 p95 不超过 3 秒；
- reparse 成功路径使目标 source 的 Qdrant point IDs 与 keyword_index point IDs 一致；
- reparse 前置失败不会删除旧索引；
- 所有提交、tag、push 仅在用户逐次授权后执行。
