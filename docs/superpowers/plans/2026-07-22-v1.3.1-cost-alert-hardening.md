# KB-AI v1.3.1 · cost-alert 鲁棒性强化 · 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 known-limitations §6 whole-branch review 列出的 A-D 4 项 cost-alert 子系统鲁棒性技术债收口,推到"长期可无人值守运行"等级,零业务功能新增 / 零契约变更。

**Architecture:**
- **Task 1 (atomic commit 1)** — Item A+B:`scripts/lib/CostLog-Rotate.ps1` 新建 PS 5.1 库,导出 `Rotate-CostLog` + `Get-MonthlyCostFromAllSources` + `Get-CanonicalUtcMonth`;`scripts/cost-alert.ps1` 改 dot-source 库,加日志轮转与 UTC 时区修正。
- **Task 2 (atomic commit 2)** — Item C:`backend/core/cost_alert_guard.py` 新建 `safe_get_usage_tokens`;`backend/core/rag/llm.py` 的 `_post_chat`(`:102-109`)与 `_post_chat_stream`(`:167-172`)改调 helper;8 个 pytest 测试覆盖容错矩阵。
- **Task 3 (atomic commit 3)** — Item D:`cost_alert_guard.py` 追加 `validate_cost_alert_payload` + `DEFAULT_COST_ALERT`;`backend/api/dashboard.py:_read_cost_alert()` 末尾调 validate;`tests/unit/test_chat_cost_alert_block.py` 追加 2 测(health_status.json 损坏不崩 chat 入口);6 个 validate 测覆盖矩阵。
- **Task 4 (release commit)** — 文档收口:known-limitations §6 A-D ❌ → ✅;CHANGELOG.md 顶部 v1.3.1 段;AGENTS.md §13 追加 v1.3.1 节点;`version` 1.3.0 → 1.3.1;`.gitignore` 追加 `data/cost_log.jsonl.*.gz`。

**Tech Stack:** Python 3.12 / pytest / FastAPI 0.111 / PowerShell 5.1 + .NET Framework 4.x GZipStream / ruff / vitest (本任务无前端改动) / 阿里云百炼 OpenAI 兼容协议

## Global Constraints

从 spec §1.3 提炼(每条都不允许违反):

- **不修改**:`.env` / `package.bat` / `start.bat` / `stop.bat` / `docker-compose.yml` 容器镜像 tag / `architecture-validation-report.md` Part 1 / `.gitignore` 既有规则(只追加不删除)
- **不引入新依赖**(零新增 pip / npm / PowerShell 模块;`[System.IO.Compression.GZipStream]` 是 .NET Framework 4.x 自带)
- **业务代码白名单**(v1.3.1 允许改动):`backend/core/rag/llm.py`(2 处 usage 解析改 helper 调用)、`backend/api/dashboard.py`(_read_cost_alert 末尾加 validate)、`scripts/cost-alert.ps1`(dot-source lib + 轮转 + UTC 修正)
- **PS 5.1 兼容**:所有新建 .ps1 脚本不依赖 PS 6+ 特性(无 `??`、`??=` 等 null-coalescing)
- **run-checks.ps1 不破坏**:ruff / pytest 291 / eslint / vite build 4/4 全绿
- **Git 提交**:4 atomic commit + 1 release commit,每次 `git commit` 前向用户单独授权(AGENTS.md §7.2)
- **月度切月**:UTC 自然月(每月 1 日 00:00:00 UTC 起算)
- **`_COST_ALERT_DEFAULT` 不动**:warn=$500 / high=$1000 / block=$1500 阈值在 v1.3.0 已锁版,v1.3.1 只追加字段校验不修改数值
- **5 处 `_log_token_usage` 调用点保持原样**:`backend/core/rag/llm.py:273, :285, :298, :308, :363` 已有 `if usage is not None: _log_token_usage(...)` 模式,语义已对(只确保 helper 返回 None 而非抛异常)
- **cost_log 阈值 50MB**:U 盘项目单点故障,需保留

---

## File Structure(本任务修改清单)

**新建文件**:
- `E:/scripts/lib/CostLog-Rotate.ps1` — PS 5.1 dot-source 库(A+B 共用,~60 行)
- `E:/backend/core/cost_alert_guard.py` — Python helper 模块(C+D 共用,~80 行)
- `E:/tests/unit/test_cost_alert_guard.py` — pytest 单元测试(14 测 = 8 safe + 6 validate,~180 行)

**修改文件**:
- `E:/scripts/cost-alert.ps1` — dot-source + 轮转 + UTC 修正
- `E:/backend/core/rag/llm.py` — 2 处改 helper 调用(不动 5 处 _log_token_usage)
- `E:/backend/api/dashboard.py` — `_read_cost_alert()` 末尾调 validate
- `E:/tests/unit/test_chat_cost_alert_block.py` — 追加 2 测
- `E:/docs/known-limitations.md` — §6 A-D ❌ → ✅
- `E:/CHANGELOG.md` — 顶部 v1.3.1 段
- `E:/AGENTS.md` — §13 追加 v1.3.1 节点
- `E:/version` — 1.3.0 → 1.3.1
- `E:/.gitignore` — 追加 `data/cost_log.jsonl.*.gz`

---

## Task 1: cost_log.jsonl 日志轮转 + UTC 时区修正 (Items A+B)

**Files:**
- Create: `E:/scripts/lib/CostLog-Rotate.ps1`
- Modify: `E:/scripts/cost-alert.ps1`

**Interfaces:**
- Consumes: 既有 `scripts/lib/` 目录(v1.3.0 已建立);既有 `data/cost_log.jsonl`(若存在);既有 `data/health_status.json`
- Produces:
  - `Rotate-CostLog -Path <string> -ThresholdBytes <int>` — 检测 size;超阈值则 gzip 归档 + 截断原文件
  - `Get-MonthlyCostFromAllSources -DataDir <string> -YearMonth <string>` — 累加当前 + 同月所有 .gz 归档,返回 `{input_tokens, output_tokens, today_yuan, month_yuan, sources}`(供 cost-alert.ps1 调用,替代原硬编码单文件循环)
  - `Get-CanonicalUtcMonth -Timestamp <string>` — `[DateTimeOffset]::Parse().ToUniversalTime()` 规范化,返回 "yyyy-MM" 字符串

- [ ] **Step 1: 创建 `E:/scripts/lib/CostLog-Rotate.ps1`**

```powershell
<#
.SYNOPSIS
  KB-AI · cost_log.jsonl 日志轮转 + UTC 规范化 (v1.3.1)

.DESCRIPTION
  PS 5.1 dot-source 库。导出 3 个函数:
    - Rotate-CostLog                — 50MB 阈值,gzip 归档 + 截断
    - Get-MonthlyCostFromAllSources — 累加当前 + 同月所有 .gz 归档
    - Get-CanonicalUtcMonth         — ISO8601 → UTC yyyy-MM 规范化

.NOTES
  PowerShell 5.1 兼容。所有异常 try/catch + Write-Warning,不抛。
#>

$ErrorActionPreference = "Continue"

# 阈值常量
$script:CostLogRotateBytesDefault = 50 * 1024 * 1024

function Rotate-CostLog {
    [CmdletBinding()]
    param(
        [Parameter(Mandatory = $true)]
        [string]$Path,

        [Parameter(Mandatory = $false)]
        [int]$ThresholdBytes = $script:CostLogRotateBytesDefault
    )

    if (-not (Test-Path $Path)) {
        Write-Warning "Rotate-CostLog: $Path 不存在,跳过"
        return
    }

    $file = Get-Item $Path
    if ($file.Length -lt $ThresholdBytes) {
        return  # 未超阈值,no-op
    }

    $dir = Split-Path -Parent $Path
    $base = Split-Path -Leaf $Path
    # 归档名格式: <base>.<UTC-YYYY-MM>.gz (同月重复追加 -N)
    $utcNow = [DateTimeOffset]::UtcNow
    $ym = $utcNow.ToString("yyyy-MM")
    $archiveBase = "$base.$ym"

    $archivePath = Join-Path $dir "$archiveBase.gz"
    $suffix = 2
    while (Test-Path $archivePath) {
        $archivePath = Join-Path $dir "$archiveBase-$suffix.gz"
        $suffix++
    }

    try {
        $sourceBytes = [System.IO.File]::ReadAllBytes($Path)
        $gzStream = [System.IO.File]::Create($archivePath)
        try {
            $gzip = New-Object System.IO.Compression.GZipStream($gzStream, [System.IO.Compression.CompressionMode]::Compress)
            try {
                $gzip.Write($sourceBytes, 0, $sourceBytes.Length)
            } finally {
                $gzip.Dispose()
            }
        } finally {
            $gzStream.Dispose()
        }
        # 截断原文件为空(UTF-8 无 BOM)
        [System.IO.File]::WriteAllText($Path, "", [System.Text.UTF8Encoding]::new($false))
        Write-Host "[Rotate-CostLog] archived to $archivePath ($([math]::Round($file.Length / 1MB, 1)) MB)" -ForegroundColor Green
    } catch {
        Write-Warning "Rotate-CostLog: 归档失败,保留原文件 - $($_.Exception.Message)"
    }
}

function Get-MonthlyCostFromAllSources {
    [CmdletBinding()]
    param(
        [Parameter(Mandatory = $true)]
        [string]$DataDir,

        [Parameter(Mandatory = $true)]
        [string]$YearMonth
    )

    $result = @{
        input_tokens = 0
        output_tokens = 0
        today_yuan = 0.0
        month_yuan = 0.0
        today_count = 0
        month_count = 0
        sources = @()
    }

    $todayDate = [DateTimeOffset]::UtcNow.ToString("yyyy-MM-dd")
    $priceTable = @{
        "qwen3.6-plus"      = @{ input = 4.0;   output = 12.0 }
        "qwen3.7-max"       = @{ input = 12.0;  output = 36.0 }
        "text-embedding-v3" = @{ input = 0.7;   output = 0.0  }
    }

    # 1. 当前 cost_log.jsonl
    $currentPath = Join-Path $DataDir "cost_log.jsonl"
    $sources = @($currentPath)
    # 2. 同月所有 .gz 归档(YYYY-MM[-N])
    Get-ChildItem -Path $DataDir -Filter "cost_log.jsonl.$YearMonth*.gz" -ErrorAction SilentlyContinue | ForEach-Object {
        $sources += $_.FullName
    }

    foreach ($src in $sources) {
        if (-not (Test-Path $src)) { continue }
        $result.sources += $src
        $content = $null
        try {
            if ($src.EndsWith(".gz")) {
                $gzStream = [System.IO.File]::OpenRead($src)
                try {
                    $gzip = New-Object System.IO.Compression.GZipStream($gzStream, [System.IO.Compression.CompressionMode]::Decompress)
                    try {
                        $reader = New-Object System.IO.StreamReader($gzip, [System.Text.UTF8Encoding]::new($false))
                        try {
                            $content = $reader.ReadToEnd()
                        } finally {
                            $reader.Dispose()
                        }
                    } finally {
                        $gzip.Dispose()
                    }
                } finally {
                    $gzStream.Dispose()
                }
            } else {
                $content = Get-Content $src -Raw -ErrorAction Stop
            }
        } catch {
            Write-Warning "Get-MonthlyCostFromAllSources: 读 $src 失败 - $($_.Exception.Message)"
            continue
        }

        if (-not $content) { continue }

        foreach ($line in ($content -split "`n")) {
            if ([string]::IsNullOrWhiteSpace($line)) { continue }
            try {
                $entry = $line.Trim() | ConvertFrom-Json -ErrorAction Stop
            } catch {
                continue  # 跳过损坏行
            }
            if (-not $entry.model -or $null -eq $entry.in -or $null -eq $entry.out) { continue }
            $price = $priceTable[$entry.model]
            if (-not $price) { continue }

            $entryMonth = Get-CanonicalUtcMonth -Timestamp $entry.ts
            $entryDate = ""
            try {
                $entryDate = ([DateTimeOffset]::Parse($entry.ts).ToUniversalTime()).ToString("yyyy-MM-dd")
            } catch {
                $entryDate = ""
            }

            $cost = ($entry.in / 1000000.0) * $price.input + ($entry.out / 1000000.0) * $price.output

            if ($entryMonth -eq $YearMonth) {
                $result.input_tokens += [int]$entry.in
                $result.output_tokens += [int]$entry.out
                $result.month_yuan += $cost
                $result.month_count++
            }
            if ($entryDate -eq $todayDate) {
                $result.today_yuan += $cost
                $result.today_count++
            }
        }
    }

    return $result
}

function Get-CanonicalUtcMonth {
    [CmdletBinding()]
    param(
        [Parameter(Mandatory = $true)]
        [AllowEmptyString()]
        [string]$Timestamp
    )
    if ([string]::IsNullOrWhiteSpace($Timestamp)) {
        return [DateTimeOffset]::UtcNow.ToString("yyyy-MM")
    }
    try {
        $dto = [DateTimeOffset]::Parse($Timestamp).ToUniversalTime()
        return $dto.ToString("yyyy-MM")
    } catch {
        Write-Warning "Get-CanonicalUtcMonth: unparseable ts '$Timestamp', fallback to current UTC month"
        return [DateTimeOffset]::UtcNow.ToString("yyyy-MM")
    }
}
```

- [ ] **Step 2: 修改 `E:/scripts/cost-alert.ps1` — 顶部 dot-source 库**

在文件顶部(`$ErrorActionPreference = "Continue"` 之后、`$scriptRoot = ...` 之前)插入:

```powershell
# v1.3.1: dot-source cost_log 轮转 + UTC 规范化库
. (Join-Path $PSScriptRoot 'lib/CostLog-Rotate.ps1')
```

- [ ] **Step 3: 修改 `E:/scripts/cost-alert.ps1` — 加轮转步骤**

将原第 79-117 行的"读 cost_log.jsonl + 累加"逻辑全部替换为:

```powershell
# v1.3.1: 轮转超 50MB 的 cost_log.jsonl(失败也不抛)
Rotate-CostLog -Path $logPath

# v1.3.1: 累加当前 + 同月所有 .gz 归档
if (-not (Test-Path $logPath)) {
    Write-Host "[cost-alert] [WARN] $logPath 不存在;写入 level=0" -ForegroundColor Yellow
    $agg = @{
        today_yuan = 0.0; month_yuan = 0.0
        today_count = 0; month_count = 0
        input_tokens = 0; output_tokens = 0
        sources = @()
    }
} else {
    $agg = Get-MonthlyCostFromAllSources -DataDir $dataDir -YearMonth $yearMonth
    Write-Host "[cost-alert] Today ($($agg.today_date)): ¥$([math]::Round($agg.today_yuan, 2)) / $($agg.today_count) calls" -ForegroundColor Green
    Write-Host "[cost-alert] Month ($yearMonth): ¥$([math]::Round($agg.month_yuan, 2)) / $($agg.month_count) calls" -ForegroundColor Green
    if ($agg.sources.Count -gt 1) {
        Write-Host "[cost-alert] Sources ($($agg.sources.Count) files): $($agg.sources -join ', ')" -ForegroundColor Cyan
    }
}

$todayYuan = $agg.today_yuan
$monthYuan = $agg.month_yuan
```

- [ ] **Step 4: 修改 `E:/scripts/cost-alert.ps1` — 替换 `-like` 字符串前缀比较**

确认 Step 3 已通过 `Get-MonthlyCostFromAllSources` 替代原 `$entry.ts -like "$yearMonth*"` 和 `$entry.ts -like "$todayDate*"` 比较(改用 `Get-CanonicalUtcMonth` 在库内做)。

验证方法:`grep -n '\-like' E:/scripts/cost-alert.ps1 | grep -v 'COST_ALERT_THRESHOLDS\|RAW'` 应无输出(原字符串比较已全部替换)。

- [ ] **Step 5: 手测日志轮转**

```bash
# 造 50MB+ 文件(需要管理员权限)
pwsh -Command "1..1500000 | ForEach-Object { '{\"ts\":\"2026-07-22T10:00:00Z\",\"model\":\"qwen3.6-plus\",\"in\":100,\"out\":50}' } | Set-Content -Path E:/data/cost_log.jsonl -Encoding UTF8"
```

```bash
# 跑 cost-alert.ps1
pwsh -File E:/scripts/cost-alert.ps1
```

**预期输出**:
```
[cost-alert] Rollup month: 2026-07
[cost-alert] Thresholds: warn=500, high=1000, block=1500
[Rotate-CostLog] archived to E:/data/cost_log.jsonl.2026-07.gz (... MB)
[cost-alert] Today (...): ¥... / ... calls
[cost-alert] Month (2026-07): ¥... / ... calls
[cost-alert] Level: ... 
[cost-alert] OK Wrote .../health_status.json (level=...)
```

验证:
- `E:/data/cost_log.jsonl.2026-07.gz` 文件存在
- 原 `cost_log.jsonl` 大小 < 1KB(被截断)
- `data/health_status.json` 写成功

```bash
# 清理测试文件
rm E:/data/cost_log.jsonl E:/data/cost_log.jsonl.2026-07.gz
```

- [ ] **Step 6: 手动 smoke 测试 — 验证 ruff 与 pytest 不破坏**

```bash
cd E:/ && backend/.venv/Scripts/python -m ruff check backend/ tests/
cd E:/ && backend/.venv/Scripts/python -m pytest tests/unit/ -q
```

**预期**:ruff exit 0;pytest 275 测全过(本任务未改 Python 代码,基线不变)。

- [ ] **Step 7: 提交 atomic commit 1**

```bash
cd E:/ && git add scripts/lib/CostLog-Rotate.ps1 scripts/cost-alert.ps1
cd E:/ && git commit -m "feat(cost-alert): log rotation + UTC normalization (v1.3.1 Item A+B)"
```

> 注:`git commit` 需要用户单独授权(AGENTS.md §7.2)。

---

## Task 2: safe_get_usage_tokens 防御性解析 (Item C)

**Files:**
- Create: `E:/tests/unit/test_cost_alert_guard.py`
- Create: `E:/backend/core/cost_alert_guard.py`
- Modify: `E:/backend/core/rag/llm.py` — `_post_chat` 与 `_post_chat_stream` 改 helper

**Interfaces:**
- Consumes: 既有 `backend/core/rag/llm.py`(已存在)
- Produces:
  - `safe_get_usage_tokens(usage: Any) -> Optional[Dict[str, int]]` — 防御性解析 usage,返回 `{"input_tokens", "output_tokens"}` 或 None

- [ ] **Step 1: TDD — 创建 `E:/tests/unit/test_cost_alert_guard.py`,先写 8 个测试**

```python
"""cost_alert_guard.py helper 单元测试 (v1.3.1)。

覆盖 safe_get_usage_tokens 防御性解析 + validate_cost_alert_payload 字段校验。
"""
from __future__ import annotations

import pytest

from backend.core.cost_alert_guard import safe_get_usage_tokens


# ---- safe_get_usage_tokens ----

def test_safe_get_usage_tokens_none():
    """usage 为 None → 返回 None。"""
    assert safe_get_usage_tokens(None) is None


def test_safe_get_usage_tokens_non_mapping_string():
    """usage 为非 Mapping(字符串) → 返回 None。"""
    assert safe_get_usage_tokens("invalid") is None


def test_safe_get_usage_tokens_empty_dict():
    """usage 为 {} → 返回 None(所有字段都缺)。"""
    assert safe_get_usage_tokens({}) is None


def test_safe_get_usage_tokens_dashscope_full():
    """DashScope-style 完整字段。"""
    result = safe_get_usage_tokens({"input_tokens": 100, "output_tokens": 50})
    assert result == {"input_tokens": 100, "output_tokens": 50}


def test_safe_get_usage_tokens_openai_full():
    """OpenAI-style 完整字段(prompt_tokens / completion_tokens)。"""
    result = safe_get_usage_tokens({"prompt_tokens": 200, "completion_tokens": 80})
    assert result == {"input_tokens": 200, "output_tokens": 80}


def test_safe_get_usage_tokens_partial_fields():
    """只给 input_tokens → output_tokens 填 0(允许 None → 0)。"""
    result = safe_get_usage_tokens({"input_tokens": 100})
    assert result == {"input_tokens": 100, "output_tokens": 0}


def test_safe_get_usage_tokens_mixed_invalid_string_and_valid():
    """字符串字段 "abc" 无效 → 该字段 0;另一字段有效 → 取该值。"""
    result = safe_get_usage_tokens(
        {"prompt_tokens": "abc", "completion_tokens": 50}
    )
    assert result == {"input_tokens": 0, "output_tokens": 50}


def test_safe_get_usage_tokens_numeric_string():
    """字符串数字 "100" → int("100") = 100。"""
    result = safe_get_usage_tokens({"input_tokens": "100"})
    assert result == {"input_tokens": 100, "output_tokens": 0}
```

- [ ] **Step 2: 跑测试确认 8 个全部 FAIL(模块未创建)**

```bash
cd E:/ && backend/.venv/Scripts/python -m pytest tests/unit/test_cost_alert_guard.py -v
```

**预期**:所有 8 个测试因 `ModuleNotFoundError: No module named 'backend.core.cost_alert_guard'` 而 FAIL。

- [ ] **Step 3: 创建 `E:/backend/core/cost_alert_guard.py` — 仅含 `safe_get_usage_tokens`(本任务只放这一个函数)**

```python
"""v1.3.1: cost-alert 子系统防御性 helper。

两个函数:
  - safe_get_usage_tokens:防御性解析 LLM usage 字段(避免 AttributeError 误触发 fallback)
  - validate_cost_alert_payload:字段级校验 cost_alert dict(Task 3 实现)
"""
from __future__ import annotations

from typing import Any, Mapping, Optional


def safe_get_usage_tokens(usage: Any) -> Optional[dict[str, int]]:
    """Defensive extraction of input_tokens/output_tokens from LLM usage payload.

    兼容 DashScope-style {input_tokens, output_tokens} 与
    OpenAI-style {prompt_tokens, completion_tokens} 两种字段命名。

    规则:
      - usage is None 或非 Mapping → 返回 None
      - 任一字段值是合法非负 int(或可转 int 的字符串数字) → 该字段取该值
      - 任一字段值是其他类型(None / 浮点 / 非数字字符串 / 负数) → 该字段取 0
      - 所有字段都缺失或全部无效 → 返回 None

    返回值仅在"至少有一个字段是有效整数"时为 dict;否则 None。
    调用方对 None 走"跳过 cost 记录但不触发 LLM fallback"分支。
    """
    if usage is None or not isinstance(usage, Mapping):
        return None

    # 字段映射:(字段名列表, 目标键)
    candidates = [
        (("input_tokens", "prompt_tokens"), "input_tokens"),
        (("output_tokens", "completion_tokens"), "output_tokens"),
    ]

    result: dict[str, int] = {}
    has_any_valid = False

    for field_names, target_key in candidates:
        value = None
        for fname in field_names:
            if fname in usage:
                value = usage[fname]
                break
        if value is None:
            result[target_key] = 0
            continue
        # 尝试转 int
        if isinstance(value, bool):  # bool 是 int 子类,但语义非"token 数"
            result[target_key] = 0
            continue
        if isinstance(value, int):
            if value < 0:
                result[target_key] = 0
                continue
            result[target_key] = value
            has_any_valid = True
        elif isinstance(value, str):
            try:
                parsed = int(value)
                if parsed < 0:
                    result[target_key] = 0
                    continue
                result[target_key] = parsed
                has_any_valid = True
            except (ValueError, TypeError):
                result[target_key] = 0
        else:
            # float / None / 其他类型
            result[target_key] = 0

    if not has_any_valid:
        return None
    return result
```

- [ ] **Step 4: 跑测试确认 8 个全部 PASS**

```bash
cd E:/ && backend/.venv/Scripts/python -m pytest tests/unit/test_cost_alert_guard.py -v
```

**预期**:8 个测试全 PASS。

- [ ] **Step 5: 修改 `E:/backend/core/rag/llm.py` 第 102-109 行 — `_post_chat` 改 helper 调用**

在文件顶部 import 区添加:
```python
from backend.core.cost_alert_guard import safe_get_usage_tokens
```

将原 102-109 行:
```python
    usage = parsed.get("usage")
    if usage is not None:
        # 规整为统一字段名(input_tokens / output_tokens)。
        # DashScope: input_tokens/output_tokens;OpenAI: prompt_tokens/completion_tokens。
        usage = {
            "input_tokens": int(usage.get("input_tokens", usage.get("prompt_tokens", 0))),
            "output_tokens": int(usage.get("output_tokens", usage.get("completion_tokens", 0))),
        }
```

替换为:
```python
    # v1.3.1: 改用 safe_get_usage_tokens 防御性解析(异常时返回 None 而非抛)
    usage = safe_get_usage_tokens(parsed.get("usage"))
```

- [ ] **Step 6: 修改 `E:/backend/core/rag/llm.py` 第 167-172 行 — `_post_chat_stream` 改 helper 调用**

将原 167-172 行:
```python
                # 收集 usage(可能出现在最后一个 chunk,此时 choices.delta 为空)
                if "usage" in chunk and chunk["usage"]:
                    u = chunk["usage"]
                    accumulated_usage = {
                        "input_tokens": int(u.get("input_tokens", u.get("prompt_tokens", 0))),
                        "output_tokens": int(u.get("output_tokens", u.get("completion_tokens", 0))),
                    }
```

替换为:
```python
                # v1.3.1: 改用 safe_get_usage_tokens 防御性解析
                if "usage" in chunk and chunk["usage"]:
                    accumulated_usage = safe_get_usage_tokens(chunk["usage"])
```

- [ ] **Step 7: 跑全套单测确认 275 基线 + 8 新增全过**

```bash
cd E:/ && backend/.venv/Scripts/python -m pytest tests/unit/ -q
```

**预期**:283 测全过(275 既有 + 8 新增)。

- [ ] **Step 8: ruff 检查**

```bash
cd E:/ && backend/.venv/Scripts/python -m ruff check backend/ tests/
```

**预期**:exit 0。

- [ ] **Step 9: 提交 atomic commit 2**

```bash
cd E:/ && git add backend/core/cost_alert_guard.py backend/core/rag/llm.py tests/unit/test_cost_alert_guard.py
cd E:/ && git commit -m "feat(llm): safe_get_usage_tokens defensive parsing (v1.3.1 Item C)"
```

> 注:`git commit` 需要用户单独授权(AGENTS.md §7.2)。

---

## Task 3: validate_cost_alert_payload 字段级校验 (Item D)

**Files:**
- Modify: `E:/tests/unit/test_cost_alert_guard.py` — 追加 6 个 validate 测试
- Modify: `E:/backend/core/cost_alert_guard.py` — 追加 `validate_cost_alert_payload` + `DEFAULT_COST_ALERT`
- Modify: `E:/backend/api/dashboard.py` — `_read_cost_alert()` 末尾调 validate
- Modify: `E:/tests/unit/test_chat_cost_alert_block.py` — 追加 2 测

**Interfaces:**
- Consumes: 既有 `backend/api/dashboard.py` 与既有 `tests/unit/test_chat_cost_alert_block.py`(v1.3.0 已建立,3 测)
- Produces:
  - `validate_cost_alert_payload(raw: Optional[Dict[str, Any]]) -> Dict[str, Any]` — 字段级校验,返回完整 dict(无效字段降级到默认值)

- [ ] **Step 1: 在 `E:/tests/unit/test_cost_alert_guard.py` 末尾追加 6 个 validate 测试**

```python
# ---- validate_cost_alert_payload (Task 3) ----

from backend.core.cost_alert_guard import validate_cost_alert_payload, DEFAULT_COST_ALERT  # noqa: E402


def test_validate_cost_alert_payload_none():
    """None → DEFAULT_COST_ALERT 拷贝。"""
    result = validate_cost_alert_payload(None)
    assert result["level"] == 0
    assert result["month_yuan"] == 0.0
    assert result["thresholds"]["block"] == 1500.0


def test_validate_cost_alert_payload_empty_dict():
    """{} → DEFAULT_COST_ALERT 拷贝。"""
    result = validate_cost_alert_payload({})
    assert result["level"] == 0
    assert result["today_yuan"] == 0.0
    assert result["month"] == ""


def test_validate_cost_alert_payload_level_string():
    """level="3"(字符串) → 降级 0。"""
    result = validate_cost_alert_payload({"level": "3"})
    assert result["level"] == 0


def test_validate_cost_alert_payload_level_out_of_range():
    """level=99 → 降级 0(超出 0-3 范围)。"""
    result = validate_cost_alert_payload({"level": 99})
    assert result["level"] == 0


def test_validate_cost_alert_payload_thresholds_null():
    """thresholds=null → 用 DEFAULT_COST_ALERT.thresholds。"""
    result = validate_cost_alert_payload({"level": 2, "thresholds": None})
    assert result["level"] == 2
    assert result["thresholds"] == {"warn": 500.0, "high": 1000.0, "block": 1500.0}


def test_validate_cost_alert_payload_negative_month_yuan():
    """month_yuan=-5(负数) → 降级 0.0。"""
    result = validate_cost_alert_payload({"level": 2, "month_yuan": -5})
    assert result["level"] == 2
    assert result["month_yuan"] == 0.0


def test_validate_cost_alert_payload_partial_invalid_thresholds():
    """thresholds.warn="abc" → 该字段降级 500.0,其他保留。"""
    result = validate_cost_alert_payload(
        {"level": 2, "thresholds": {"warn": "abc", "high": 800, "block": 1200}}
    )
    assert result["level"] == 2
    assert result["thresholds"]["warn"] == 500.0  # 无效 → 默认
    assert result["thresholds"]["high"] == 800.0
    assert result["thresholds"]["block"] == 1200.0
```

- [ ] **Step 2: 跑测试确认 7 个 validate 测试 FAIL(函数未实现)**

```bash
cd E:/ && backend/.venv/Scripts/python -m pytest tests/unit/test_cost_alert_guard.py -v -k "validate"
```

**预期**:7 个 validate 测试因 `ImportError: cannot import name 'validate_cost_alert_payload'` FAIL;前 8 个 safe 测试仍 PASS。

- [ ] **Step 3: 在 `E:/backend/core/cost_alert_guard.py` 末尾追加 `DEFAULT_COST_ALERT` 与 `validate_cost_alert_payload`**

```python
from typing import Any, Dict, Optional


DEFAULT_COST_ALERT: Dict[str, Any] = {
    "level": 0,
    "today_yuan": 0.0,
    "month_yuan": 0.0,
    "month": "",
    "thresholds": {"warn": 500.0, "high": 1000.0, "block": 1500.0},
    "updated_at": "",
}


def validate_cost_alert_payload(raw: Optional[Dict[str, Any]]) -> Dict[str, Any]:
    """Field-level merge/validation of cost_alert payload.

    规则(每个字段独立降级到默认值,互不影响):
      - level: int 且 0 <= level <= 3 → 取该值;否则 → 0
      - today_yuan / month_yuan:数值(int/float)且 >= 0 → 取该值(转 float);否则 → 0.0
      - month / updated_at:字符串 → 原值;否则 → ""
      - thresholds: dict 且 warn/high/block 都是合法数值 → 取该值;
          任何字段非法或缺失 → 用 DEFAULT_COST_ALERT.thresholds 对应字段

    始终返回一个完整 dict(确保下游 chat.py / dashboard.py 调用字段不抛 KeyError)。
    """
    if raw is None or not isinstance(raw, dict):
        # 深拷贝,避免下游修改污染 DEFAULT
        import copy
        return copy.deepcopy(DEFAULT_COST_ALERT)

    result: Dict[str, Any] = {}

    # level
    lvl = raw.get("level", 0)
    if isinstance(lvl, int) and not isinstance(lvl, bool) and 0 <= lvl <= 3:
        result["level"] = lvl
    else:
        result["level"] = 0

    # today_yuan / month_yuan
    for yuan_key in ("today_yuan", "month_yuan"):
        v = raw.get(yuan_key, 0.0)
        if isinstance(v, bool):  # bool 是 int 子类
            result[yuan_key] = 0.0
            continue
        if isinstance(v, (int, float)) and not isinstance(v, bool) and v >= 0:
            result[yuan_key] = float(v)
        else:
            result[yuan_key] = 0.0

    # month / updated_at
    for str_key in ("month", "updated_at"):
        v = raw.get(str_key, "")
        result[str_key] = v if isinstance(v, str) else ""

    # thresholds
    raw_thresh = raw.get("thresholds")
    default_thresh = DEFAULT_COST_ALERT["thresholds"]
    if isinstance(raw_thresh, dict):
        merged = {}
        for k in ("warn", "high", "block"):
            v = raw_thresh.get(k, default_thresh[k])
            if isinstance(v, bool) or not isinstance(v, (int, float)) or v < 0:
                merged[k] = float(default_thresh[k])
            else:
                merged[k] = float(v)
        result["thresholds"] = merged
    else:
        import copy
        result["thresholds"] = copy.deepcopy(default_thresh)

    return result
```

- [ ] **Step 4: 跑测试确认 14 个全过(safe 8 + validate 6)**

```bash
cd E:/ && backend/.venv/Scripts/python -m pytest tests/unit/test_cost_alert_guard.py -v
```

**预期**:14 个测试全 PASS。

- [ ] **Step 5: 修改 `E:/backend/api/dashboard.py` — `_read_cost_alert()` 末尾调 validate**

在文件顶部 import 区添加:
```python
from backend.core.cost_alert_guard import validate_cost_alert_payload
```

将原 `_read_cost_alert()` 函数体(第 199-216 行):
```python
def _read_cost_alert() -> Dict[str, Any]:
    """v1.3.0: 从 data/health_status.json 读 cost_alert 字段。

    文件不存在 / JSON 损坏 / 字段缺失 → 返回默认 level=0(不阻断)。
    默认阈值与 scripts/cost-alert.ps1 默认值保持一致。
    """
    try:
        data_dir = get_data_dir()
        health_path = data_dir / "health_status.json"
        if not health_path.exists():
            return dict(_COST_ALERT_DEFAULT)
        health = json.loads(health_path.read_text(encoding="utf-8"))
        ca = health.get("cost_alert")
        if not isinstance(ca, dict):
            return dict(_COST_ALERT_DEFAULT)
        return ca
    except Exception:
        return dict(_COST_ALERT_DEFAULT)
```

替换为:
```python
def _read_cost_alert() -> Dict[str, Any]:
    """v1.3.0: 从 data/health_status.json 读 cost_alert 字段。

    文件不存在 / JSON 损坏 / 字段缺失 → 返回默认 level=0(不阻断)。
    默认阈值与 scripts/cost-alert.ps1 默认值保持一致。

    v1.3.1: 末尾调用 validate_cost_alert_payload,字段级降级(level="3" → 0,
    thresholds=null → 默认,负数 month_yuan → 0 等),防止 chat 入口崩溃。
    """
    try:
        data_dir = get_data_dir()
        health_path = data_dir / "health_status.json"
        if not health_path.exists():
            return dict(_COST_ALERT_DEFAULT)
        health = json.loads(health_path.read_text(encoding="utf-8"))
        ca = health.get("cost_alert")
        if not isinstance(ca, dict):
            return dict(_COST_ALERT_DEFAULT)
        return validate_cost_alert_payload(ca)
    except Exception:
        return dict(_COST_ALERT_DEFAULT)
```

- [ ] **Step 6: 在 `E:/tests/unit/test_chat_cost_alert_block.py` 末尾追加 2 个 chat 入口防御测试**

```python
# v1.3.1: 验证 health_status.json 字段损坏时 chat 入口不崩溃
def test_chat_handles_corrupted_health_status_level_string(tmp_data_dir, monkeypatch):
    """health_status.json cost_alert.level="3"(字符串) → chat 不崩溃,retrieve 仍被调用。"""
    from backend.api import chat as chat_mod

    retrieve_called = {"flag": False}

    def _fake_retrieve(*args, **kwargs):
        retrieve_called["flag"] = True
        return []

    monkeypatch.setattr(chat_mod, "retrieve", _fake_retrieve)

    _write_health(
        tmp_data_dir,
        {"level": "3", "month_yuan": 1500.0, "thresholds": {"warn": 500, "high": 1000, "block": 1500}},
    )

    from backend.main import app

    client = TestClient(app)
    # 不应该抛 TypeError;Stream 真实跑可能走到 LLM(被 mock 拦),核心是不崩
    try:
        response = client.post("/api/chat", json={"question": "hello"})
    except TypeError as e:
        pytest.fail(f"chat endpoint crashed on corrupted level: {e}")

    # 关键断言:即使 cost_alert.level="3" 被降级为 0(不是真的 3),
    # validate 后仍会调用 retrieve(因为 validate 把字符串 level 降级为 0,
    # 不触发 level >= 3 阻断分支)
    assert retrieve_called["flag"] is True, "validate 后 level=0,应走到 retrieve"


def test_chat_handles_thresholds_null(tmp_data_dir, monkeypatch):
    """health_status.json cost_alert.thresholds=null → chat 不崩溃。"""
    from backend.api import chat as chat_mod

    def _fake_retrieve(*args, **kwargs):
        return []

    monkeypatch.setattr(chat_mod, "retrieve", _fake_retrieve)

    _write_health(
        tmp_data_dir,
        {"level": 0, "month_yuan": 0.0, "thresholds": None},
    )

    from backend.main import app

    client = TestClient(app)
    try:
        response = client.post("/api/chat", json={"question": "hello"})
    except (TypeError, AttributeError) as e:
        pytest.fail(f"chat endpoint crashed on thresholds=null: {e}")
```

- [ ] **Step 7: 跑测试确认 275 基线 + 16 新增全过**

```bash
cd E:/ && backend/.venv/Scripts/python -m pytest tests/unit/ -q
```

**预期**:291 测全过(275 既有 + 14 cost_alert_guard + 2 chat 防御 + 既有可能新增的项)。

- [ ] **Step 8: ruff 检查**

```bash
cd E:/ && backend/.venv/Scripts/python -m ruff check backend/ tests/
```

**预期**:exit 0。

- [ ] **Step 9: 提交 atomic commit 3**

```bash
cd E:/ && git add backend/core/cost_alert_guard.py backend/api/dashboard.py tests/unit/test_cost_alert_guard.py tests/unit/test_chat_cost_alert_block.py
cd E:/ && git commit -m "feat(dashboard): validate_cost_alert_payload field-level guard (v1.3.1 Item D)"
```

> 注:`git commit` 需要用户单独授权(AGENTS.md §7.2)。

---

## Task 4: 文档收口 + release commit

**Files:**
- Modify: `E:/docs/known-limitations.md`
- Modify: `E:/CHANGELOG.md`
- Modify: `E:/AGENTS.md`
- Modify: `E:/version`
- Modify: `E:/.gitignore`

**Interfaces:**
- Consumes: v1.3.1 全部 atomic commit 已落
- Produces:版本号 1.3.0 → 1.3.1;known-limitations §6 A-D ❌ → ✅;CHANGELOG/AGENTS/version/.gitignore 同步

- [ ] **Step 1: 修改 `E:/docs/known-limitations.md` — §6 A-D ❌ → ✅**

定位到 §6 "v1.3.0 release 期间 Whole-branch Review Important backlog(2026-07-21 同步)" 章节下的表格,将 A / B / C / D 4 行的 `❌ 明确推迟` 改为 `✅ v1.3.1 关闭`。

修改前(示例 A 行):
```
| A | `cost-alert.ps1` 50 MB 日志轮转未实现 | 长期运行后日志无限增长,人工轮转会漏算历史成本 | 超阈值时 gzip 归档 `cost_log.jsonl.YYYY-MM.gz`;rollup 同时扫描当前 + 归档 | 0.5d | ❌ **明确推迟** — 候选 v1.3.1+ |
```

修改后:
```
| A | `cost-alert.ps1` 50 MB 日志轮转未实现 | 长期运行后日志无限增长,人工轮转会漏算历史成本 | 超阈值时 gzip 归档 `cost_log.jsonl.YYYY-MM.gz`;rollup 同时扫描当前 + 归档 | 0.5d | ✅ v1.3.1 关闭(`scripts/lib/CostLog-Rotate.ps1:Rotate-CostLog` + `cost-alert.ps1` dot-source 库) |
```

B / C / D 类似处理。每行在工作量后追加 ✅ + 关闭说明:
- B:`✅ v1.3.1 关闭(Get-CanonicalUtcMonth + Get-MonthlyCostFromAllSources 替代 -like 字符串比较)`
- C:`✅ v1.3.1 关闭(backend/core/cost_alert_guard.py:safe_get_usage_tokens 替代裸 int(...))`
- D:`✅ v1.3.1 关闭(backend/core/cost_alert_guard.py:validate_cost_alert_payload + dashboard.py:_read_cost_alert 末尾调)`

顶部"最新更新"日期保持 2026-07-21 不变(本任务在 v1.3.1 完成;不改既有"最新更新"日期)。

- [ ] **Step 2: 修改 `E:/CHANGELOG.md` — 顶部新增 v1.3.1 段**

在 `[1.3.0] - 2026-07-21` 段之前(顶部)插入新段:

```markdown
## [1.3.1] - 2026-07-22

### cost-alert 鲁棒性强化(patch bump,zero surface area)

> 目标:把 known-limitations §6 列出的 A-D 4 项 whole-branch review 技术债收口,使 cost-alert 子系统达到"长期可无人值守运行"等级。
> 约束:不改 chat SSE / Embedding / Qdrant 契约 / 前端契约;不引入新依赖;不动 .env / start.bat / stop.bat / package.bat / 容器镜像 tag / _COST_ALERT_DEFAULT 阈值。

### 变更

- **Item A 日志轮转**:新建 `scripts/lib/CostLog-Rotate.ps1` 导出 `Rotate-CostLog`(50MB 阈值 → gzip 归档为 `cost_log.jsonl.YYYY-MM.gz`,同月重复追加 `-N`);`cost-alert.ps1` dot-source 库并在累加前调用
- **Item B UTC 规范化**:`Get-CanonicalUtcMonth` 用 `DateTimeOffset::Parse().ToUniversalTime()` 替代 `$entry.ts -like "$yearMonth*"` 字符串前缀比较;`Get-MonthlyCostFromAllSources` 同时扫描当前 + 同月归档,月度统计不再受 ISO8601 时区漂移影响
- **Item C safe_get_usage_tokens**:新建 `backend/core/cost_alert_guard.py:safe_get_usage_tokens`;`backend/core/rag/llm.py:_post_chat` (`:102-109`) 与 `_post_chat_stream` (`:167-172`) 改调 helper,malformed usage(`{"input_tokens": "abc"}` 等)不再抛 `AttributeError` 误触发 Tavily/Bing fallback;5 处 `_log_token_usage` 调用点保持 `if usage is not None: _log_token_usage(...)` 原状(语义已对)
- **Item D validate_cost_alert_payload**:`cost_alert_guard.py` 追加 `validate_cost_alert_payload` + `DEFAULT_COST_ALERT`;`backend/api/dashboard.py:_read_cost_alert()` 末尾调 validate,字段级降级(`level="3"` → 0;`thresholds=null` → 默认;负数 `month_yuan` → 0),防止 chat 入口 TypeError 崩溃

### 连带更新

- `docs/known-limitations.md §6` A-D ❌ → ✅
- `AGENTS.md §13` 追加 v1.3.1 节点
- `version` 1.3.0 → 1.3.1
- `.gitignore` 追加 `data/cost_log.jsonl.*.gz`(既有规则不动)

### 单测总数

- **基线 v1.3.0 release `cccb11e`+ 后 = pytest 275 / vitest 13**
- **v1.3.1 增量**:`tests/unit/test_cost_alert_guard.py` 新建(+14 测,safe 8 + validate 6)+ `tests/unit/test_chat_cost_alert_block.py` 追加 +2 测
- **release HEAD 总数**:**pytest 291 / vitest 13 / ruff clean / tsc clean / vite build clean**
- **FastAPI 路由**:20(无变化)
- **前端影响**:零

### 不修改(系统策略)

- ❌ `.env` / `package.bat` / `start.bat` / `stop.bat` / 容器镜像 tag / `architecture-validation-report.md` Part 1
- ❌ `.gitignore` 既有规则(只追加不删除)
- ❌ `_COST_ALERT_DEFAULT` 默认阈值(warn=$500 / high=$1000 / block=$1500,v1.3.0 锁定)

### 内部重构(零行为变更)

- 单文件拆分:`scripts/lib/CostLog-Rotate.ps1` 抽出 3 个函数(原内嵌于 `cost-alert.ps1` 的轮转 / UTC 比较 / 多源累加逻辑),`cost-alert.ps1` 改 dot-source 调用
- 测试拆分:`tests/unit/test_cost_alert_guard.py` 集中覆盖 C+D 两 helper;既有 `test_chat_cost_alert_block.py` 仅追加 2 测,既有 3 测不动
```

- [ ] **Step 3: 修改 `E:/AGENTS.md` §13 — 追加 v1.3.1 节点**

在 §13 表格中找到 `2026-07-21 | v1.3.0` 行,在其后(或最新一行之后)插入:

```
| 2026-07-22 | v1.3.1 | **cost-alert 鲁棒性强化(patch)**:whole-branch review §6 A-D 4 项收口 — 日志轮转(50MB gzip 归档)+ UTC 规范化(`DateTimeOffset`)+ `safe_get_usage_tokens` 防御性解析 + `validate_cost_alert_payload` 字段级校验;pytest 275 → 291(+16);详见 CHANGELOG.md v1.3.1 + `docs/superpowers/specs/2026-07-22-v1.3.1-cost-alert-hardening-design.md` |
```

格式参考既有 v1.1.0 / v1.2.0 / v1.3.0 节点的"日期 | 版本 | 关键变更"三栏。

- [ ] **Step 4: 修改 `E:/version` — 1.3.0 → 1.3.1**

```bash
cd E:/ && printf '1.3.1\n' > version
```

验证:
```bash
cat E:/version
```
**预期**:`1.3.1`(无尾随换行可有,但建议保持单一换行)。

- [ ] **Step 5: 修改 `E:/.gitignore` — 追加 `data/cost_log.jsonl.*.gz`**

在文件末尾追加新一行:
```
data/cost_log.jsonl.*.gz
```

**注意**:只追加,不删除既有规则(AGENTS.md §7.2)。既有 `data/cost_log.jsonl` 规则保留(运行时数据不入仓)。

- [ ] **Step 6: 跑全套门禁**

```bash
cd E:/ && powershell -File scripts/run-checks.ps1
```

**预期**:ruff / pytest 291 / eslint / vite build 4/4 全绿。

- [ ] **Step 7: 提交 release commit**

```bash
cd E:/ && git add docs/known-limitations.md CHANGELOG.md AGENTS.md version .gitignore
cd E:/ && git commit -m "release(v1.3.1): cost-alert hardening (Items A-D closed; pytest 275 → 291)"
```

> 注:`git commit` 需要用户单独授权(AGENTS.md §7.2)。

- [ ] **Step 8: 验证 release commit**

```bash
cd E:/ && git log --oneline -10
cd E:/ && git status
```

**预期**:
- 最近 5 条 commit 形如:`release(v1.3.1)` / `feat(dashboard)` / `feat(llm)` / `feat(cost-alert)` / 既有 HEAD
- `git status` 干净(无未提交改动)

---

## 验证清单(v1.3.1 完成判定)

| 项 | 期望 |
|---|---|
| `scripts/run-checks.ps1` | 4/4 全绿 |
| pytest | 291 测全过 |
| ruff | exit 0 |
| git log | 1 release + 3 atomic(feat 顺序 A+B → C → D) |
| version 文件 | `1.3.1` |
| known-limitations §6 A-D | ✅ |
| data/cost_log.jsonl.2026-07.gz(手测) | 50MB+ 文件触发后生成 |
| health_status.json corrupted payload(手测) | chat 入口不抛 TypeError |
| .gitignore | 含 `data/cost_log.jsonl.*.gz` 且既有规则未动 |
| CHANGELOG.md | 顶部 v1.3.1 段存在 |

---

## 不在 v1.3.1 范围(明确推迟)

- Dockerfile 多步构建(`known-limitations.md §4 #3`)→ v1.4.0+
- Pester 测试(`§6 #E`,为单一脚本建框架 ROI 太低)→ v1.4.0+
- COS 异地备份(`§4 #5`,用户决策)→ 待用户授权
- 远程 CI(`§5 #1b`,无 git remote)→ 等上 GitHub
- PRD 项目外文件同步(`<private>/.harness/intake/custom-kb-qa-ai-prd-draft.md`)→ v1.3.0 延期;v1.3.1 不动