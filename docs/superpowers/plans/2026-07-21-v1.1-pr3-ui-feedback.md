# PR #3 · ui-feedback · Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 关闭 known-limitations.md §2 #2(图片缩略图)+ #5(REQ-5 多选题 UI)+ #6(REQ-4 "直接回答"跳过按钮)。

**Architecture:**
- 后端:仅 `chat.py` 加 `skip_clarification: true` 请求体处理(其他字段契约已存在)
- 前端 `MessageBubble.tsx`:三段新增 — `<img>` 缩略图网格、`options` 数组单选/多选按钮、`clarification` 字段的"直接回答"按钮
- 前端 `global.css`:加缩略图网格 + 按钮样式(遵循 XAIAgent 令牌)
- RTL 测试:6 个 snapshot

**Tech Stack:** React 18 + TypeScript / Vitest + React Testing Library / CSS Modules

## Global Constraints

- **版本**:v1.1.0;本 PR 不动 version
- **不修改**:`.env` / `package.bat` / `start.bat` / `stop.bat`
- **依赖**:前端已装 `@testing-library/react`,无新依赖
- **样式**:严格遵循 `design-system/XAIAgent-design-spec.md`(#000000 / #FF540E / JetBrains Mono + Inter)
- **RTL**:`tests/unit/test_ui_components.py`(实际是 Vitest + RTL;放在 `frontend/src/__tests__/` 下)

## File Structure

| 文件 | 责任 |
|---|---|
| `backend/api/chat.py` | ChatRequest 加 `skip_clarification: bool`;handler 跳过反问 |
| `frontend/src/components/MessageBubble.tsx` | 三段新增:ImageThumbnails / OptionButtons / SkipClarificationButton |
| `frontend/src/styles/global.css` | 缩略图网格 + 多选按钮 + 跳过按钮样式 |
| `frontend/src/__tests__/MessageBubble.test.tsx`(新) | 6 个 snapshot 测 |
| `frontend/vitest.config.ts`(可能新) | Vitest + jsdom 配置 |

---

## Task 3.1: 后端 `chat.py` 加 `skip_clarification` 处理

**Files:**
- Modify: `backend/api/chat.py:ChatRequest` + SSE handler

- [ ] **Step 1: 写失败测试 — skip_clarification 字段存在**

```python
# tests/unit/test_skip_clarification.py (新文件,1 测即可)
from backend.api.chat import ChatRequest


def test_chat_request_has_skip_clarification_field():
    """ChatRequest 接受 skip_clarification bool 字段。"""
    req = ChatRequest(question="test", skip_clarification=True)
    assert req.skip_clarification is True
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /e && backend/.venv/Scripts/python -m pytest tests/unit/test_skip_clarification.py -v`
Expected: `TypeError: __init__() got an unexpected keyword argument 'skip_clarification'`

- [ ] **Step 3: 改 `backend/api/chat.py:ChatRequest`**

在现有 fields 后追加:

```python
    # v1.1.0 PR#3: REQ-4 "按原问题回答" 跳过反问按钮
    skip_clarification: bool = False
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /e && backend/.venv/Scripts/python -m pytest tests/unit/test_skip_clarification.py -v`
Expected: PASS

- [ ] **Step 5: 改 SSE handler 跳过反问路径**

定位 chat handler 中走 `clarify` 分支的位置(通常 LLM 返回 JSON 带 `clarify` 类型时)。加:

```python
    # v1.1.0 PR#3: skip_clarification=True 时,若 LLM 想反问,改走 answer 路径
    llm_response = await asyncio.to_thread(rag_llm.chat_with_fallback, ...)
    if payload.skip_clarification and llm_response.get("clarify"):
        llm_response = {
            "answer": llm_response.get("fallback_answer")
                      or "已按原问题给出回答: " + payload.question,
            "citations": llm_response.get("citations", []),
        }
```

(具体实现看现有 LLM 调用代码的分支逻辑;如果当前没有"想反问"分支,可能需要在 system prompt 加 "若用户已确认,直接给答案"的 hint。)

- [ ] **Step 6: Commit**

```bash
git add backend/api/chat.py tests/unit/test_skip_clarification.py
git commit -m "feat(chat): v1.1 PR#3 - skip_clarification flag"
```

---

## Task 3.2: 后端 `GET /api/knowledge/image` 静态图片端点

**Files:**
- Modify: `backend/api/knowledge.py`
- Test: `tests/unit/test_image_endpoint.py`(新)

- [ ] **Step 1: 写失败测试 — 静态图片端点**

```python
# tests/unit/test_image_endpoint.py
from fastapi.testclient import TestClient
from backend.main import app


def test_image_endpoint_returns_file_bytes(tmp_path, monkeypatch):
    """GET /api/knowledge/image?path= 应返回 data/uploads 下文件字节。"""
    import io
    from backend.core.config import get_data_dir
    # 准备一张假图片
    upload_dir = get_data_dir() / "uploads"
    upload_dir.mkdir(parents=True, exist_ok=True)
    target = upload_dir / "fake.png"
    target.write_bytes(b"\x89PNG\r\n\x1a\n" + b"\x00" * 100)

    client = TestClient(app)
    resp = client.get(f"/api/knowledge/image?path=fake.png")
    assert resp.status_code == 200
    assert resp.headers["content-type"].startswith("image/")
    assert resp.content[:8] == b"\x89PNG\r\n\x1a\n"


def test_image_endpoint_rejects_path_traversal():
    """path 越界(data 目录外)→ 403。"""
    client = TestClient(app)
    resp = client.get("/api/knowledge/image?path=../../etc/passwd")
    assert resp.status_code in (400, 403, 404)


def test_image_endpoint_missing_file_returns_404():
    """path 指向不存在文件 → 404。"""
    client = TestClient(app)
    resp = client.get("/api/knowledge/image?path=nonexistent_xyz.png")
    assert resp.status_code == 404
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /e && backend/.venv/Scripts/python -m pytest tests/unit/test_image_endpoint.py -v`
Expected: 404(端点不存在)

- [ ] **Step 3: 实现 `backend/api/knowledge.py` 端点**

定位 knowledge.py,在文件末尾追加:

```python
from fastapi.responses import FileResponse
from backend.core.config import get_data_dir


_IMAGE_EXT_TO_MIME = {
    ".png": "image/png",
    ".jpg": "image/jpeg",
    ".jpeg": "image/jpeg",
    ".webp": "image/webp",
    ".gif": "image/gif",
}


@router.get("/knowledge/image")
def get_image(path: str = Query(..., description="data/uploads 下的相对文件名")):
    """Serve image bytes from data/uploads. Path-traversal protected.

    Pre-Flight Finding 3 修复:T3.3 ImageThumbnails 依赖此端点。
    """
    upload_dir = (get_data_dir() / "uploads").resolve()
    target = (upload_dir / path).resolve()
    # path-traversal 防护:resolved path 必须仍在 upload_dir 下
    try:
        target.relative_to(upload_dir)
    except ValueError as exc:
        raise HTTPException(status_code=400, detail="path 越界") from exc
    if not target.exists() or not target.is_file():
        raise HTTPException(status_code=404, detail=f"image '{path}' 不存在")
    mime = _IMAGE_EXT_TO_MIME.get(target.suffix.lower(), "application/octet-stream")
    return FileResponse(str(target), media_type=mime)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /e && backend/.venv/Scripts/python -m pytest tests/unit/test_image_endpoint.py -v`
Expected: 3 passed

- [ ] **Step 5: Commit**

```bash
git add backend/api/knowledge.py tests/unit/test_image_endpoint.py
git commit -m "feat(api): v1.1 PR#3 - GET /api/knowledge/image static endpoint"
```

---

## Task 3.3: 前端 `MessageBubble.tsx` 加图片缩略图

**Files:**
- Modify: `frontend/src/components/MessageBubble.tsx`
- Test: `frontend/src/__tests__/MessageBubble.test.tsx`

- [ ] **Step 1: 读 MessageBubble.tsx 现状**

Run: `grep -n "image_paths\|options\|clarification" frontend/src/components/MessageBubble.tsx | head -20`

- [ ] **Step 2: 装 vitest + RTL(若未装)**

Run: `cd /e/frontend && npm install --save-dev vitest @testing-library/react @testing-library/jest-dom jsdom`
Expected: package.json devDependencies 更新

- [ ] **Step 3: 加 vitest 配置**

创建 `frontend/vitest.config.ts`:

```typescript
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/__tests__/setup.ts'],
  },
});
```

创建 `frontend/src/__tests__/setup.ts`:

```typescript
import '@testing-library/jest-dom/vitest';
```

- [ ] **Step 4: 加 ImageThumbnails 子组件**

在 `MessageBubble.tsx` 顶部加:

```typescript
interface ImageThumbnailsProps {
  paths: string[];
  onImageClick?: (path: string) => void;
}

function ImageThumbnails({ paths, onImageClick }: ImageThumbnailsProps) {
  if (!paths || paths.length === 0) return null;
  return (
    <div className="image-thumbnails" data-testid="image-thumbnails">
      {paths.map((p, idx) => (
        <img
          key={idx}
          src={`/api/knowledge/image?path=${encodeURIComponent(p)}`}
          alt={`附件 ${idx + 1}`}
          className="thumbnail"
          onClick={() => onImageClick?.(p)}
          loading="lazy"
        />
      ))}
    </div>
  );
}
```

注意:`/api/knowledge/image?path=` 端点可能不存在;若不存在,改用 `URL.createObjectURL` 路径(需后端支持)。**简化方案**:先做相对路径直接渲染 `<img src={path}>`(假定 path 已是 `/data/uploads/xxx.png` 类的 URL),后端若不支持再加端点。

- [ ] **Step 5: 在 MessageBubble 主体渲染 ImageThumbnails**

定位 MessageBubble 组件函数体,在文本渲染前后插入:

```typescript
  // v1.1.0 PR#3: 图片缩略图渲染
  const imagePaths = (message.image_paths || []) as string[];
  // ...
  return (
    <div className="message-bubble ...">
      {imagePaths.length > 0 && <ImageThumbnails paths={imagePaths} />}
      {/* 现有文本渲染 */}
    </div>
  );
```

(具体位置看 MessageBubble 现有 JSX 结构)

- [ ] **Step 6: 加 CSS 样式**

`frontend/src/styles/global.css` 追加:

```css
.image-thumbnails {
  display: grid;
  grid-template-columns: repeat(auto-fill, 80px);
  gap: 8px;
  margin: 8px 0;
}

.image-thumbnails .thumbnail {
  width: 80px;
  height: 80px;
  object-fit: cover;
  border-radius: 8px;
  border: 1px solid #1F2937;
  cursor: pointer;
  transition: transform 150ms ease;
}

.image-thumbnails .thumbnail:hover {
  transform: scale(1.05);
  border-color: #FF540E;
}
```

- [ ] **Step 7: 写 RTL 测试 — 缩略图渲染**

```typescript
// frontend/src/__tests__/MessageBubble.test.tsx
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { MessageBubble } from '../components/MessageBubble';

describe('MessageBubble image thumbnails', () => {
  it('renders thumbnails when image_paths provided', () => {
    const message = {
      role: 'user' as const,
      content: '看这张图',
      image_paths: ['/data/uploads/test.png', '/data/uploads/test2.png'],
    };
    render(<MessageBubble message={message} />);
    expect(screen.getByTestId('image-thumbnails')).toBeInTheDocument();
    expect(screen.getAllByRole('img')).toHaveLength(2);
  });

  it('does not render thumbnails when image_paths empty', () => {
    const message = { role: 'user' as const, content: 'no image' };
    render(<MessageBubble message={message} />);
    expect(screen.queryByTestId('image-thumbnails')).not.toBeInTheDocument();
  });
});
```

- [ ] **Step 8: 跑测试**

Run: `cd /e/frontend && npx vitest run src/__tests__/MessageBubble.test.tsx`
Expected: 2 passed

- [ ] **Step 9: 跑 vite build 验证**

Run: `cd /e/frontend && npm run build`
Expected: build 成功

- [ ] **Step 10: Commit**

```bash
git add frontend/src/components/MessageBubble.tsx frontend/src/styles/global.css frontend/src/__tests__/MessageBubble.test.tsx frontend/vitest.config.ts frontend/src/__tests__/setup.ts frontend/package.json frontend/package-lock.json
git commit -m "feat(ui): v1.1 PR#3 - image thumbnail grid in MessageBubble"
```

---

## Task 3.4: 前端 `MessageBubble.tsx` 加多选题 UI

**Files:**
- Modify: `frontend/src/components/MessageBubble.tsx`
- Test: `frontend/src/__tests__/MessageBubble.test.tsx`

- [ ] **Step 1: 加 OptionButtons 子组件**

```typescript
interface OptionButtonsProps {
  options: string[];
  multi: boolean;
  onSelect: (selected: string[]) => void;
}

function OptionButtons({ options, multi, onSelect }: OptionButtonsProps) {
  const [selected, setSelected] = useState<string[]>([]);

  const toggle = (opt: string) => {
    if (multi) {
      setSelected((s) =>
        s.includes(opt) ? s.filter((x) => x !== opt) : [...s, opt]
      );
    } else {
      onSelect([opt]);
      return;
    }
  };

  const handleConfirm = () => {
    if (selected.length > 0) onSelect(selected);
  };

  return (
    <div className="option-buttons" data-testid="option-buttons">
      {options.map((opt, idx) => (
        <button
          key={idx}
          className={`option-btn ${selected.includes(opt) ? 'selected' : ''}`}
          onClick={() => toggle(opt)}
        >
          {opt}
        </button>
      ))}
      {multi && (
        <button
          className="option-confirm"
          onClick={handleConfirm}
          disabled={selected.length === 0}
        >
          确认 ({selected.length})
        </button>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 在 MessageBubble 渲染 options**

```typescript
  // v1.1.0 PR#3: REQ-5 多选题 UI
  const options = (message.options || []) as string[];
  const isMulti = (message.question_type === 'multi_choice');
  // ...
  {options.length > 0 && (
    <OptionButtons
      options={options}
      multi={isMulti}
      onSelect={(selected) => onUserReply(selected.join(', '))}
    />
  )}
```

需 props 加 `onUserReply(text: string)`(由 App.tsx 传入,发出新一轮消息)。

- [ ] **Step 3: 加 CSS**

```css
.option-buttons {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin: 12px 0;
}

.option-btn {
  background: #1F2937;
  color: #FAFAFA;
  border: 1px solid #374151;
  border-radius: 8px;
  padding: 8px 16px;
  font-family: 'Inter', sans-serif;
  cursor: pointer;
  transition: all 150ms ease;
}

.option-btn:hover {
  border-color: #FF540E;
}

.option-btn.selected {
  background: #FF540E;
  border-color: #FF540E;
}

.option-confirm {
  background: #FF540E;
  color: white;
  border: none;
  border-radius: 8px;
  padding: 8px 16px;
  cursor: pointer;
}

.option-confirm:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}
```

- [ ] **Step 4: 写 RTL 测试**

```typescript
// MessageBubble.test.tsx 加:
describe('MessageBubble option buttons', () => {
  it('renders single-choice buttons and triggers reply on click', () => {
    const onReply = vi.fn();
    const message = {
      role: 'assistant' as const,
      content: '请选择',
      options: ['A', 'B', 'C'],
      question_type: 'single_choice',
    };
    render(<MessageBubble message={message} onUserReply={onReply} />);
    const buttons = screen.getAllByRole('button');
    expect(buttons).toHaveLength(3);
    fireEvent.click(buttons[0]);
    expect(onReply).toHaveBeenCalledWith('A');
  });

  it('renders multi-choice with confirm button', () => {
    const onReply = vi.fn();
    const message = {
      role: 'assistant' as const,
      content: '可多选',
      options: ['X', 'Y', 'Z'],
      question_type: 'multi_choice',
    };
    render(<MessageBubble message={message} onUserReply={onReply} />);
    fireEvent.click(screen.getByText('X'));
    fireEvent.click(screen.getByText('Z'));
    fireEvent.click(screen.getByText(/确认/));
    expect(onReply).toHaveBeenCalledWith('X, Z');
  });
});
```

- [ ] **Step 5: 跑测试**

Run: `cd /e/frontend && npx vitest run src/__tests__/MessageBubble.test.tsx`
Expected: 4 passed (含 thumbnail 2 + options 2)

- [ ] **Step 6: Commit**

```bash
git add frontend/src/components/MessageBubble.tsx frontend/src/styles/global.css frontend/src/__tests__/MessageBubble.test.tsx
git commit -m "feat(ui): v1.1 PR#3 - multi-choice option buttons in MessageBubble"
```

---

## Task 3.5: 前端 `MessageBubble.tsx` 加"直接回答"跳过按钮

**Files:**
- Modify: `frontend/src/components/MessageBubble.tsx`
- Modify: `frontend/src/pages/ChatPage.tsx`(发消息时带 `skip_clarification: true`)
- Test: `frontend/src/__tests__/MessageBubble.test.tsx`

- [ ] **Step 1: 加 SkipClarificationButton 子组件**

```typescript
interface SkipClarificationButtonProps {
  originalQuestion: string;
  onSkip: () => void;
}

function SkipClarificationButton({ originalQuestion, onSkip }: SkipClarificationButtonProps) {
  return (
    <button
      className="skip-clarification-btn"
      onClick={onSkip}
      data-testid="skip-clarification"
    >
      ↪ 按原问题"{originalQuestion}"直接回答
    </button>
  );
}
```

- [ ] **Step 2: 在 MessageBubble 渲染**

```typescript
  // v1.1.0 PR#3: REQ-4 "按原问题回答" 跳过反问按钮
  if (message.clarification && onSkipClarification) {
    // 在 assistant 消息下追加按钮
    body = (
      <>
        {body}
        <SkipClarificationButton
          originalQuestion={message.clarification.original_question || lastUserMessage}
          onSkip={onSkipClarification}
        />
      </>
    );
  }
```

需 MessageBubble props 加 `onSkipClarification: () => void` 和 `lastUserMessage: string`。

- [ ] **Step 3: ChatPage 处理 skip 回调**

```typescript
// ChatPage.tsx:
const handleSkipClarification = useCallback(() => {
  // 重新发原问题,带 skip_clarification: true
  sendMessage(currentQuestion, { skip_clarification: true });
}, [currentQuestion, sendMessage]);
```

`sendMessage` 第二参接受 options 对象(需 lib/api.ts 加签名或 App.tsx 处理)。

- [ ] **Step 4: 加 CSS**

```css
.skip-clarification-btn {
  background: transparent;
  color: #FF540E;
  border: 1px dashed #FF540E;
  border-radius: 8px;
  padding: 6px 12px;
  margin-top: 8px;
  font-family: 'Inter', sans-serif;
  font-size: 13px;
  cursor: pointer;
}

.skip-clarification-btn:hover {
  background: rgba(255, 84, 14, 0.1);
}
```

- [ ] **Step 5: 写 RTL 测试**

```typescript
describe('MessageBubble skip clarification', () => {
  it('renders skip button when clarification present', () => {
    const onSkip = vi.fn();
    const message = {
      role: 'assistant' as const,
      content: '你想问哪个?',
      clarification: { original_question: '什么是 RAG?' },
    };
    render(
      <MessageBubble
        message={message}
        lastUserMessage="什么是 RAG?"
        onSkipClarification={onSkip}
      />
    );
    const btn = screen.getByTestId('skip-clarification');
    expect(btn).toBeInTheDocument();
    fireEvent.click(btn);
    expect(onSkip).toHaveBeenCalled();
  });
});
```

- [ ] **Step 6: 跑测试 + build**

Run: `cd /e/frontend && npx vitest run src/__tests__/MessageBubble.test.tsx`
Run: `cd /e/frontend && npm run build`
Expected: 5 passed;build 成功

- [ ] **Step 7: Commit**

```bash
git add frontend/src/components/MessageBubble.tsx frontend/src/pages/ChatPage.tsx frontend/src/styles/global.css frontend/src/__tests__/MessageBubble.test.tsx
git commit -m "feat(ui): v1.1 PR#3 - skip clarification button"
```

---

## Task 3.6: 跑全套回归 + 更新 CHANGELOG

- [ ] **Step 1: 跑 run-checks.ps1**

Run: `cd /e && pwsh -File scripts/run-checks.ps1`
Expected: ruff ✅ / pytest ✅ / eslint ✅ / vite build ✅

- [ ] **Step 2: 跑 RTL 全套**

Run: `cd /e/frontend && npx vitest run`
Expected: 5 passed

- [ ] **Step 3: 跑 RAG eval**

```bash
cd /e && curl -X POST http://localhost:8000/api/eval/run -H "Content-Type: application/json" -d '{}'
```
Expected: ≥ 80% 通过

- [ ] **Step 4: 更新 CHANGELOG**

在 v1.1.0 PR #2 段后追加:

```markdown
### PR #3 ui-feedback(REQ-4 / REQ-5 / 图片缩略图)

- **#2 图片缩略图**:`MessageBubble` 渲染 `image_paths` 为 80×80 圆角网格;点击放大;遵循 XAIAgent 令牌
- **#5 多选题 UI**:`options` 数组 → 单选(直接回调)/ 多选(确认按钮);沿用 #FF540E 主色
- **#6 跳过反问按钮**:`clarification` 字段 → 渲染"按原问题直接回答"虚线按钮;后端 `skip_clarification: true` 跳过反问分支

新增测试:
- `tests/unit/test_skip_clarification.py` 1 测
- `frontend/src/__tests__/MessageBubble.test.tsx` 5 测(RTL)

单测总数: 177 → 183(+6)
```

- [ ] **Step 5: Commit**

```bash
git add CHANGELOG.md
git commit -m "docs(changelog): v1.1 PR#3 - ui-feedback CHANGELOG"
```

---

**Status**:🟡 待用户 review · PR #3 完成后,执行 PR #4(tag-and-title)