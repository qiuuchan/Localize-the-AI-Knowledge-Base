# KB-AI v1.4.0 · Dockerfile 多步构建范式化交付 · 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `known-limitations.md §4 #3`「Dockerfile 多步构建未优化」以范式化交付方式关闭:`dify-db-init` 切到本地多阶段构建 + 根 `.dockerignore` 范本 + `docs/docker-build.md` 边界文档;**不动业务代码、不动既有镜像 tag**。

**Architecture:** 在 `docker/dify-db-init/Dockerfile` 落 8 行多阶段样本(builder: alpine:3.19 + apk add sqlite / runtime: alpine:3.19 + COPY sqlite3);`docker-compose.yml` `dify-db-init` service 单字段切换 `image: alpine:3.19` → `build: + image: kb-ai/dify-db-init:local`;其余 3 个 service 与所有镜像 tag 锁版不动。

**Tech Stack:** Docker Compose v2 / Alpine 3.19 / SQLite / Markdown

---

## Global Constraints

来自 spec `docs/superpowers/specs/2026-07-22-v1.4-dockerfile-paradigm-design.md` §1.4 与 §1.5:

- ❌ 不改 `.env` / `package.bat` / `start.bat` / `stop.bat` 任何文件
- ❌ 不改 `docker-compose.yml` 中 qdrant / dify-api / dify-worker 的 `image:` 字段(`qdrant:1.7.0` / `dify-api:1.0.0` × 2 全部锁版)
- ❌ 不改 `docker/dify-db-init/Dockerfile` 中 `FROM alpine:3.19` 的 alpine 版本号(3.19 锁版)
- ❌ 不改 `architecture-validation-report.md` Part 1(§1-§5)
- ❌ 不引入新 Python/Node/PowerShell 依赖
- ❌ 不改 backend / frontend / scripts 任何业务代码
- ❌ 不跑 `docker compose up`(需真实 API key)
- ⚠️ `dify-db-init` 从 `image: alpine:3.19` 切到 `build: + image: kb-ai/dify-db-init:local` 是**新增本地 tag**,已在 spec §1.4 显式声明,user 授权 commit spec 时已一并授权
- ✅ 既有 292 pytest / 13 vitest / ruff / tsc / vite build 全绿(无业务改动,无新增测)
- ✅ commit 节奏:1 个范式 commit(T1-3)+ 1 个文档 commit(T4)+ 1 个 release commit(T5),与 v1.3.0/v1.3.1 一致

---

## File Structure

### 新建

| 路径 | 责任 |
|---|---|
| `E:/docker/dify-db-init/Dockerfile` | 多阶段构建范式样本(8 行,纯展示性;行为等价 `image: alpine:3.19 + apk add sqlite`) |
| `E:/.dockerignore` | build context 排除规则(数据卷 / 虚拟环境 / 前端 dist / .env 等) |
| `E:/docs/docker-build.md` | 多步构建范式说明 + 边界声明 + 未来扩展路径 |

### 修改

| 路径 | 行号 | 改动 |
|---|---|---|
| `E:/docker-compose.yml` | 177-208(dify-db-init service)| `image: alpine:3.19` → `build: { context: ./docker/dify-db-init, dockerfile: Dockerfile }` + `image: kb-ai/dify-db-init:local` |
| `E:/docs/known-limitations.md` | §4 #3 状态行 + §6 backlog 段 | ❌ → ✅;§6 移除 Dockerfile 行 |
| `E:/CHANGELOG.md` | 顶部新增 | `[1.4.0]` 段 |
| `E:/AGENTS.md` | §13 表格 | 追加 v1.4.0 节点 |
| `E:/version` | 全文 | `1.3.1` → `1.4.0` |
| `E:/README.md` | 顶部版本行 | 可选:`(v1.3.1)` → `(v1.4.0)` |

---

## Task 1: 多阶段 Dockerfile + docker-compose.yml 切换

**Files:**
- Create: `E:/docker/dify-db-init/Dockerfile`
- Modify: `E:/docker-compose.yml:177-208`
- Verify: `docker compose config` 干跑 + `docker compose build dify-db-init` build-only 验证

**Interfaces:**
- Consumes: 无(无前置 task 依赖)
- Produces:
  - `E:/docker/dify-db-init/Dockerfile`:`FROM alpine:3.19 AS builder` + `FROM alpine:3.19` + `COPY --from=builder /usr/bin/sqlite3 /usr/bin/sqlite3`
  - `E:/docker-compose.yml` `dify-db-init` service: `build:` 字段指向 `./docker/dify-db-init`,`image:` 字段为 `kb-ai/dify-db-init:local`

### Steps

- [ ] **Step 1.1: 创建 `E:/docker/dify-db-init/` 目录**

Run (Windows cmd via Bash):
```bash
mkdir -p "E:/docker/dify-db-init"
```

Expected: directory created, no error. Verify:
```bash
ls -la "E:/docker/dify-db-init"
```
Expected: empty directory listing.

- [ ] **Step 1.2: 写 `E:/docker/dify-db-init/Dockerfile`(8 行多阶段样本)**

用 `Write` 工具写入,内容逐字如下(注意:`E:/docker/dify-db-init/Dockerfile` 路径,内容用 LF 行尾):

```dockerfile
# KB-AI v1.4.0 · 多阶段构建范式样本
# 目的:为未来 backend / frontend 容器化预留可复用的多阶段模板
# 现状:dify-db-init 仅需 alpine + sqlite,builder 与 runtime 共享 base;多阶段用作范式示例
# 边界:本镜像实际功能等价于直接使用 alpine:3.19 + apk add sqlite;多阶段语法仅为未来扩展入口

# === Stage 1: builder ===
# 在独立阶段装 sqlite,验证 apk cache + layer 复用
FROM alpine:3.19 AS builder
RUN apk add --no-cache sqlite

# === Stage 2: runtime ===
# COPY builder 产物到最终镜像;若未来需要 init.sql 模板或额外工具,在 builder 阶段注入即可
FROM alpine:3.19
COPY --from=builder /usr/bin/sqlite3 /usr/bin/sqlite3
```

- [ ] **Step 1.3: 验证 Dockerfile 可被 build**

Run:
```bash
cd "E:/" && docker build --no-cache -t kb-ai/dify-db-init:test-paradigm "E:/docker/dify-db-init"
```
Expected: build 成功,输出含 `naming to docker.io/library/kb-ai/dify-db-init:test-paradigm`,exit 0。

如果失败,fallback 到 docker compose 命令(可能 docker compose 路径在 PATH):
```bash
cd "E:/" && docker compose build dify-db-init
```
Expected: build 成功,dify-db-init service image 显示为 `kb-ai/dify-db-init:local`。

- [ ] **Step 1.4: 验证镜像大小合理**

Run:
```bash
docker images kb-ai/dify-db-init:test-paradigm --format "{{.Size}}"
```
Expected: 大小 ≤ 15MB(alpine base 5MB + sqlite 1MB + COPY 层极小)。

清理测试镜像:
```bash
docker rmi kb-ai/dify-db-init:test-paradigm
```
Expected: 退出码 0。

- [ ] **Step 1.5: 修改 `E:/docker-compose.yml` dify-db-init service**

读取 `E:/docker-compose.yml` 找到 `dify-db-init` service 起始位置(原第 177 行),用 `Edit` 工具精确替换。

`old_string`(第 177-179 行,精确匹配):
```
  dify-db-init:
    image: alpine:3.19
    container_name: kb-ai-dify-db-init
```

`new_string`(替换为):
```
  dify-db-init:
    # v1.4.0:从 image: alpine:3.19 切到 build: (多阶段范式样本,见 docker/dify-db-init/Dockerfile)
    build:
      context: ./docker/dify-db-init
      dockerfile: Dockerfile
    image: kb-ai/dify-db-init:local   # 自定义 tag,避免与 alpine:3.19 冲突
    container_name: kb-ai-dify-db-init
```

注意:仅替换 `image:` 那 1 行 + 在 `image:` 前插入 3 行 `build:`;其余字段(`volumes:` / `networks:` / `depends_on:` / `mem_limit:` / `entrypoint:` / `command:`)完全保留原状。**不要替换整个 service 块**。

- [ ] **Step 1.6: 验证 compose 配置解析正确**

Run:
```bash
cd "E:/" && docker compose config --services
```
Expected: 输出含 `dify-db-init`,exit 0。

Run:
```bash
cd "E:/" && docker compose config | grep -A 4 "dify-db-init:"
```
Expected: 输出含 `build:` + `context: ./docker/dify-db-init` + `dockerfile: Dockerfile` + `image: kb-ai/dify-db-init:local`。

- [ ] **Step 1.7: 验证 build cache 命中(等价性)**

Run:
```bash
cd "E:/" && docker compose build dify-db-init
```
Expected: build 成功(可能首次会拉 alpine:3.19 层 + apk 装 sqlite ~5-8s;二次跑应该秒级命中 cache)。退出码 0。

- [ ] **Step 1.8: 验证 entrypoint/command 行为等价**

Read `E:/docker-compose.yml` dify-db-init service 的 `entrypoint:` 和 `command:` 字段,确认与 v1.3.1 完全一致(应是 `["/bin/sh", "-c"]` + 包含 `apk add --no-cache sqlite >/dev/null 2>&1` 的 shell 脚本)。

说明:由于 alpine:3.19 的 base layer 已经在 build 阶段包含 sqlite3 客户端(via `apk add` in builder + COPY in runtime),运行时不再需要 `apk add`;但 `command:` 仍包含 `apk add --no-cache sqlite >/dev/null 2>&1`(幂等,二次装包耗时 < 0.5s,功能等价)。**这是有意的**:保留 v1.3.1 的 `command:` 字面量,确保向前兼容 / 易于回滚到 `image: alpine:3.19` 时行为不变。

- [ ] **Step 1.9: Commit**

```bash
cd "E:/" && git add docker/dify-db-init/Dockerfile docker-compose.yml
git commit -m "feat(v1.4.0): dify-db-init 多阶段构建范式样本

- 新建 docker/dify-db-init/Dockerfile (8 行多阶段: builder stage
  装 sqlite, runtime stage COPY 二进制; 仅作范式, 行为等价于
  直接 alpine:3.19 + apk add sqlite)
- docker-compose.yml dify-db-init service 从 image: alpine:3.19
  切到 build: + image: kb-ai/dify-db-init:local (新增本地 tag,
  性质不同于 bump 已锁版的官方镜像 tag; spec §1.4 已显式声明
  并经用户授权)
- 其余 3 个 service (qdrant / dify-api / dify-worker) 与所有
  既有镜像 tag 锁版不动
- 既有 292 单测不回归 (本 task 无业务代码改动)
- 关闭 known-limitations §4 #3 状态 ❌ -> ✅ (T4 同步)"
```

---

## Task 2: 根 `.dockerignore` 范本

**Files:**
- Create: `E:/.dockerignore`
- Verify: 文件存在 + 关键条目齐

**Interfaces:**
- Consumes: 无
- Produces: `E:/.dockerignore` 含 `data/` / `vectors/` / `cache/` / `logs/` / `tmp/` / `*.sqlite` / `.env` 等关键排除规则

### Steps

- [ ] **Step 2.1: 写 `E:/.dockerignore`(32 行范本)**

用 `Write` 工具写入,内容逐字如下:

```dockerignore
# KB-AI v1.4.0 · 根 .dockerignore(范本,为未来容器化预留)
# 当前:项目无完整 Dockerfile,本文件实际生效需待 backend / frontend 容器化 PR 落地
# 覆盖目标:把数据卷 / 虚拟环境 / 前端构建产物 / 文档 / 大文件全部排除,避免污染 build context

# VCS 与元数据
.git
.gitignore
.gitattributes
.kb-ai-root
.superpowers
.agents
.claude
.pytest_cache
.ruff_cache

# 运行时数据卷(由 docker-compose volumes 挂载,不进镜像)
data/
vectors/
cache/
logs/
tmp/

# 文档(仅 README 保留,作为镜像入口说明)
*.md
docs/
README.md
!README.md
QUICKSTART.md
!QUICKSTART.md

# 前端 / 后端虚拟环境与构建产物
frontend/node_modules/
frontend/dist/       # 已构建好,不需要重新打包进镜像
backend/.venv/
backend/__pycache__/
backend/**/__pycache__/

# 测试与覆盖率
tests/.pytest_cache/
tests/integration/.pytest_cache/
frontend/coverage/
frontend/.vitest/

# 大文件 / 临时产物
*.sqlite
*.sqlite-journal
*.sqlite-shm
*.sqlite-wal
*.gz
*.zip
*.tar
*.log
!logs/.gitkeep

# 环境与凭据(绝不能进镜像)
.env
.env.example
.env.local

# 编辑器与 IDE
.vscode/
.idea/
*.swp
*.swo

# 系统文件
Thumbs.db
desktop.ini
.DS_Store
```

- [ ] **Step 2.2: 验证 `.dockerignore` 关键规则齐**

Run:
```bash
cd "E:/" && grep -E "^(data/|\.env|\*\.sqlite|frontend/dist/|backend/\.venv/)" .dockerignore
```
Expected: 至少命中 5 行(`data/` / `.env` / `*.sqlite` / `frontend/dist/` / `backend/.venv/`)。

- [ ] **Step 2.3: 验证 `.gitignore` 未被误覆盖**

说明:`.dockerignore` 与 `.gitignore` 是两个独立的文件,不应混用。本次新增 `.dockerignore`,**不动 `.gitignore`**。

Run:
```bash
cd "E:/" && git diff --name-only HEAD -- .gitignore
```
Expected: 无输出(`.gitignore` 未被修改)。

- [ ] **Step 2.4: 顺手验证 build context 体积缩减(可选 sanity check)**

如果 docker 可用,验证 `.dockerignore` 在 build 时生效:
```bash
cd "E:/" && docker build --no-cache --progress=plain -f docker/dify-db-init/Dockerfile . 2>&1 | head -40
```
Expected: build 输出中 `transferring context` 行显示的字节数 ≤ 50MB(因为 `data/` / `vectors/` / `cache/` / `logs/` / `tmp/` / `node_modules/` / `.venv/` 全被排除)。如果出现"transfer context: XXX MB",XXX 应远小于 1GB。

注:此步骤为 sanity check,**失败不阻塞**;若 docker 环境不可用可跳过。

- [ ] **Step 2.5: Commit**

```bash
cd "E:/" && git add .dockerignore
git commit -m "feat(v1.4.0): 根 .dockerignore 范本(为未来容器化预留)

- 排除运行时数据卷 (data/ vectors/ cache/ logs/ tmp/)
- 排除虚拟环境 (backend/.venv/ frontend/node_modules/)
- 排除前端构建产物 (frontend/dist/)
- 排除凭据 (.env / .env.example / .env.local)
- 排除文档 (*.md 默认; README.md / QUICKSTART.md 显式保留)
- 排除大文件 (*.sqlite / *.gz / *.zip / *.tar / *.log)
- 当前项目无完整 Dockerfile,本文件实际生效需待 backend /
  frontend 容器化 PR 落地; 此处作为范本与未来构建入口"
```

---

## Task 3: 创建 `docs/docker-build.md` 边界文档

**Files:**
- Create: `E:/docs/docker-build.md`
- Verify: 关键章节齐 + 边界声明在 §1.3

**Interfaces:**
- Consumes: 无
- Produces: 完整 markdown 文档,含 §1 现状/边界 / §2 范式样本 / §3 .dockerignore 说明 / §4 不在范围

### Steps

- [ ] **Step 3.1: 写 `E:/docs/docker-build.md`(约 100 行)**

用 `Write` 工具写入,内容逐字如下:

```markdown
# KB-AI · Docker 构建范式

> **本文档目的**:记录 v1.4.0 引入的多阶段构建范式 + 当前架构边界 + 未来扩展指引。
> **配套版本**:KB-AI v1.4.0(2026-07-22)
> **维护**:本文档随 docker-compose.yml / Dockerfile / .dockerignore 变更同步更新

---

## 1. 当前架构(v1.4.0)

### 1.1 容器组成

| Service | 类型 | Image Tag | 备注 |
|---|---|---|---|
| qdrant | 官方 | `qdrant/qdrant:v1.7.0` | 锁版 |
| dify-api | 官方 | `langgenius/dify-api:1.0.0` | 锁版 |
| dify-worker | 官方 | `langgenius/dify-api:1.0.0` | 锁版 |
| dify-db-init | **本地构建(v1.4.0+)** | `kb-ai/dify-db-init:local` ← `alpine:3.19` 多阶段 | 新增 |

### 1.2 host 直跑组件(不进容器)

- FastAPI backend:`scripts/start-backend.ps1` 启 `backend/.venv/Scripts/python -m uvicorn`
- MinerU parser:`backend/mineru_server.py`(host 端 Python 进程)
- Frontend:`frontend/dist/` 由 FastAPI 挂载到 `:8000`

### 1.3 边界声明(必读)

**当前架构无可"多步优化"的源码**:

- backend / frontend / mineru 全部 host 直跑,**无 Dockerfile 可构建**
- 1.5GB 镜像体积来自 `langgenius/dify-api:1.0.0` 官方镜像本体,我们无法减小
- 冷启动 5-10min 的真正瓶颈是 **Docker Hub 国内拉镜像延迟**(改 `~/.docker/daemon.json` 的 `registry-mirrors` 可缓解,非本文档范围);dify-api 内部 cold start 需改 Dify 1.0 启动逻辑,我们也无源码
- v1.4.0 **不解决**以上任何一项;仅落地多阶段构建**范式样本** + `.dockerignore` 范本 + 本文档

---

## 2. v1.4.0 范式样本

### 2.1 dify-db-init 多阶段 Dockerfile

文件位置:`docker/dify-db-init/Dockerfile`(8 行)

```dockerfile
FROM alpine:3.19 AS builder
RUN apk add --no-cache sqlite

FROM alpine:3.19
COPY --from=builder /usr/bin/sqlite3 /usr/bin/sqlite3
```

**功能等价性**:本镜像实际功能等价于"alpine:3.19 + apk add sqlite"。多阶段语法仅为未来扩展入口(若需要 init.sql 模板、额外工具,可注入 builder 阶段)。

**compose 集成**:见 `docker-compose.yml` dify-db-init service:
```yaml
dify-db-init:
  build:
    context: ./docker/dify-db-init
    dockerfile: Dockerfile
  image: kb-ai/dify-db-init:local
  # ... 其余字段不变
```

### 2.2 未来扩展路径(backend 容器化,伪代码示例)

> ⚠️ **非 v1.4.0 范围**;以下为示意,等用户决策后再实施。

```dockerfile
# 伪代码,展示未来 backend Dockerfile 结构(不实现)
FROM python:3.12-slim AS builder
WORKDIR /app
COPY backend/requirements.txt .
RUN pip wheel --wheel-dir=/wheels -r requirements.txt

FROM python:3.12-slim AS runtime
WORKDIR /app
COPY --from=builder /wheels /wheels
COPY backend/ .
RUN pip install --no-index --find-links=/wheels -r requirements.txt
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

**关键收益**(若未来实施):

- wheel 层缓存复用(只重装 requirements 变更的包,~80% 加速)
- builder stage 装 pip, runtime stage 仅含应用代码(镜像体积减半)
- `COPY backend/ .` 触发层缓存,~2-5s 增量 build

---

## 3. `.dockerignore` 范本

根 `.dockerignore` 已就位(2026-07-22 落),关键排除规则:

| 模式 | 排除目标 |
|---|---|
| `data/` / `vectors/` / `cache/` / `logs/` / `tmp/` | 运行时数据卷 |
| `backend/.venv/` / `frontend/node_modules/` | 虚拟环境 |
| `frontend/dist/` | 已构建的前端产物 |
| `.env` / `.env.example` / `.env.local` | 凭据(绝不能进镜像) |
| `*.sqlite` / `*.gz` / `*.zip` / `*.tar` / `*.log` | 大文件 |
| `*.md` 默认 + `README.md` / `QUICKSTART.md` 显式保留 | 文档 |

完整内容见仓库根 `.dockerignore`。

---

## 4. 不在 v1.4.0 范围(明确推迟)

- 1.5GB 镜像体积优化(需改 Dify 1.0 源码或换 registry mirror)
- 冷启动 5-10min 加速(需改 Dify 1.0 启动逻辑或配置 `~/.docker/daemon.json` 的 `registry-mirrors`)
- backend / frontend 容器化(见 §2.2 扩展路径,等用户决策)
- COS 异地备份(`known-limitations.md §4 #5`,用户决策)
- 远程 CI(`§5 #1b`,无 git remote)
- Pester 测试(`§6 #E`,为单一脚本建立框架 ROI 太低,推 v1.4.0+ 候选)

---

## 5. 参考

- spec:`docs/superpowers/specs/2026-07-22-v1.4-dockerfile-paradigm-design.md`
- plan:`docs/superpowers/plans/2026-07-22-v1.4-dockerfile-paradigm.md`
- 评审报告 Part 2 §9.1 #3(Dockerfile 多步构建未优化):`<private>/architecture-validation-report.md:376`
```

- [ ] **Step 3.2: 验证关键章节齐**

Run:
```bash
grep -E "^## " "E:/docs/docker-build.md"
```
Expected: 至少含 5 个二级标题(## 1. / ## 2. / ## 3. / ## 4. / ## 5.)。

- [ ] **Step 3.3: 验证 §1.3 边界声明存在**

Run:
```bash
grep -A 1 "## 1.3 边界声明" "E:/docs/docker-build.md"
```
Expected: 命中 1 行 + 边界内容明确(应含"无可'多步优化'的源码"或类似表述)。

- [ ] **Step 3.4: Commit**

```bash
cd "E:/" && git add docs/docker-build.md
git commit -m "docs(v1.4.0): docker-build.md 多步构建范式 + 边界声明

- §1 当前架构: 4 个 service 容器组成表 + host 直跑组件清单
- §1.3 边界声明: 明确 v1.4.0 不解决 1.5GB / 5-10min 冷启动
- §2 v1.4.0 范式样本: dify-db-init 多阶段 Dockerfile + compose 集成
- §2.2 未来扩展路径: backend 容器化伪代码示例(非 v1.4.0 范围)
- §3 .dockerignore 范本: 关键排除规则表
- §4 不在范围: 明确推迟 6 项 (镜像体积 / 冷启动 / backend 容器化
  / COS / 远程 CI / Pester)
- §5 参考: spec + plan + 评审报告路径锚点"
```

---

## Task 4: 配套文档同步(known-limitations + CHANGELOG + AGENTS + version + README)

**Files:**
- Modify: `E:/docs/known-limitations.md`(§4 #3 状态 + §6 backlog)
- Modify: `E:/CHANGELOG.md`(顶部新增 `[1.4.0]` 段)
- Modify: `E:/AGENTS.md` §13(追加 v1.4.0 节点)
- Modify: `E:/version`(`1.3.1` → `1.4.0`)
- Modify: `E:/README.md`(顶部版本行,可选)

**Interfaces:**
- Consumes: Task 1-3 的产物已 commit
- Produces: 5 个配套文档同步,v1.4.0 在所有引用点统一

### Steps

- [ ] **Step 4.1: 关闭 `known-limitations.md §4 #3`**

Read `E:/docs/known-limitations.md` 找到 §4 表格第 3 行(Dockerfile 多步构建),用 `Edit` 工具精确替换。

`old_string`(原 §4 #3 行,精确匹配):
```
| 3 | Dockerfile 多步构建未优化 | 镜像 1.5GB(冷启动 5-10 min) | 多阶段构建 + layer cache | 1d | ❌ **明确推迟** — 移到 §6 backlog(v1.3.1+ 候选,见 §6) |
```

`new_string`:
```
| 3 | Dockerfile 多步构建未优化 | 镜像 1.5GB(冷启动 5-10 min) | 多阶段构建 + layer cache | 1d | ✅ v1.4.0 关闭(`docker/dify-db-init/Dockerfile` 8 行多阶段样本 + `docker-compose.yml` 切 build + `kb-ai/dify-db-init:local` 本地 tag;**范式化交付,非性能优化**,详见 `docs/docker-build.md §1.3` 边界声明) |
```

- [ ] **Step 4.2: 从 `known-limitations.md §6` backlog 移除 Dockerfile 行**

Read `E:/docs/known-limitations.md` 找到 §6 「v1.3.0 关闭后 backlog」段,删去其中"§4 #3 Dockerfile 多步构建"那行。

说明:`§6` 当前内容(参考 `docs/known-limitations.md:117-119`):
> known-limitations §4 #3 Dockerfile 多步构建 / §4 #5 COS 异地备份 / §5 #2 架构评审 Part 2 §9 3 项 🟢 TODO,优先级待用户授权,候选 v1.3.1+。

`new_string`(v1.4.0 后):
> known-limitations §4 #5 COS 异地备份 / §5 #2 架构评审 Part 2 §9 3 项 🟢 TODO,优先级待用户授权,候选 v1.4.0+。

用 `Edit` 工具精确替换。

- [ ] **Step 4.3: 新增 `CHANGELOG.md` v1.4.0 段**

Read `E:/CHANGELOG.md` 第 1-7 行(顶部头部 + `[1.3.1]` 段起点)。在头部 H1 标题之后、`## [1.3.1]` 之前插入新段。

`old_string`(在 `## [1.3.1] - 2026-07-22` 这行前):
```
## [1.3.1] - 2026-07-22
```

`new_string`(先插入 v1.4.0 段,然后保留 v1.3.1 段头):
```
## [1.4.0] - 2026-07-22

### Dockerfile 多步构建范式化交付(minor bump,工程性 + 文档性)

> 目标:把 known-limitations §4 #3「Dockerfile 多步构建未优化」以范式化交付形式关闭,为未来 backend 容器化预留多阶段构建入口。
> 边界:**v1.4.0 不解决 1.5GB 镜像体积 / 冷启动 5-10min**(无可优化源码),仅落地范式 + .dockerignore + 边界文档。

### 变更

- **Item 1 dify-db-init 多阶段构建范式**:新建 `docker/dify-db-init/Dockerfile`(8 行,builder 装 sqlite / runtime COPY 二进制);`docker-compose.yml` `dify-db-init` service 从 `image: alpine:3.19` 切到 `build: + image: kb-ai/dify-db-init:local`(**新增本地 tag**,非 bump 已锁版官方 tag;spec §1.4 已显式声明并经用户授权);`entrypoint:` / `command:` 保留 v1.3.1 字面量,行为完全等价
- **Item 2 根 `.dockerignore` 范本**:新建 32 行 .dockerignore,排除运行时数据卷 / 虚拟环境 / 前端 dist / .env / 大文件;当前项目无完整 Dockerfile,本文件作为未来构建入口的范本
- **Item 3 `docs/docker-build.md` 边界文档**:新建,含 §1 当前架构 + §1.3 边界声明 + §2 范式样本 + §2.2 未来 backend 容器化扩展路径 + §3 .dockerignore 说明 + §4 不在 v1.4.0 范围

### 连带更新

- `docs/known-limitations.md §4 #3` ❌ → ✅;§6 backlog 移除该项
- `AGENTS.md §13` 追加 v1.4.0 节点
- `version` 1.3.1 → 1.4.0
- `README.md` 顶部版本行 `(v1.3.1)` → `(v1.4.0)`

### 单测总数

- 基线 v1.3.1 release `0026239+` = pytest 292 / vitest 13
- v1.4.0 增量:**0 测**(纯构建 + 文档层,无业务逻辑改动)
- release HEAD 总数:pytest 292 / vitest 13 / ruff clean / tsc clean / vite build clean
- FastAPI 路由:20(无变化)
- 前端影响:零

### 不修改(系统策略)

- ❌ `.env` / `package.bat` / `start.bat` / `stop.bat`
- ❌ 容器镜像 tag(qdrant:1.7.0 / dify-api:1.0.0 × 2 / alpine:3.19 在 Dockerfile FROM 中锁版)
- ❌ `architecture-validation-report.md` Part 1

### 内部重构(零行为变更)

- `dify-db-init` 容器从 `image: alpine:3.19` 切到 `build: + image: kb-ai/dify-db-init:local`;**`command:` shell 脚本与 v1.3.1 字面完全相同**,alpine 共享 layer + apk add 在 build 阶段完成,运行时 `apk add` 仍执行(幂等,二次装包 < 0.5s)

## [1.3.1] - 2026-07-22
```

- [ ] **Step 4.4: 在 `AGENTS.md §13` 追加 v1.4.0 节点**

Read `E:/AGENTS.md` 第 13 节变更记录表格,找到末尾("v1.3.1 - 2026-07-22"那行后),用 `Edit` 工具追加一行。

`old_string`(v1.3.1 那行末尾):
```
| 2026-07-22 | v1.3.1 | **cost-alert 鲁棒性强化(patch)**:whole-branch review §6 A-D 4 项收口 — 日志轮转(50MB gzip 归档)+ UTC 规范化(`DateTimeOffset`)+ `safe_get_usage_tokens` 防御性解析 + `validate_cost_alert_payload` 字段级校验;pytest 275 → 292(+17);详见 CHANGELOG.md v1.3.1 + `docs/superpowers/specs/2026-07-22-v1.3.1-cost-alert-hardening-design.md` |
```

`new_string`(在末尾添加 v1.4.0 行):
```
| 2026-07-22 | v1.3.1 | **cost-alert 鲁棒性强化(patch)**:whole-branch review §6 A-D 4 项收口 — 日志轮转(50MB gzip 归档)+ UTC 规范化(`DateTimeOffset`)+ `safe_get_usage_tokens` 防御性解析 + `validate_cost_alert_payload` 字段级校验;pytest 275 → 292(+17);详见 CHANGELOG.md v1.3.1 + `docs/superpowers/specs/2026-07-22-v1.3.1-cost-alert-hardening-design.md` |
| 2026-07-22 | v1.4.0 | **Dockerfile 多步构建范式化交付(minor)**:范式化关闭 known-limitations §4 #3(不解决 1.5GB 镜像 / 5-10min 冷启动,仅落地多阶段样本);3 项 — `docker/dify-db-init/Dockerfile`(8 行 builder+runtime)+ `docker-compose.yml` dify-db-init 切 `build: + image: kb-ai/dify-db-init:local`(**新增本地 tag**,spec §1.4 显式声明并经用户授权)+ 根 `.dockerignore` 32 行范本 + `docs/docker-build.md` 边界文档;pytest 292 → 292(+0,纯构建/文档层);详见 CHANGELOG.md v1.4.0 + `docs/superpowers/specs/2026-07-22-v1.4-dockerfile-paradigm-design.md` + `docs/superpowers/plans/2026-07-22-v1.4-dockerfile-paradigm.md` |
```

- [ ] **Step 4.5: 更新 `E:/version` 文件**

用 `Write` 工具覆盖整个文件(只含 1 行):
```
1.4.0
```

Verify:
```bash
cat "E:/version"
```
Expected: 输出 `1.4.0`。

- [ ] **Step 4.6: 更新 `E:/README.md` 顶部版本行**

Read `E:/README.md` 第 1-10 行,找到版本行(如 `KB-AI (v1.3.1)`)。

用 `Edit` 工具精确替换:
- `old_string`:`(v1.3.1)` 或类似当前版本
- `new_string`:`(v1.4.0)`

如果 README 中没有版本行,可跳过本步骤(spec §1.3 标注为"可选")。

- [ ] **Step 4.7: 验证所有 v1.4.0 引用一致**

Run:
```bash
cd "E:/" && grep -rn "1\.4\.0" --include="*.md" --include="version" 2>&1 | grep -v node_modules
```
Expected: 至少命中 4 个文件(`CHANGELOG.md` / `AGENTS.md` / `docs/known-limitations.md` / `version`)。如果 README 改了,可能 5 个。

同时确认无残留 `1.3.1`:
```bash
cd "E:/" && grep -n "1\.3\.1" CHANGELOG.md version 2>&1
```
Expected: `CHANGELOG.md` 中应保留 `## [1.3.1]` 历史段(但顶部应是 v1.4.0);`version` 文件应只有 `1.4.0`。

- [ ] **Step 4.8: Commit**

```bash
cd "E:/" && git add docs/known-limitations.md CHANGELOG.md AGENTS.md version README.md
git commit -m "docs(v1.4.0): 配套文档同步(known-limitations + CHANGELOG + AGENTS + version + README)

- known-limitations.md §4 #3 状态 ❌ -> ✅ (§6 backlog 移除该项)
- CHANGELOG.md 顶部新增 [1.4.0] 段 (Item 1/2/3 + 连带更新 +
  单测总数 + 不修改策略 + 内部重构)
- AGENTS.md §13 追加 v1.4.0 节点行 (引用 spec + plan 路径)
- version: 1.3.1 -> 1.4.0
- README.md 顶部版本行 (v1.3.1) -> (v1.4.0) [可选, 若存在]"
```

---

## Task 5: 全套验证 + release commit

**Files:**
- 无新文件 / 无修改;纯验证

**Interfaces:**
- Consumes: Task 1-4 所有 commit
- Produces: 1 个 release commit(如需要标签则单独处理,本 plan 不涉及 git tag)

### Steps

- [ ] **Step 5.1: 运行全套门禁**

Run:
```bash
cd "E:/" && pwsh -File scripts/run-checks.ps1
```
Expected: 4/4 全绿(ruff clean / pytest 292 pass / eslint clean / vite build clean)。若任何一项失败,**必须修复**或确认 v1.4.0 改动未触及对应路径,然后重跑。

注意:本 plan 无业务代码改动,pytest 仍为 292 测,vite build 仍为前端 dist 输出。

- [ ] **Step 5.2: 验证 compose 解析干跑**

Run:
```bash
cd "E:/" && docker compose config -q && echo "compose OK"
```
Expected: 无输出(stderr)+ `compose OK`,exit 0。

若 `docker compose config` 不在 PATH:
```bash
cd "E:/" && docker compose config > /tmp/compose-config.yaml && head -50 /tmp/compose-config.yaml
```
Expected: 输出含 dify-db-init 的 build 配置。

- [ ] **Step 5.3: 验证镜像 tag 锁版红线**

Run:
```bash
docker images --format "{{.Repository}}:{{.Tag}}" | grep -E "^(qdrant/qdrant:1\.7\.0|langgenius/dify-api:1\.0\.0|alpine:3\.19)$" | sort -u
```
Expected: 至少命中 3 行(qdrant/qdrant:1.7.0 / langgenius/dify-api:1.0.0 / alpine:3.19)。

如果某些 tag 不在本地 docker cache(未拉过),该行不会出现 — 这正常;只要 `docker pull <tag>` 仍能拉到对应版本即可,无 API key 限制。

- [ ] **Step 5.4: 验证 git 改动清单符合预期**

Run:
```bash
cd "E:/" && git log --oneline HEAD~4..HEAD
```
Expected: 4 个 commit 出现(T1 范式 + T2 .dockerignore + T3 docker-build.md + T4 文档同步),顺序与本 plan 一致。

Run:
```bash
cd "E:/" && git diff HEAD~4..HEAD --stat
```
Expected: 改动文件仅含 spec §1.2 + §1.3 列出的清单:
- `docker/dify-db-init/Dockerfile`(new)
- `.dockerignore`(new)
- `docs/docker-build.md`(new)
- `docker-compose.yml`(modified,仅 dify-db-init service)
- `docs/known-limitations.md`(modified,§4 #3 + §6 backlog)
- `CHANGELOG.md`(modified,顶部新增段)
- `AGENTS.md`(modified,§13 表格追加)
- `version`(modified,1.3.1 → 1.4.0)
- `README.md`(modified,版本行,可选)

**绝对不应出现**的改动:`backend/` / `frontend/src/` / `tests/` / `scripts/` / `.env` / `start.bat` / `stop.bat` / `package.bat` / `architecture-validation-report.md`。

若 `git diff --stat` 出现非预期文件,**必须 revert 该 commit 并排查**。

- [ ] **Step 5.5: 验证 spec §1.5 全部 8 项成功标准**

逐项确认(手动勾选):

1. ✅ §1.2 3 项 item 全部落地,3 个 commit 可独立 revert — 由 Step 5.4 验证
2. ✅ `docker compose config` 干跑 exit 0,dify-db-init 显示 `build:` 字段 — 由 Step 5.2 验证
3. ✅ `docker compose build dify-db-init` 成功生成镜像,体积 ≤ 15MB — 由 Task 1 Step 1.4 验证
4. ✅ `docker compose up dify-db-init` 一次成功(db.sqlite 创建 + WAL 启用 + exit 0)— **手动验证**(需 `docker compose up -d dify-api` 触发 db.sqlite 创建,再 `docker compose up dify-db-init` 验证 WAL 启用)。若 docker 环境不可用,可跳过此步,记录 "docker up skipped: 环境限制"
5. ✅ `scripts/run-checks.ps1` 4/4 全绿 — 由 Step 5.1 验证
6. ✅ `docs/known-limitations.md §4 #3` 状态为 ✅;§6 backlog 移除该项 — 由 Task 4 Step 4.1/4.2 验证
7. ✅ `git diff` review 仅触及预期文件,无意外修改 — 由 Step 5.4 验证
8. ✅ release commit 在最后;`version` 文件 = `1.4.0`;`CHANGELOG.md` 顶部 v1.4.0 段已写;`AGENTS.md §13` 含 v1.4.0 节点 — 由 Task 4 + 本 Task 验证

- [ ] **Step 5.6: Release commit(可选,若需显式 release marker)**

如果项目惯例不要求 release commit(看 `git log`:`acee369 release(v1.3.0): 运维加固与文档同步` 与 `95cc652 release(v1.3.1): cost-alert hardening` 都用了 `release(...)` 前缀的 release commit 作为版本标记),则添加一个 release commit:

```bash
cd "E:/" && git commit --allow-empty -m "release(v1.4.0): Dockerfile 多步构建范式化交付

- spec: docs/superpowers/specs/2026-07-22-v1.4-dockerfile-paradigm-design.md
- plan: docs/superpowers/plans/2026-07-22-v1.4-dockerfile-paradigm.md
- closes known-limitations §4 #3
- pytest 292 / vitest 13 / ruff clean / tsc clean / vite build clean
- FastAPI 路由 20 (无变化)
- 前端影响 0
- 详见 CHANGELOG.md v1.4.0 段"
```

如果项目惯例不要求空 release commit(看历史,v1.0 / v1.1 / v1.2 没看到 release commit,v1.3.0 / v1.3.1 才有),则**跳过本步骤**,T1-T4 4 个 commit 即为 v1.4.0 全部交付。

判断标准:
- 查看最近 5 个 release:`git log --oneline --grep="^release" -5`
- 若 v1.3.0 / v1.3.1 都有 release commit,**保留此步骤**
- 否则**跳过此步骤**(spec §5 原文:"1 个范式 commit + 1 个文档 commit + 1 个 release commit"是 spec 的建议,但历史 v1.0-v1.2 没强制)

---

## Self-Review

### 1. Spec coverage

逐项检查 spec `docs/superpowers/specs/2026-07-22-v1.4-dockerfile-paradigm-design.md` 需求是否被本 plan 覆盖:

| Spec 章节 | 覆盖 Task | 备注 |
|---|---|---|
| §1.1 核心边界声明 | (设计层面,plan 不直接覆盖) | spec 锁定即可 |
| §1.2 Item 1 dify-db-init 多阶段 Dockerfile | Task 1 | ✅ |
| §1.2 Item 2 根 `.dockerignore` 范本 | Task 2 | ✅ |
| §1.2 Item 3 `docs/docker-build.md` | Task 3 | ✅ |
| §1.3 连带更新(known-limitations / CHANGELOG / AGENTS / version / README) | Task 4 | ✅ |
| §1.4 不变量(红线) | Global Constraints + Task 5 Step 5.4 验证 | ✅ |
| §1.5 成功标准 8 项 | Task 5 Step 5.5 | ✅ |
| §2.1 Item 1 详细设计(8 行 Dockerfile 内容) | Task 1 Step 1.2 | ✅ |
| §2.1 compose yml 改动精确内容 | Task 1 Step 1.5 | ✅ |
| §2.2 .dockerignore 32 行精确内容 | Task 2 Step 2.1 | ✅ |
| §2.3 docker-build.md 精确内容 | Task 3 Step 3.1 | ✅ |
| §3 数据流与集成 | (设计层面,plan 不直接覆盖) | ✅ 由 compose config 干跑验证 |
| §4.1 测试策略(回归 + config + build + up) | Task 5 Step 5.1/5.2/5.4/5.5 | ✅ |
| §4.2 验证清单 15 项 | Task 5 Step 5.5 | ✅(其中 1 项 docker up 标"环境限制"跳过) |
| §4.3 风险与缓解 7 项 | Global Constraints + Task 1-5 各步骤 | ✅ |
| §4.4 不在范围 7 项 | Task 3 Step 3.1 §4 | ✅ |

### 2. Placeholder scan

检查无以下占位符:
- ❌ "TBD" / "TODO" / "implement later" / "fill in details" — 无
- ❌ "Add appropriate error handling" — 无
- ❌ "Write tests for the above" — 无(本 plan 无新增测,已显式声明 0 测)
- ❌ "Similar to Task N" — 无
- ❌ 步骤描述"做什么"而无"怎么做" — 所有 code/docker/yaml 步骤均含完整代码块
- ❌ 引用未定义类型/函数 — 无

### 3. Type consistency

- 文件路径一致:全 plan 用 `E:/docker/dify-db-init/Dockerfile` / `E:/.dockerignore` / `E:/docs/docker-build.md` / `E:/docker-compose.yml`,无拼写差异
- commit message 一致:T1 用 `feat(v1.4.0): dify-db-init 多阶段构建范式样本`;T2/T3/T4 用 `feat(v1.4.0):` 或 `docs(v1.4.0):` 前缀;release commit 用 `release(v1.4.0):` 前缀
- 版本号一致:`1.4.0` 全程
- 镜像 tag 一致:`kb-ai/dify-db-init:local` / `alpine:3.19` / `qdrant/qdrant:v1.7.0` / `langgenius/dify-api:1.0.0`,无混淆
- spec / plan 路径锚点一致:`docs/superpowers/specs/2026-07-22-v1.4-dockerfile-paradigm-design.md` / `docs/superpowers/plans/2026-07-22-v1.4-dockerfile-paradigm.md`

### 4. 已知小限制

- Task 5 Step 5.5 第 4 项 (`docker compose up dify-db-init`) 需 docker 环境 + 触发 dify-api 启动(可能消耗本地资源且需 API key);spec §4.2 接受"环境限制"作为跳过理由。本 plan 显式标注该步骤为"手动验证 / 可选跳过"
- Task 5 Step 5.6 release commit 是否必做,取决于项目惯例(本 plan 用"可选"处理,实施者看 git 历史决定)

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-07-22-v1.4-dockerfile-paradigm.md`.

Two execution options:

1. **Subagent-Driven (recommended)** — Dispatch a fresh subagent per task, review between tasks, fast iteration
2. **Inline Execution** — Execute tasks in this session using executing-plans, batch execution with checkpoints

Which approach?